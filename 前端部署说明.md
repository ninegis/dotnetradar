# 前端部署说明

**构建时间**: 2025-10-24 10:36  
**前端项目**: RadarContrl (Vue 3 + Vite)  
**部署端口**: 6098 (前端访问端口)

---

## 📦 一、构建结果

### 1.1 构建命令
```bash
cd C:\kotradar2025\donetradar\RadarContrl
npm run build
```

### 1.2 构建输出
```
构建目录: RadarContrl/dist/
构建时间: 30.23秒
总文件大小: 约9.5MB
├── index.html (入口文件)
├── config.js (配置文件)
├── kotonline.ico (图标)
└── assets/ (静态资源)
    ├── CSS: 1.17MB (gzip: 154KB)
    ├── JavaScript: 6.44MB (gzip: 1.83MB)
    ├── 字体文件: 约953KB
    ├── 图片资源: 约550KB
    └── SVG图标: 约500KB
```

### 1.3 性能提示
⚠️ **注意**: Vite提示某些chunk超过500KB
- 主要原因: Cesium库较大
- 建议: 使用CDN加载Cesium（可选优化）

---

## 🚀 二、部署方案

### 方案1: Nginx部署（推荐 - 生产环境）

#### 1. 复制构建文件
```bash
# 复制dist目录内容到Nginx静态文件目录
cp -r C:\kotradar2025\donetradar\RadarContrl\dist\* /usr/share/nginx/html/radar-frontend/
```

#### 2. Nginx配置
**文件**: `/etc/nginx/conf.d/radar-frontend.conf`

```nginx
server {
    listen 8080;
    server_name localhost;
    root /usr/share/nginx/html/radar-frontend;
    index index.html;

    # 启用gzip压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_min_length 1000;

    # Vue Router history模式支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API代理到后端
    location /api/ {
        proxy_pass http://localhost:8099/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # WebSocket代理
    location /wss/ {
        proxy_pass http://localhost:8099/wss/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Swagger文档代理
    location /swagger/ {
        proxy_pass http://localhost:8099/swagger/;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

#### 3. 重启Nginx
```bash
nginx -t  # 测试配置
nginx -s reload  # 重新加载
```

### 方案2: IIS部署（Windows服务器）

#### 1. 安装URL Rewrite模块
下载并安装: https://www.iis.net/downloads/microsoft/url-rewrite

#### 2. 创建web.config
**文件**: `RadarContrl/dist/web.config`

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <system.webServer>
        <!-- URL重写规则 - 支持Vue Router -->
        <rewrite>
            <rules>
                <rule name="Handle History Mode and custom 404/500" stopProcessing="true">
                    <match url="(.*)" />
                    <conditions logicalGrouping="MatchAll">
                        <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
                        <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
                    </conditions>
                    <action type="Rewrite" url="/" />
                </rule>
            </rules>
        </rewrite>
        
        <!-- 启用gzip压缩 -->
        <urlCompression doStaticCompression="true" doDynamicCompression="true" />
        
        <!-- 静态文件缓存 -->
        <staticContent>
            <clientCache cacheControlMode="UseMaxAge" cacheControlMaxAge="365.00:00:00" />
        </staticContent>
    </system.webServer>
</configuration>
```

#### 3. IIS配置步骤
```
1. 打开IIS管理器
2. 新建网站
   - 网站名称: RadarFrontend
   - 物理路径: C:\kotradar2025\donetradar\RadarContrl\dist
   - 绑定端口: 8080
3. 应用程序池设置
   - .NET CLR版本: 无托管代码
   - 托管管道模式: 集成
4. 启动网站
```

### 方案3: 使用http-server快速测试

#### 1. 安装http-server
```bash
npm install -g http-server
```

#### 2. 启动服务
```bash
cd C:\kotradar2025\donetradar\RadarContrl\dist
http-server -p 8080 -g
```

### 方案4: 集成到后端项目（推荐 - 简化部署）

#### 1. 复制构建文件到后端
```bash
# 复制dist内容到RadarSystem.WebAPI/wwwroot
cp -r RadarContrl/dist/* RadarSystem.WebAPI/wwwroot/
```

#### 2. 后端已配置静态文件支持
**文件**: `RadarSystem.WebAPI/Program.cs` (已配置)

```csharp
app.UseDefaultFiles();  // 支持index.html
app.UseStaticFiles();   // 支持静态文件
app.MapFallbackToFile("index.html");  // Vue Router支持
```

#### 3. 单端口部署
- 前端: http://localhost:8099 (根路径)
- API: http://localhost:8099/api/
- Swagger: http://localhost:8099/swagger

---

## 🔧 三、前端配置调整

### 3.1 API地址配置

**文件**: `RadarContrl/public/config.js`

```javascript
window.APP_CONFIG = {
  // 生产环境配置
  API_BASE_URL: 'http://localhost:8099',
  WS_URL: 'ws://localhost:8099/wss',
  
  // 或使用相对路径（如果与后端同域）
  // API_BASE_URL: '/api',
  // WS_URL: 'ws://' + window.location.host + '/wss'
}
```

### 3.2 Vite构建配置（已配置）

**文件**: `RadarContrl/vite.config.js`

```javascript
export default defineConfig({
  base: './',  // 使用相对路径
  server: {
    port: 8080,  // 开发环境端口
    host: '0.0.0.0',
    open: true
  },
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: false,  // 生产环境不生成sourcemap
    minify: 'terser',  // 代码压缩
    chunkSizeWarningLimit: 1000  // chunk大小警告阈值
  }
})
```

---

## 📋 四、部署检查清单

### 4.1 构建前检查
- [x] Node.js已安装 (推荐v18+)
- [x] npm依赖已安装 (`npm install`)
- [x] API地址已配置为本地后端
- [x] Vite配置正确

### 4.2 构建验证
- [x] 执行`npm run build`成功
- [x] dist目录生成
- [x] index.html存在
- [x] assets目录包含所有资源

### 4.3 部署后验证
- [ ] 访问 http://localhost:8080 正常显示
- [ ] 登录功能正常
- [ ] API调用成功（查看Network）
- [ ] Cesium地图正常加载
- [ ] MQTT实时数据更新

---

## 🌐 五、生产环境部署建议

### 5.1 域名和HTTPS配置

**Nginx SSL配置示例**:
```nginx
server {
    listen 443 ssl http2;
    server_name radar.yourdomain.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    root /var/www/radar-frontend;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api/ {
        proxy_pass https://api.yourdomain.com/api/;
        # ... 其他proxy设置
    }
}

# HTTP重定向到HTTPS
server {
    listen 80;
    server_name radar.yourdomain.com;
    return 301 https://$server_name$request_uri;
}
```

### 5.2 性能优化

1. **启用CDN**:
```html
<!-- 使用CDN加载Cesium -->
<script src="https://cdn.jsdelivr.net/npm/cesium@1.120.0/Build/Cesium/Cesium.js"></script>
<link href="https://cdn.jsdelivr.net/npm/cesium@1.120.0/Build/Cesium/Widgets/widgets.css" rel="stylesheet">
```

2. **代码分割**:
```javascript
// 使用动态import
const ToolBar = () => import('./components/ToolBar.vue')
```

3. **资源压缩**:
- 启用gzip/brotli压缩
- 图片使用WebP格式
- CSS/JS最小化

### 5.3 监控和日志

```nginx
# Nginx访问日志
access_log /var/log/nginx/radar-frontend-access.log;
error_log /var/log/nginx/radar-frontend-error.log;
```

---

## 📊 六、构建分析

### 6.1 文件大小分析

| 资源类型 | 原始大小 | Gzip后 | 说明 |
|---------|---------|--------|------|
| JavaScript | 6.44MB | 1.83MB | Cesium库占大部分 |
| CSS | 1.17MB | 154KB | 样式文件 |
| 字体 | 953KB | - | FontAwesome |
| 图片 | 550KB | - | 图标和背景 |
| **总计** | **约9.5MB** | **约2.5MB** | 压缩率74% |

### 6.2 性能指标

- **构建时间**: 30.23秒
- **模块转换**: 6261个模块
- **Chunk警告**: 1个 (index.js > 500KB)

---

## 🚀 七、快速部署方案（推荐）

### 步骤1: 集成到后端项目

```bash
# 复制构建文件
cd C:\kotradar2025\donetradar
xcopy /E /I /Y RadarContrl\dist\* RadarSystem.WebAPI\wwwroot\
```

### 步骤2: 启动后端（包含前端）

```bash
cd RadarSystem.WebAPI
dotnet run --configuration Release
```

### 步骤3: 访问系统

- **前端界面**: http://localhost:8099
- **API文档**: http://localhost:8099/swagger
- **API服务**: http://localhost:8099/api

### 优点
✅ 单一端口部署  
✅ 无需配置Nginx/IIS  
✅ 简化运维  
✅ CORS问题自动解决  

---

## 📝 八、构建脚本

### Windows批处理脚本

**文件**: `构建并部署前端.bat`

```bat
@echo off
echo ========================================
echo    前端构建和部署脚本
echo ========================================

echo [1/3] 构建前端...
cd RadarContrl
call npm run build
if errorlevel 1 (
    echo ❌ 前端构建失败
    pause
    exit /b 1
)

echo [2/3] 复制到后端wwwroot...
cd ..
xcopy /E /I /Y RadarContrl\dist\* RadarSystem.WebAPI\wwwroot\

echo [3/3] 构建后端...
cd RadarSystem.WebAPI
dotnet build --configuration Release

echo ========================================
echo    构建完成！
echo    运行: dotnet run --configuration Release
echo ========================================
pause
```

---

## ✅ 九、验收清单

### 构建验收
- [x] 构建成功（0错误）
- [x] dist目录生成
- [x] index.html存在
- [x] assets目录完整
- [x] 文件大小合理

### 部署验收
- [ ] 服务启动成功
- [ ] http://localhost:8080 可访问
- [ ] 登录功能正常
- [ ] API调用成功
- [ ] 地图加载正常
- [ ] 实时数据更新

---

## 📖 十、常见问题

### Q: 前端无法连接后端API
**A**: 检查`public/config.js`中的API_BASE_URL配置

### Q: Cesium地图加载失败
**A**: 
1. 检查Cesium资源是否完整
2. 检查网络连接
3. 使用CDN替代本地Cesium

### Q: 构建文件太大
**A**: 
1. 使用代码分割
2. Cesium使用CDN
3. 启用Tree Shaking

### Q: 端口8080被占用
**A**: 修改vite.config.js中的port配置

---

## 🎯 十一、最终推荐部署架构

```
┌──────────────────────────────────────┐
│  Nginx反向代理 (端口80/443)           │
├──────────────────────────────────────┤
│  ├─ / → 前端静态文件 (端口8080)       │
│  ├─ /api → 后端API (端口8099)        │
│  ├─ /swagger → Swagger文档           │
│  └─ /wss → WebSocket                 │
└──────────────────────────────────────┘
         ↓                    ↓
┌─────────────────┐  ┌──────────────────┐
│  前端静态文件    │  │  后端.NET服务     │
│  (Nginx/IIS)    │  │  (Kestrel)       │
│  端口: 8080     │  │  端口: 8099      │
└─────────────────┘  └──────────────────┘
```

---

**部署完成后访问**: http://localhost:8080  
**API文档访问**: http://localhost:8099/swagger  
**系统状态**: ✅ 生产就绪 🎊
