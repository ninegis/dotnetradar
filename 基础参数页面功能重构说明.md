# 基础参数页面功能重构说明

**重构时间**: 2025-10-25  
**目标**: 将"基础参数设置"拆分为"保存基础信息"和"下发雷达参数"两个独立操作

---

## 🎯 重构目标

### 用户需求

**原有问题**:
- 一个按钮同时做两件事：保存基础信息 + 下发雷达参数
- 如果指令下发失败，用户误以为保存也失败了
- 无法区分哪一步出了问题

**重构目标**:
- **按钮1**: 保存基础信息（直接入库，不下发指令）
- **按钮2**: 下发雷达参数（先下发指令，成功后再入库）
- 职责明确，操作清晰

---

## 📋 功能拆分

### 功能1: 保存基础信息

**按钮名称**: "保存基础信息"

**包含字段**（直接保存到数据库，不下发指令）:
- 设备名称
- 出厂ID (FactoryId)
- 经度 (Longitude)
- 纬度 (Latitude)
- 高程 (Elevation)
- 零点朝向 (Orientation)

**操作流程**:
```
用户修改基础信息
  ↓
点击"保存基础信息"
  ↓
验证必填字段
  ↓
PUT /api/Device/{deviceId}
  ↓
更新 Devices表
  ↓
✅ 返回成功
  ↓
重新加载设备信息
  ↓
提示"基础信息保存成功" ✅
```

**数据库表**: `Devices`

**不需要下发指令**: 这些信息是设备的静态配置，不需要发送到雷达硬件

---

### 功能2: 下发雷达参数

**按钮名称**: "下发雷达参数"

**包含字段**（先下发指令，成功后再入库）:
- 起始角度 (ImgAngleStart)
- 终止角度 (ImgAngleEnd)
- 最近距离 (RngMin)
- 最远距离 (RngMax)
- 频段选择 (FreqBand)
- 天线波束角 (AnteBeam_half)
- 刷新频率 (UpdateTime - MIMO Lite专用)
- 模型选择 (ModelSelect - MIMO Lite专用)

**操作流程**:
```
用户修改雷达参数
  ↓
点击"下发雷达参数"
  ↓
验证参数（角度、距离不能小于0）
  ↓
步骤1: 发送指令到雷达设备
  ├─ MIMO Lite: POST /api/mimoLite/command/{projectId}/{deviceId}/11/{userName}
  └─ ArcSAR: POST /api/arcsar/command/{projectId}/{deviceId}/11/{userName}
  ↓
检查指令响应
  ├─ ✅ 成功 (200) → 继续步骤2
  └─ ❌ 失败 (405/500) → 提示"指令发送失败"，保持原有参数值 ⚠️
  ↓
步骤2: 保存参数到数据库（只在指令成功时执行）
  ├─ MIMO Lite: POST /api/protocol/update/radar/mimolite/param
  └─ ArcSAR: POST /api/protocol/update/radar/param
  ↓
更新 radar_params表
  ↓
✅ 返回成功
  ↓
重新加载设备信息
  ↓
提示"雷达参数下发并保存成功" ✅
```

**数据库表**: `radar_params`

**必须下发指令**: 这些参数需要发送到雷达硬件才能生效

---

## 🔄 关键逻辑

### 为什么先发指令再入库？

**原因**:
1. **确保参数有效**: 如果雷达硬件拒绝了参数，数据库中不应该保存错误的参数
2. **保持一致性**: 数据库中的参数应该是雷达当前实际使用的参数
3. **错误提示明确**: 如果指令失败，用户知道雷达没有更新，数据库也不会更新

**流程**:
```
下发指令成功 → 保存到数据库 ✅
下发指令失败 → 不保存到数据库，保持原有值 ⚠️
```

---

## 📝 代码实现

### 前端页面修改

**文件**: `RadarContrl/src/components/ToolBar/Tool/RadarParams/RadarParams.vue`

#### 1. 修改按钮

**修改前**:
```vue
<a-button @click="commitUpdate">基础参数设置</a-button>
```

**修改后**:
```vue
<a-button @click="saveBasicInfo">保存基础信息</a-button>
<a-button @click="sendRadarParams">下发雷达参数</a-button>
```

#### 2. 实现`saveBasicInfo`方法

```javascript
const saveBasicInfo = async () => {
  // 验证必填字段
  if (!store.radarInfo.deviceName) {
    showMessage('设备名称不能为空', 'error');
    return;
  }
  
  // 准备基础信息
  const basicInfo = {
    deviceName: store.radarInfo.deviceName,
    factoryId: currentDevice.factoryId,
    longitude: currentDevice.longitude,
    latitude: currentDevice.latitude,
    elevation: currentDevice.elevation,
    orientation: currentDevice.orientation
  };
  
  // ✅ 直接调用PUT /api/Device/{id}
  const res = await axios.put(
    ApiRadar.apiUrl + '/api/Device/' + store.radarInfo.deviceId,
    basicInfo
  );
  
  if (res.data && res.data.code === 200) {
    showMessage('基础信息保存成功');
    // 重新加载设备信息
  }
};
```

#### 3. 实现`sendRadarParams`方法

```javascript
const sendRadarParams = async () => {
  // 准备雷达参数
  const radarParams = {
    projectId: store.radarInfo.projectId,
    deviceId: store.radarInfo.deviceId,
    ImgAngleStart: params['ImgAngleStart'],
    ImgAngleEnd: params['ImgAngleEnd'],
    RngMin: params['RngMin'],
    RngMax: params['RngMax'],
    FreqBand: params['FreqBand'],
    AnteBeam_half: params['AnteBeam_half']
  };
  
  // ✅ 步骤1: 发送指令到雷达设备
  let commandResult;
  if (currentRadar.value === 'MIMOLITE') {
    commandResult = await ApiRadar.setPushiRadarParamControl(projectId, deviceId);
  } else {
    commandResult = await ApiRadar.setParamControl(projectId, deviceId);
  }
  
  // ✅ 步骤2: 只有指令成功才保存到数据库
  if (commandResult && commandResult.status === 200) {
    const saveRes = await axios.post(saveUrl, radarParams);
    
    if (saveRes.data && saveRes.data.code === 200) {
      showMessage('雷达参数下发并保存成功');
    }
  } else {
    showMessage('指令发送失败，参数未保存到数据库', 'warning');
  }
};
```

---

## 🎨 用户界面

### 页面布局

```
┌─────────────────────────────────────┐
│  项目配置 > 基础参数                  │
├─────────────────────────────────────┤
│ [保存基础信息] [下发雷达参数]         │
├─────────────────────────────────────┤
│ 项目: [下拉框]                        │
│ 设备: [下拉框] [+] [-]                │
│                                       │
│ === 基础信息 ===                      │
│ 设备ID: [MIMO_xxx] (只读)             │
│ 设备名称: [输入框] ← 可修改            │
│ 出厂ID: [输入框] ← 可修改              │
│ 经度: [数字框] ← 可修改                │
│ 纬度: [数字框] ← 可修改                │
│ 高程: [数字框] ← 可修改                │
│ 零点朝向: [数字框] ← 可修改            │
│                                       │
│ === 雷达参数 ===                      │
│ 起始角度: [数字框] ← 可修改            │
│ 终止角度: [数字框] ← 可修改            │
│ 最小距离: [数字框] ← 可修改            │
│ 最大距离: [数字框] ← 可修改            │
│ 频段: [下拉框] ← 可修改                │
│ 天线波束角: [数字框] ← 可修改          │
│ (MIMO专用) 刷新频率: [输入框]         │
└─────────────────────────────────────┘
```

### 操作说明

1. **只修改设备名称、坐标等基础信息**
   - 点击"保存基础信息"
   - 立即保存到数据库
   - 不影响雷达运行

2. **修改起始角、距离等雷达参数**
   - 点击"下发雷达参数"
   - 先发送指令到雷达硬件
   - 雷达确认成功后，再保存到数据库
   - 如果雷达拒绝，数据库保持原值

3. **同时修改基础信息和雷达参数**
   - 先点击"保存基础信息"
   - 再点击"下发雷达参数"
   - 分两步操作更安全

---

## ✅ 优势

### 修改前（一个按钮）

| 问题 | 影响 |
|-----|------|
| 功能混乱 | 用户不知道具体做了什么 |
| 错误不明确 | 不知道是保存失败还是指令失败 |
| 无法单独操作 | 只想改名称也要下发指令 |

### 修改后（两个按钮）

| 优势 | 说明 |
|-----|------|
| 职责清晰 | 基础信息和雷达参数分开 |
| 错误明确 | 知道是保存失败还是指令失败 |
| 操作灵活 | 可以只保存基础信息，不影响雷达运行 |
| 安全性高 | 指令失败不会污染数据库 |

---

## 🔄 完整流程图

### 场景1: 只修改设备名称

```
用户修改设备名称
  ↓
点击"保存基础信息"
  ↓
PUT /api/Device/{deviceId}
  ↓
更新 Devices.DeviceName
  ↓
✅ 成功 (200)
  ↓
提示"基础信息保存成功"
  ↓
重新加载设备信息
  ↓
页面显示新的设备名称 ✅
```

### 场景2: 只下发雷达参数

```
用户修改起始角度
  ↓
点击"下发雷达参数"
  ↓
步骤1: POST /api/{radarType}/command/.../11/qingqiangjia
  ↓
雷达硬件接收指令
  ├─ ✅ 接受 (200) → 继续步骤2
  └─ ❌ 拒绝 (405/500) → 提示"指令发送失败"，结束 ⚠️
  ↓
步骤2: POST /api/protocol/update/radar/param
  ↓
更新 radar_params表
  ↓
✅ 成功 (200)
  ↓
提示"雷达参数下发并保存成功"
  ↓
重新加载设备信息
  ↓
页面显示新的雷达参数 ✅
```

### 场景3: 同时修改基础信息和雷达参数

```
用户修改设备名称和起始角度
  ↓
先点击"保存基础信息"
  ↓
✅ 设备名称已保存
  ↓
再点击"下发雷达参数"
  ↓
✅ 雷达参数已下发并保存
  ↓
两项操作都成功 ✅
```

---

## 📊 数据表对应关系

### Devices表（基础信息）

| 字段 | 前端绑定 | 说明 | 是否下发指令 |
|-----|---------|------|------------|
| DeviceName | store.radarInfo.deviceName | 设备名称 | ❌ 否 |
| FactoryId | currentDevice.factoryId | 出厂ID | ❌ 否 |
| Longitude | currentDevice.longitude | 经度 | ❌ 否 |
| Latitude | currentDevice.latitude | 纬度 | ❌ 否 |
| Elevation | currentDevice.elevation | 高程 | ❌ 否 |
| Orientation | currentDevice.orientation | 零点朝向 | ❌ 否 |

### radar_params表（雷达参数）

| 字段 | 前端绑定 | 说明 | 是否下发指令 |
|-----|---------|------|------------|
| img_angle_start | params['ImgAngleStart'] | 起始角度 | ✅ 是 |
| img_angle_end | params['ImgAngleEnd'] | 终止角度 | ✅ 是 |
| rng_min | params['RngMin'] | 最近距离 | ✅ 是 |
| rng_max | params['RngMax'] | 最远距离 | ✅ 是 |
| freq_band | params['FreqBand'] | 频段选择 | ✅ 是 |
| ante_beam_half | params['AnteBeam_half'] | 天线波束角 | ✅ 是 |
| data_version | dataVersion.value | 数据版本 | ✅ 是 |
| model_select | params['ModelSelect'] | 模型选择 | ✅ 是 (MIMO) |

---

## 🚀 测试验证

### 测试1: 保存基础信息

1. 打开"基础参数"页面
2. 修改设备名称: "雷达1" → "新雷达1"
3. 修改出厂ID: "F001" → "F002"
4. 点击"保存基础信息"
5. **预期结果**:
   - ✅ 提示"基础信息保存成功"
   - ✅ 刷新页面后，设备名称和出厂ID是新值
   - ✅ 数据库Devices表已更新
   - ✅ 没有发送指令到雷达硬件

### 测试2: 下发雷达参数

1. 修改起始角度: 0 → 10
2. 修改最大距离: 1000 → 1500
3. 点击"下发雷达参数"
4. **预期结果**:
   - ✅ 发送指令到雷达硬件
   - ✅ 雷达确认成功
   - ✅ 保存到数据库radar_params表
   - ✅ 提示"雷达参数下发并保存成功"
   - ✅ 刷新页面后，参数是新值

### 测试3: 指令发送失败场景

1. 修改起始角度
2. 点击"下发雷达参数"
3. **假设雷达离线或拒绝参数**:
   - ⚠️ 提示"指令发送失败，参数未保存到数据库"
   - ✅ 数据库中的参数保持原值
   - ✅ 刷新页面后，显示的还是原来的参数

---

## 💡 关于`11`和`qingqiangjia`

### 指令格式

```
POST /api/{radarType}/command/{projectId}/{deviceId}/{commandCode}/{userName}
```

**参数说明**:
- `radarType`: `arcsar` 或 `mimoLite`
- `projectId`: 项目ID
- `deviceId`: 设备ID
- `commandCode`: **`11`** = 参数控制指令
- `userName`: **`qingqiangjia`** = 操作用户（贾庆强）

**其他可能的指令代码**:
- `01`: 启动雷达
- `02`: 停止雷达
- `03`: 重启雷达
- `04`: 获取状态
- `11`: 下发参数（当前使用的）

---

## ✅ 修改文件清单

| 文件 | 修改内容 |
|-----|---------|
| `RadarContrl/src/components/ToolBar/Tool/RadarParams/RadarParams.vue` | 1. 添加两个按钮<br>2. 实现`saveBasicInfo`方法<br>3. 实现`sendRadarParams`方法<br>4. 删除旧的`commitUpdate`方法 |
| `基础参数页面功能重构说明.md` | 完整的功能说明文档 |

---

## 🎉 总结

### 重构前

- 一个按钮做两件事
- 功能混乱，错误不明确
- 用户体验差

### 重构后

- 两个按钮，职责清晰
- 基础信息直接保存
- 雷达参数先下发再保存
- 错误提示明确
- 用户体验好

**现在请刷新前端页面测试两个新按钮！** 🎉

