# 圆弧雷达数据监测说明

> **设备**: FactoryId = 20  
> **端口**: 1030  
> **更新时间**: 2025-11-02

---

## 修复完成内容

### ✅ 1. 启用Netty设备服务器

**修改文件**: `RadarSystem.WebAPI/Program.cs`

已启用：
- MQTT服务（用于消息推送）
- AllDeviceNettyServersHostedService（设备通信后台服务）
- ArcRadarNettyServer（圆弧雷达专用服务器）

### ✅ 2. 实现 FactoryId → DeviceId 映射

**修改文件**: `RadarSystem.Communication/Services/ArcRadarNettyServer.cs`

新增功能：
```csharp
// 从API加载设备映射
LoadDeviceMappingAsync()
  ├─ 调用 GET /api/Device
  ├─ 解析返回的设备列表
  ├─ 建立映射: FactoryId → DeviceId
  └─ 存储到 _deviceIdMap
```

### ✅ 3. 增加控制台数据输出

**修改位置**: `HandleData()` 和 `SaveRadarImage()` 方法

**输出格式**:

#### A. 数据接收时输出
```
================================================================================
【数据接收】时间: 2025-11-02 18:30:00.123
【数据接收】端口: 1030
【数据接收】唯一值(FactoryId/SlaveId): 20
【数据接收】命令代码: 0x0302
【数据接收】数据长度: 1048576 字节
【数据接收】原始数据(HEX): 5A5A000000140302...
【设备映射】FactoryId: 20 → DeviceId: RADAR_001
================================================================================
```

#### B. 文件保存时输出
```
********************************************************************************
【文件保存】时间: 2025-11-02 18:30:00.250
【文件保存】设备ID: RADAR_001
【文件保存】FactoryId: 20
【文件保存】数据类型: 形变
【文件保存】文件路径: ../../data/project/PROJECT001/radar/RADAR_001/00_20251102183000.dat
【文件保存】文件大小: 1024.00 KB
【文件保存】状态: ✅ 保存成功
********************************************************************************
```

---

## 数据流转流程（FactoryId=20）

```
1️⃣ 设备连接到1030端口
   └─ FactoryId: 20

2️⃣ 发送数据包
   └─ 格式: 5A5A + 00000014(20的十六进制) + 命令 + 数据

3️⃣ 服务器解析
   ├─ 提取 SlaveId: 0x00000014 = 20
   ├─ 从 _deviceIdMap 查找: 20 → DeviceId
   └─ 如果未找到，使用"20"作为DeviceId

4️⃣ 数据处理
   ├─ 根据命令类型处理（形变/复散射/置信度）
   ├─ 生成文件路径
   └─ 入队列

5️⃣ 异步保存
   ├─ 创建目录结构
   ├─ 保存 .dat 文件
   └─ 输出保存结果

6️⃣ MQTT通知（如果可用）
   └─ Topic: /dev/radar/defo/...
```

---

## 如何启动系统

### 方法1: 使用批处理脚本（推荐）

```batch
启动雷达系统并监测1030端口.bat
```

自动执行：
- 停止现有进程
- 启动系统
- 检查端口监听
- 显示服务地址

### 方法2: 手动启动

```powershell
# 停止现有进程
Get-Process | Where-Object {$_.ProcessName -like "*RadarSystem*"} | Stop-Process -Force

# 启动系统
cd RadarSystem.WebAPI
dotnet run --configuration Release

# 查看端口监听
netstat -ano | findstr ":1030"
```

---

## 如何验证系统运行

### 1. 检查端口监听

```powershell
netstat -ano | findstr "LISTENING" | findstr "1030 6098 8099"
```

**期望输出**:
```
TCP    0.0.0.0:1030     0.0.0.0:0      LISTENING  12345
TCP    0.0.0.0:6098     0.0.0.0:0      LISTENING  12345
TCP    0.0.0.0:8099     0.0.0.0:0      LISTENING  12345
```

### 2. 查看控制台输出

当有设备（FactoryId=20）连接时，控制台会显示：

```
================================================================================
【数据接收】时间: 2025-11-02 18:30:00.123
【数据接收】端口: 1030
【数据接收】唯一值(FactoryId/SlaveId): 20
【数据接收】命令代码: 0x0302
【数据接收】数据长度: 1048576 字节
【数据接收】原始数据(HEX): 5A5A000000140302001000000123456789ABCDEF...
【设备映射】FactoryId: 20 → DeviceId: 20
================================================================================
```

### 3. 检查数据文件

```powershell
# 查看数据目录
Get-ChildItem -Path "data\project\PROJECT001\radar\20\" -Recurse

# 查看最新文件
Get-ChildItem -Path "data\project\PROJECT001\radar\20\" -Recurse | Sort-Object LastWriteTime -Descending | Select-Object -First 5
```

### 4. 访问前端界面

打开浏览器访问: **http://localhost:6098**

---

## 数据库配置检查

### 查看设备配置

使用API查询：
```bash
curl http://localhost:8099/api/Device
```

或访问Swagger界面：
```
http://localhost:8099/swagger
```

查找：
```json
{
  "deviceId": "RADAR_001",
  "factoryId": "20",
  "deviceName": "圆弧雷达#1",
  "projectId": "PROJECT001",
  "port": 1030
}
```

---

## FactoryId 映射机制

### 工作原理

```
1. 应用启动时
   └─ ArcRadarNettyServer.LoadDeviceMappingAsync()
      ├─ 调用 GET http://localhost:8099/api/Device
      ├─ 解析所有设备的 FactoryId 和 DeviceId
      └─ 建立映射表: _deviceIdMap["20"] = "RADAR_001"

2. 接收数据时
   └─ HandleData(byte[] data)
      ├─ 解析 SlaveId = 20
      ├─ 查询映射: GetDeviceId("20")
      ├─ 返回: "RADAR_001"
      └─ 使用 DeviceId 保存文件

3. 文件路径生成
   └─ {DataPath}/project/{ProjectId}/radar/{DeviceId}/
      示例: data/project/PROJECT001/radar/RADAR_001/
```

### 映射规则

| 数据包中的SlaveId | 数据库FactoryId | 映射到DeviceId | 文件保存路径 |
|------------------|----------------|---------------|-------------|
| 0x00000014 (20) | "20" | "RADAR_001" | .../radar/RADAR_001/ |
| 0x00000015 (21) | "21" | "RADAR_002" | .../radar/RADAR_002/ |

---

## 故障排查

### 问题1: 端口1030未监听

**原因**: 
- Netty服务未启用
- ArcRadar配置 Enable=false
- MQTT连接失败导致服务启动失败

**解决**:
1. 检查 `appsettings.json`:
   ```json
   "Netty": {
     "ArcRadar": {
       "Port": 1030,
       "Enable": true  ← 确保为true
     }
   }
   ```

2. 查看启动日志:
   ```
   [INF] 正在启动圆弧雷达 Netty 服务器，端口: 1030
   [INF] ✅ 圆弧雷达服务器启动成功 - 端口: 1030
   ```

### 问题2: 数据未保存

**原因**:
- 路径不存在
- 权限不足
- 磁盘空间不足

**解决**:
1. 检查DataPath配置:
   ```json
   "DataPath": "../.."  ← 相对路径
   ```

2. 手动创建目录:
   ```powershell
   New-Item -Path "data\project\PROJECT001\radar\20" -ItemType Directory -Force
   ```

3. 查看错误日志:
   ```powershell
   Get-Content RadarSystem.WebAPI\logs\*.log -Tail 100 | Select-String "错误\|失败\|ERROR"
   ```

### 问题3: FactoryId映射失败

**症状**: 
```
【设备映射】FactoryId: 20 → DeviceId: 20
```
（DeviceId与FactoryId相同，说明未找到映射）

**原因**:
- API未启动（6098/8099端口未监听）
- 数据库中无设备记录
- 设备的FactoryId字段为空

**解决**:
1. 确保API正常运行
2. 检查数据库:
   ```
   GET http://localhost:8099/api/Device
   ```
3. 添加设备记录（如果不存在）

---

## 日志文件

### 查看实时日志

```powershell
# 实时监控
Get-Content RadarSystem.WebAPI\logs\radar-system*.log -Tail 50 -Wait

# 过滤关键信息
Get-Content RadarSystem.WebAPI\logs\radar-system*.log -Tail 200 | Select-String "1030|FactoryId|数据接收|文件保存"
```

---

## 控制台输出示例

当FactoryId=20的设备发送数据时，您将看到：

```
================================================================================
【数据接收】时间: 2025-11-02 18:35:12.456
【数据接收】端口: 1030
【数据接收】唯一值(FactoryId/SlaveId): 20
【数据接收】命令代码: 0x0302
【数据接收】数据长度: 1048576 字节
【数据接收】原始数据(HEX): 5A5A000000140302001000005B3C4D...
【设备映射】FactoryId: 20 → DeviceId: RADAR_001
================================================================================

[18:35:12 INF] 接收到圆弧雷达数据 - 端口:1030, FactoryId:20, 命令:0x0302, 长度:1048576字节
[18:35:12 INF] 圆弧雷达接收到形变数据上报，设备: RADAR_001, 数据长度: 1048576
[18:35:12 INF] 形变数据存储地址: ../../data/project/PROJECT001/radar/RADAR_001

********************************************************************************
【文件保存】时间: 2025-11-02 18:35:12.678
【文件保存】设备ID: RADAR_001
【文件保存】FactoryId: 20
【文件保存】数据类型: 形变
【文件保存】文件路径: ../../data/project/PROJECT001/radar/RADAR_001/00_20251102183512.dat
【文件保存】文件大小: 1024.00 KB
【文件保存】状态: ✅ 保存成功
********************************************************************************
```

---

## 快速启动命令

```batch
# Windows
启动雷达系统并监测1030端口.bat

# 或PowerShell
cd RadarSystem.WebAPI
dotnet run --configuration Release
```

---

**准备就绪！** 

运行启动脚本后，当圆弧雷达（FactoryId=20）连接并发送数据时，您将在控制台看到详细的数据接收和保存信息。

