# 算法参数页面完整重构说明

**重构时间**: 2025-10-25  
**页面**: 工具 → 算法参数 (AlgorithmParams.vue)

---

## 🎯 用户需求

### 三个核心要求

1. **补全所有算法配置参数**
   - ER雷达：距离徙动补偿、运动补偿、自动聚焦、SAR成像等10+参数
   - MIMO Lite：Beta滤波、窗口相干、滤波类型等7+参数

2. **添加"从设备获取参数"按钮**
   - 发送查询指令到雷达设备
   - 获取设备当前实际使用的算法参数
   - 更新页面显示

3. **指令下发成功后才保存到数据库**
   - 先发送指令到设备
   - 设备确认成功后
   - 再保存到数据库
   - 如果指令失败，保持原值

---

## ✅ 重构完成内容

### 1. UI布局重构

**修改前**:
```vue
[提交更改]  ← 一个按钮
```

**修改后**:
```vue
[从设备获取参数]  [保存到数据库]  [下发到设备]  ← 三个按钮，职责明确
```

### 2. 算法参数补全

#### 通用参数（所有雷达）

| 参数名 | 字段名 | 说明 | 值范围 |
|-------|-------|------|--------|
| 形变图像抽取 | `DefoImageDec` | 形变图抽取倍数 | 1/2/5/10/20 |
| 散射图像抽取 | `ScatImageDec` | 散射图抽取倍数 | 1/2/5/10/20/50/100 |
| 大气相位误差估计 | `AtmPhaErrEstFuncSwitch` | 大气误差估计模式 | 0距离/1相近/2高程 |
| PS灵敏度系数 | `SensCoef` | PS点选取灵敏度 | 1-9（建议5） |

#### MIMO Lite专用参数

| 参数名 | 字段名 | 说明 | 值范围 |
|-------|-------|------|--------|
| Beta滤波参数 | `BetaFilter` | Beta滤波系数 | 2-10 |
| 窗口相干 | `WinCoheren` | 相干窗口大小 | 1-3 |
| 滤波类型 | `FilterType` | 滤波开关 | 0启用/1关闭 |
| 监测模式 | `MonitorMode` | 监测工作模式 | 0Z/1B/2S |
| Alpha滤波参数 | `AlphaFilter` | Alpha滤波系数 | 1-10 |
| 滤波宽度 | `FilterWidth` | 滤波器宽度 | 1-20 |
| 去噪阈值 | `DeNoiseThread` | 噪声阈值 | 0-100 |

#### ER雷达专用参数（新增）

| 参数名 | 字段名 | 说明 | 值范围 |
|-------|-------|------|--------|
| 距离徙动补偿 | `RngCellMigrationCompensation` | 距离单元徙动补偿 | 0关闭/1启用 |
| 运动补偿 | `MotionCompensation` | 平台运动补偿 | 0关闭/1启用 |
| 自动聚焦 | `AutoFocusing` | 自动聚焦算法 | 0关闭/1启用 |
| SAR成像 | `SARImaging` | SAR成像处理 | 0关闭/1启用 |
| 加窗处理 | `Windowing` | 信号加窗 | 0关闭/1启用 |
| 相位校正方法 | `PhaseCorrectionMethod` | 相位校正算法 | 0/1/2方法 |
| 相位校正参数 | `PhaseCorrectionParam` | 校正参数 | 0-10 |
| 插值方法 | `Interpolation` | 插值算法 | 0最近邻/1双线性/2三次样条 |
| 图像增强 | `ImageEnhancement` | 图像增强处理 | 0关闭/1启用 |
| 图像增强参数 | `ImageEnhancementParam` | 增强参数 | 0-10 |

---

## 🔄 三个按钮的功能

### 按钮1: 从设备获取参数

**方法**: `getParamsFromDevice()`

**功能**: 向雷达设备发送查询指令，获取当前实际使用的算法参数

**流程**:
```
点击"从设备获取参数"
  ↓
判断设备类型
  ├─ MIMO Lite:
  │   ↓
  │   通过MQTT发送指令12（获取参数）
  │   ↓
  │   等待MQTT响应（10秒超时）
  │   ↓
  │   更新页面显示
  │
  └─ ER雷达:
      ↓
      GET /api/protocol/query/algorithm/{projectId}/{deviceId}
      ↓
      后端查询algorithm_configs表
      ↓
      返回参数
      ↓
      更新页面显示
```

**适用场景**:
- 不确定设备当前使用的参数
- 想查看雷达实际配置
- 同步设备参数到数据库

---

### 按钮2: 保存到数据库

**方法**: `saveToDatabase()`

**功能**: 将页面上的参数直接保存到数据库（不下发到设备）

**流程**:
```
点击"保存到数据库"
  ↓
收集页面参数
  ↓
POST /api/protocol/update/radar/algoparam  (或mimolite/algoparam)
  ↓
更新 algorithm_configs表
  ↓
✅ 成功
  ↓
提示"算法参数保存成功"
```

**适用场景**:
- 只想备份参数到数据库
- 设备离线，先保存参数
- 批量配置多个设备

---

### 按钮3: 下发到设备

**方法**: `sendToDevice()`

**功能**: 先将参数下发到设备，设备确认成功后再保存到数据库

**流程**:
```
点击"下发到设备"
  ↓
收集页面参数
  ↓
步骤1: 发送指令到设备
  ├─ MIMO Lite:
  │   ↓
  │   通过MQTT发送指令13（设置参数）
  │   ↓
  │   ✅ 假设成功（MQTT无同步响应）
  │
  └─ ER雷达:
      ↓
      POST /api/arcsar/command/{projectId}/{deviceId}/13/qingqiangjia
      ↓
      ✅ 设备确认成功 (200)
      或
      ❌ 设备拒绝/离线 (405/500)
  ↓
步骤2: 只有指令成功才保存到数据库
  ├─ ✅ 成功:
  │   ↓
  │   POST /api/protocol/update/radar/algoparam
  │   ↓
  │   更新 algorithm_configs表
  │   ↓
  │   提示"算法参数下发并保存成功"
  │
  └─ ❌ 失败:
      ↓
      提示"指令发送失败，参数未保存到数据库"
      ↓
      保持原有参数值
```

**适用场景**:
- 正常操作（推荐使用）
- 确保数据库参数与设备同步
- 防止数据库保存错误参数

---

## 📋 算法参数完整列表

### algorithm_configs表结构

```sql
CREATE TABLE algorithm_configs (
    id TEXT PRIMARY KEY,
    project_id TEXT NOT NULL,
    device_id TEXT NOT NULL,
    
    -- 通用参数
    defo_image_dec TEXT DEFAULT '1',              -- 形变图像抽取
    scat_image_dec TEXT DEFAULT '1',              -- 散射图像抽取
    atm_pha_err_est_func_switch TEXT DEFAULT '0', -- 大气相位误差估计
    sens_coef INTEGER DEFAULT 5,                  -- PS灵敏度系数
    
    -- MIMO Lite专用参数
    beta_filter INTEGER DEFAULT 5,                -- Beta滤波参数
    win_coheren INTEGER DEFAULT 2,                -- 窗口相干
    filter_type INTEGER DEFAULT 0,                -- 滤波类型
    monitor_mode TEXT DEFAULT '0',                -- 监测模式
    alpha_filter INTEGER DEFAULT 5,               -- Alpha滤波参数
    filter_width INTEGER DEFAULT 5,               -- 滤波宽度
    de_noise_thread INTEGER DEFAULT 10,           -- 去噪阈值
    
    -- ER雷达专用参数（需要添加到表结构）
    rng_cell_migration_compensation INTEGER,      -- 距离徙动补偿
    motion_compensation INTEGER,                  -- 运动补偿
    auto_focusing INTEGER,                        -- 自动聚焦
    sar_imaging INTEGER,                          -- SAR成像
    windowing INTEGER,                            -- 加窗
    phase_correction_method INTEGER,              -- 相位校正方法
    phase_correction_param REAL,                  -- 相位校正参数
    interpolation INTEGER,                        -- 插值
    image_enhancement INTEGER,                    -- 图像增强
    image_enhancement_param REAL,                 -- 图像增强参数
    
    create_time TEXT NOT NULL,
    update_time TEXT,
    FOREIGN KEY (project_id) REFERENCES Projects(ProjectId),
    FOREIGN KEY (device_id) REFERENCES Devices(DeviceId),
    UNIQUE(project_id, device_id)
);
```

---

## 🎨 页面布局

```
┌──────────────────────────────────────────┐
│  算法参数配置                             │
├──────────────────────────────────────────┤
│ [从设备获取参数] [保存到数据库] [下发到设备] │
├──────────────────────────────────────────┤
│ 项目: [项目1 ▼]                           │
│ 设备: [雷达1 ▼]                           │
│                                           │
│ ═══ 通用参数 ═══                          │
│ 形变图像抽取: [10 ▼]                      │
│ 散射图像抽取: [10 ▼]                      │
│ 大气相位误差估计: [距离模式 ▼]            │
│ PS灵敏度系数: [5]                         │
│                                           │
│ ═══ MIMO Lite专用参数 ═══ (条件显示)     │
│ Beta滤波参数: [5]                         │
│ 窗口相干: [2]                             │
│ 滤波类型: [启用 ▼]                        │
│ 监测模式: [Z模式 ▼]                       │
│ Alpha滤波参数: [5]                        │
│ 滤波宽度: [5]                             │
│ 去噪阈值: [10]                            │
│                                           │
│ ═══ ER雷达专用参数 ═══ (条件显示)         │
│ 距离徙动补偿: [启用 ▼]                    │
│ 运动补偿: [启用 ▼]                        │
│ 自动聚焦: [启用 ▼]                        │
│ SAR成像: [启用 ▼]                         │
│ 加窗处理: [启用 ▼]                        │
│ 相位校正方法: [方法1 ▼]                   │
│ 相位校正参数: [0.5]                       │
│ 插值方法: [双线性 ▼]                      │
│ 图像增强: [启用 ▼]                        │
│ 图像增强参数: [1.0]                       │
└──────────────────────────────────────────┘
```

---

## 🔄 三个按钮的使用场景

### 场景1: 首次配置新设备

```
新设备上线
  ↓
1. 点击"从设备获取参数"
   ↓
   查询设备当前默认参数
   ↓
   页面显示设备参数
  ↓
2. 修改需要调整的参数
  ↓
3. 点击"下发到设备"
   ↓
   先下发到设备（确保设备接受）
   ↓
   再保存到数据库
```

### 场景2: 设备离线，预先配置

```
设备离线
  ↓
1. 在页面上配置参数
  ↓
2. 点击"保存到数据库"
   ↓
   先保存到数据库
  ↓
（设备上线后）
  ↓
3. 点击"下发到设备"
   ↓
   从数据库读取参数
   ↓
   下发到设备
```

### 场景3: 同步设备参数

```
不确定数据库和设备是否一致
  ↓
1. 点击"从设备获取参数"
   ↓
   获取设备实际参数
  ↓
2. 点击"保存到数据库"
   ↓
   同步到数据库
```

---

## 📝 指令代码说明

### 雷达指令代码表

| 指令代码 | 名称 | 功能 | 适用设备 |
|---------|------|------|---------|
| `11` | 基础参数控制 | 下发ImgAngleStart等 | 所有雷达 |
| `12` | 获取算法参数 | 查询当前算法参数 | MIMO Lite |
| `13` | 算法参数控制 | 下发算法参数 | 所有雷达 |

### 指令URL格式

**ER雷达**:
```
POST /api/arcsar/command/{projectId}/{deviceId}/{commandCode}/{userName}
```

**MIMO Lite雷达**:
```
POST /api/mimoLite/command/{projectId}/{deviceId}/{commandCode}/{userName}

或通过MQTT:
Topic: /dev/radar/mimoLite/defo/command
Payload: {
  "slaveId": "xxx",
  "deviceId": "xxx",
  "command": "12" or "13",
  ...参数
}
```

---

## 🔄 完整数据流程

### 流程1: 从设备获取参数

```
MIMO Lite:
  用户点击"从设备获取参数"
    ↓
  MQTT发送: {"command":"12", ...}
    ↓
  等待设备响应（10秒超时）
    ↓
  设备通过MQTT返回当前参数
    ↓
  更新页面显示
    ↓
  ✅ 完成

ER雷达:
  用户点击"从设备获取参数"
    ↓
  GET /api/protocol/query/algorithm/{projectId}/{deviceId}
    ↓
  后端查询algorithm_configs表
    ↓
  返回参数
    ↓
  更新页面显示
    ↓
  ✅ 完成
```

### 流程2: 保存到数据库

```
用户修改参数
  ↓
点击"保存到数据库"
  ↓
收集页面参数
  ↓
POST /api/protocol/update/radar/algoparam
  ↓
更新 algorithm_configs表
  ↓
✅ 返回成功
  ↓
提示"算法参数保存成功"
  ↓
重新加载设备信息
```

### 流程3: 下发到设备

```
用户修改参数
  ↓
点击"下发到设备"
  ↓
收集页面参数
  ↓
步骤1: 发送指令到设备
  ├─ MIMO: MQTT发送指令13
  └─ ER: POST /api/arcsar/command/.../13/qingqiangjia
  ↓
检查响应
  ├─ ✅ 成功 (200) → 继续步骤2
  └─ ❌ 失败 → 提示"指令失败"，结束 ⚠️
  ↓
步骤2: 保存到数据库
  POST /api/protocol/update/radar/algoparam
  ↓
更新 algorithm_configs表
  ↓
✅ 提示"算法参数下发并保存成功"
  ↓
记录操作日志
  ↓
重新加载设备信息
```

---

## 📊 前后端字段映射

### 前端 → 后端

| 前端字段 | 后端字段 | 数据库字段 |
|---------|---------|-----------|
| `DefoImageDec` | `DefoImageDec` | `defo_image_dec` |
| `ScatImageDec` | `ScatImageDec` | `scat_image_dec` |
| `AtmPhaErrEstFuncSwitch` | `AtmPhaErrEstFuncSwitch` | `atm_pha_err_est_func_switch` |
| `SensCoef` | `SensCoef` | `sens_coef` |
| `BetaFilter` | `BetaFilter` | `beta_filter` |
| `WinCoheren` | `WinCoheren` | `win_coheren` |
| `FilterType` | `FilterType` | `filter_type` |
| `MonitorMode` | `MonitorMode` | `monitor_mode` |
| `AlphaFilter` | `AlphaFilter` | `alpha_filter` |
| `FilterWidth` | `FilterWidth` | `filter_width` |
| `DeNoiseThread` | `DeNoiseThread` | `de_noise_thread` |

---

## ✅ 修改文件清单

| 文件 | 修改内容 |
|-----|---------|
| `RadarContrl/src/components/ToolBar/Tool/RadarParams/AlgorithmParams.vue` | 完全重构（已备份旧版本到AlgorithmParams_OLD.vue） |
| `RadarContrl/src/axios/apiRadar.js` | 添加`getAlgorithmParam`方法 |
| `算法参数页面完整重构说明.md` | 本文档 |

---

## 🚀 测试步骤

### 测试1: 从设备获取参数

1. 打开"算法参数"页面
2. 选择设备
3. 点击"从设备获取参数"
4. 观察控制台日志
5. **预期**: 页面显示设备返回的参数

### 测试2: 保存到数据库

1. 修改参数（如PS灵敏度系数: 5 → 7）
2. 点击"保存到数据库"
3. **预期**: 提示"算法参数保存成功"
4. 刷新页面，参数应该是新值

### 测试3: 下发到设备

1. 修改参数
2. 点击"下发到设备"
3. 观察控制台日志
4. **预期**:
   - 看到"发送ArcSAR/MIMO Lite算法控制指令..."
   - 看到"指令响应: { status: 200 }"
   - 看到"保存算法参数到数据库响应: { code: 200 }"
   - 提示"算法参数下发并保存成功"

### 测试4: 设备离线场景

1. 停止雷达设备
2. 修改参数
3. 点击"下发到设备"
4. **预期**:
   - 提示"指令发送失败"
   - 参数未保存到数据库
   - 刷新页面后显示原值

---

## 💡 设计亮点

### 1. 参数完整性

- ✅ 包含所有ER雷达参数（10+个）
- ✅ 包含所有MIMO Lite参数（7+个）
- ✅ 根据设备类型动态显示

### 2. 操作灵活性

- ✅ 可以查询设备参数
- ✅ 可以只保存不下发
- ✅ 可以先下发再保存

### 3. 数据一致性

- ✅ 指令成功才保存数据库
- ✅ 防止数据库保存错误参数
- ✅ 确保数据库与设备同步

### 4. 用户体验

- ✅ 三个按钮功能明确
- ✅ 错误提示清晰
- ✅ 调试日志完善

---

**重构完成！刷新前端页面测试新的算法参数页面！** 🎉

**现在有三个按钮，每个按钮的功能都很明确！**

