# 前端修复说明

## 修复时间
2025-11-02

## 修复的问题

### 问题 1: 工具栏默认语言为英文
**症状**: 工具栏按钮默认显示英文而不是中文

**原因**: 
- `store/index.js` 中 `language` 默认值为数字 `0`
- 但在使用时都是用字符串 `"0"` 进行比较
- 类型不匹配导致判断失败

**修复**:
```javascript
// 修复前
config: {
    language: 0,  // 数字
}

// 修复后
config: {
    language: "0",  // ✅ 字符串，"0"表示中文
}
```

**文件**: `RadarContrl/src/store/index.js` 第33行

---

### 问题 2: 项目配置不显示设备信息
**症状**: 默认打开项目配置时，设备列表为空，只有手动切换设备选择后才显示

**原因**:
- `ProjectConfig.vue` 组件在 `onMounted` 时没有加载设备数据
- 设备列表依赖 `devices` 数组，但初始化时该数组为空

**修复**:
```javascript
// 在 ProjectConfig.vue 的 onMounted 中添加
if (store.projectInfo.projectSelected) {
  // 调用 API 加载当前项目的设备列表
  const res = await ApiRadar.getDevicesByProjectId(store.projectInfo.projectSelected);
  // 映射设备数据并更新 store
  store.projectInfo.projectData[projectIndex].devices = res.data.data.map(...)
}
```

**文件**: `RadarContrl/src/components/ToolBar/Tool/ProjectConfig/ProjectConfig.vue`

**行号**: 244-309

---

## 验证方法

### 验证问题 1 (中文显示)
1. 重新构建前端
2. 打开系统
3. 点击工具栏按钮
4. 确认菜单项显示中文（如："项目配置"、"雷达配置"等）

### 验证问题 2 (设备列表)
1. 重新构建前端
2. 打开系统
3. 点击"工具" → "项目配置"
4. 确认"当前设备"区域显示该项目下的所有设备
5. 无需手动选择，设备列表应该自动加载

---

## 重新构建步骤

```batch
# 方式1: 使用启动脚本（会重新构建）
启动系统.bat

# 方式2: 仅构建前端
cd RadarContrl
npm run build
# 然后复制 dist 到 wwwroot

# 方式3: 使用构建脚本
构建并部署前端.bat
```

---

## 相关文件

### 修改的文件
1. `RadarContrl/src/store/index.js` - 语言默认值
2. `RadarContrl/src/components/ToolBar/Tool/ProjectConfig/ProjectConfig.vue` - 设备加载

### 相关组件
- `ToolBar.vue` - 工具栏组件，显示中英文菜单
- `ProjectConfig.vue` - 项目配置组件，显示设备列表
- `DeviceList.vue` - 设备列表组件
- `RadarParams.vue` - 雷达参数组件（已有类似的设备加载逻辑）

---

## 技术细节

### 语言判断逻辑
```javascript
// 在多个组件中使用相同的判断逻辑
store.sysinfo.config.language === "0" ? '中文' : 'English'

// "0" = 中文
// "1" = English
```

### 设备数据结构
```javascript
{
  deviceId: String,
  deviceName: String,
  type: String,  // "ER" | "MIMOLITE"
  status: String,  // "Online" | "Offline"
  coordinates: [lon, lat, alt],
  ipAddress: String,
  port: Number,
  // ... 其他字段
}
```

---

**修复人**: AI Assistant  
**审核状态**: 待测试




