# 远程访问配置说明

**更新时间**: 2025-10-24  
**目的**: 允许从其他机器访问雷达监测系统

---

## ✅ 已完成的修改

### 1. Kestrel监听配置

**文件**: `RadarSystem.WebAPI/Program.cs`

**修改前**:
```csharp
options.ListenLocalhost(6098); // ❌ 只允许本机访问
options.ListenLocalhost(8099);
```

**修改后**:
```csharp
options.ListenAnyIP(6098); // ✅ 允许任何IP访问
options.ListenAnyIP(8099);
```

---

## 🔧 配置步骤

### 步骤1: 重新编译项目

```bash
cd C:\kotradar2025\donetradar\RadarSystem.WebAPI
dotnet build --configuration Release
```

### 步骤2: 配置Windows防火墙

#### 方法A: 使用PowerShell（管理员权限）

```powershell
# 允许6098端口（前端访问）
New-NetFirewallRule -DisplayName "雷达系统-前端" -Direction Inbound -LocalPort 6098 -Protocol TCP -Action Allow

# 允许8099端口（API服务）
New-NetFirewallRule -DisplayName "雷达系统-API" -Direction Inbound -LocalPort 8099 -Protocol TCP -Action Allow
```

#### 方法B: 使用图形界面

```
1. Win+R 运行 wf.msc
2. 点击"入站规则" → "新建规则"
3. 规则类型: 端口
4. 协议和端口: TCP，特定本地端口: 6098
5. 操作: 允许连接
6. 配置文件: 全部勾选
7. 名称: 雷达系统-前端访问

重复以上步骤，添加8099端口（雷达系统-API）
```

### 步骤3: 启动服务

```bash
cd RadarSystem.WebAPI
dotnet run --configuration Release --urls "http://0.0.0.0:6098;http://0.0.0.0:8099"
```

或使用启动脚本：
```bash
启动雷达系统.bat
```

### 步骤4: 验证监听状态

```bash
# 检查端口是否监听在0.0.0.0（所有IP）
netstat -an | findstr ":6098 :8099"
```

**预期结果**:
```
TCP    0.0.0.0:6098           0.0.0.0:0              LISTENING
TCP    0.0.0.0:8099           0.0.0.0:0              LISTENING
```

---

## 🌐 访问地址

### 本地访问
- **前端**: http://localhost:6098
- **API**: http://localhost:8099
- **Swagger**: http://localhost:8099/swagger

### 远程访问（从其他机器）
- **前端**: http://47.77.196.133:6098
- **API**: http://47.77.196.133:8099
- **Swagger**: http://47.77.196.133:8099/swagger

---

## ⚠️ 安全注意事项

### 1. 生产环境建议

**使用反向代理（Nginx）**:
```nginx
server {
    listen 80;
    server_name radar.yourdomain.com;
    
    location / {
        proxy_pass http://127.0.0.1:6098;
    }
    
    location /api/ {
        proxy_pass http://127.0.0.1:8099/api/;
    }
}
```

### 2. 启用HTTPS

```csharp
// Program.cs中添加HTTPS支持
builder.WebHost.ConfigureKestrel(options =>
{
    options.ListenAnyIP(6098);
    options.ListenAnyIP(8099);
    options.ListenAnyIP(443, listenOptions =>
    {
        listenOptions.UseHttps("path/to/certificate.pfx", "password");
    });
});
```

### 3. IP白名单（可选）

```csharp
// 中间件限制访问IP
app.Use(async (context, next) =>
{
    var remoteIp = context.Connection.RemoteIpAddress;
    var allowedIps = new[] { "192.168.1.100", "47.77.196.133" };
    
    if (!allowedIps.Contains(remoteIp?.ToString()))
    {
        context.Response.StatusCode = 403;
        await context.Response.WriteAsync("Access denied");
        return;
    }
    
    await next();
});
```

### 4. CORS配置（已配置）

**文件**: `RadarSystem.WebAPI/Program.cs`

```csharp
// 当前配置（允许所有来源）
builder.Services.AddCors(options =>
{
    options.AddPolicy("AllowAll", policy =>
    {
        policy.AllowAnyOrigin()
              .AllowAnyMethod()
              .AllowAnyHeader();
    });
});
```

**生产环境建议**:
```csharp
// 限制特定域名
policy.WithOrigins("http://47.77.196.133:6098", "https://radar.yourdomain.com")
      .AllowAnyMethod()
      .AllowAnyHeader()
      .AllowCredentials();
```

---

## 🔍 故障排查

### 问题1: Connection refused (111)

**可能原因**:
1. ❌ 服务未启动
2. ❌ 防火墙阻止
3. ❌ 端口被占用
4. ❌ 监听配置错误

**解决方法**:
```bash
# 1. 检查服务是否运行
netstat -an | findstr ":6098 :8099"

# 2. 检查进程
tasklist | findstr "RadarSystem"

# 3. 测试本地访问
curl http://localhost:6098

# 4. 测试防火墙
Test-NetConnection -ComputerName 47.77.196.133 -Port 6098
```

### 问题2: 无法从外部访问

**检查清单**:
- [ ] Kestrel配置使用`ListenAnyIP`而不是`ListenLocalhost`
- [ ] Windows防火墙已添加入站规则
- [ ] 云服务器安全组已开放端口（如阿里云/腾讯云）
- [ ] 本地路由器端口转发（如果需要）

### 问题3: API调用失败

**前端API地址配置**:

**文件**: `RadarContrl/src/axios/apiRadar.js`

```javascript
// 本地开发
static apiUrl = 'http://localhost:8099';

// 远程部署（需要修改）
static apiUrl = 'http://47.77.196.133:8099';
```

或使用环境变量：
```javascript
static apiUrl = import.meta.env.VITE_API_URL || 'http://localhost:8099';
```

---

## 🚀 快速部署检查清单

### 服务器端
- [x] 修改Kestrel为`ListenAnyIP`
- [ ] 重新编译项目
- [ ] 配置防火墙规则
- [ ] 启动服务
- [ ] 验证端口监听在0.0.0.0

### 云服务器（如果使用）
- [ ] 安全组开放6098端口
- [ ] 安全组开放8099端口
- [ ] 检查服务器公网IP

### 客户端
- [ ] 浏览器访问：http://服务器IP:6098
- [ ] 检查网络连接
- [ ] 检查防火墙/杀毒软件

---

## 📝 推荐部署架构

### 单机部署（当前）
```
[客户端] → [47.77.196.133:6098] → ASP.NET Core (Kestrel)
```

### 生产部署（推荐）
```
[客户端] → [80/443] → [Nginx反向代理] → [6098/8099] → ASP.NET Core
```

**优点**:
- SSL/TLS加密
- 负载均衡
- 静态文件缓存
- 隐藏内部端口
- 更好的安全性

---

**配置完成！重新编译并启动后即可远程访问！** 🌐
