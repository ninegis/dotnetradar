# 速度配置完整功能实现说明

**实现时间**: 2025-10-25  
**页面**: 工具 → 项目配置 → 速度指标 (SpeedTarget.vue)

---

## 🎯 功能概述

### 核心功能（全新）

1. **速度时间单位配置**
   - 支持下拉框多选
   - 支持复选框模式
   - 已选择标签显示

2. **速度图像自动生成** ⭐（新增）
   - 启用开关
   - 生成间隔设置
   
3. **加速度图像自动生成** ⭐（新增）
   - 启用开关
   - 生成间隔设置

4. **项目级配置管理**
   - 每个项目独立配置
   - 自动加载和保存
   - 项目切换自动刷新

---

## 📊 数据库表结构

### speed_indices表（完整）

```sql
CREATE TABLE speed_indices (
    id TEXT PRIMARY KEY,
    project_id TEXT NOT NULL UNIQUE,  -- 每个项目一条记录
    
    -- 时间单位配置
    time_units TEXT DEFAULT '04',                    -- 逗号分隔 "00,02,04"
    enable_30min INTEGER DEFAULT 0,                  -- 启用30分钟
    enable_1hour INTEGER DEFAULT 0,                  -- 启用1小时
    enable_1day INTEGER DEFAULT 1,                   -- 启用1天
    enable_3day INTEGER DEFAULT 0,                   -- 启用3天
    enable_1week INTEGER DEFAULT 0,                  -- 启用1周
    enable_1month INTEGER DEFAULT 0,                 -- 启用1月
    
    -- ✅ 速度图像自动生成配置（新增）
    auto_gen_speed_image INTEGER DEFAULT 0,          -- 自动生成速度图
    speed_image_interval INTEGER DEFAULT 60,         -- 速度图生成间隔（分钟）
    auto_gen_acceleration_image INTEGER DEFAULT 0,   -- 自动生成加速度图
    acceleration_image_interval INTEGER DEFAULT 120, -- 加速度图生成间隔（分钟）
    
    create_time TEXT NOT NULL,
    update_time TEXT,
    
    FOREIGN KEY (project_id) REFERENCES Projects(ProjectId) ON DELETE RESTRICT
);
```

---

## 🎨 页面布局

```
┌──────────────────────────────────────────┐
│  速度指标配置                             │
├──────────────────────────────────────────┤
│ [保存配置]                                │
├──────────────────────────────────────────┤
│ 项目: [项目1 ▼]                           │
│                                           │
│ ══════ 速度指标配置 ══════                │
│                                           │
│ ┌─ 速度指标说明 ───────────┐             │
│ │ 形变曲线和图像计算基准单位间隔 │        │
│ │ 选择的时间单位将用于计算速度和加速度 │  │
│ └───────────────────────────┘             │
│                                           │
│ 速度时间单位（多选）:                     │
│ [30分钟, 1天, 1周 ▼]                      │
│   • 30分钟  • 1小时  • 1天               │
│   • 3天     • 1周    • 1月               │
│                                           │
│ ══════ 快速选择 ══════                    │
│                                           │
│ 启用单选: [○使用复选框 ●使用下拉框]       │
│                                           │
│ 时间单位:                                 │
│ ☑ 30分钟  ☐ 1小时  ☑ 1天                 │
│ ☐ 3天     ☑ 1周    ☐ 1月                 │
│                                           │
│ 已选择:                                   │
│ [30分钟 ×] [1天 ×] [1周 ×]                │
│                                           │
│ ══════ 速度图像自动生成 ══════  ✨新增    │
│                                           │
│ ┌─ 速度图像生成说明 ───────┐             │
│ │ 启用后系统将自动生成速度图像和加速度图像 │ │
│ │ • 速度图：显示位移随时间的变化率 │      │
│ │ • 加速度图：显示速度随时间的变化率 │    │
│ └─────────────────────────┘              │
│                                           │
│ 自动生成速度图: [●开启 ○关闭]  ✨        │
│                                           │
│ 速度图生成间隔: [60] 分钟  ✨             │
│ 单位：分钟（建议：60分钟）                │
│                                           │
│ 自动生成加速度图: [○开启 ●关闭]  ✨      │
│                                           │
│ 加速度图生成间隔: [120] 分钟  ✨          │
│ 单位：分钟（建议：120分钟）               │
└──────────────────────────────────────────┘
```

---

## 📋 字段说明

### 时间单位代码

| 代码 | 名称 | 用途 | 数据库字段 |
|-----|------|------|-----------|
| `00` | 30分钟 | 高频监测 | `enable_30min` |
| `01` | 1小时 | 常规监测 | `enable_1hour` |
| `02` | 1天 | 每日监测 | `enable_1day` |
| `03` | 3天 | 中期监测 | `enable_3day` |
| `04` | 1周 | 周期监测 | `enable_1week` |
| `05` | 1月 | 长期监测 | `enable_1month` |

### 速度图像生成配置

| 字段 | 类型 | 说明 | 默认值 | 推荐值 |
|-----|------|------|--------|--------|
| `autoGenSpeedImage` | Boolean | 是否自动生成速度图 | false | true |
| `speedImageInterval` | Integer | 速度图生成间隔（分钟） | 60 | 60 |
| `autoGenAccelerationImage` | Boolean | 是否自动生成加速度图 | false | false |
| `accelerationImageInterval` | Integer | 加速度图生成间隔（分钟） | 120 | 120 |

---

## 🔄 完整数据流程

### 流程1: 页面加载

```
用户打开"速度指标配置"页面
  ↓
onMounted() 执行
  ↓
检查当前选中的项目
  ↓
loadSpeedConfig()
  ↓
GET /api/protocol/query/speed/target/PRJ001
  ↓
后端查询 speed_indices表
  ↓
返回配置:
  {
    timeUnits: "00,02,04",
    enable30Min: true,
    enable1Day: true,
    enable1Week: true,
    autoGenSpeedImage: true,
    speedImageInterval: 60,
    autoGenAccelerationImage: false,
    accelerationImageInterval: 120
  }
  ↓
前端解析:
  selectedTimeUnits = ['00', '02', '04']
  autoGenSpeedImage = true
  speedImageInterval = 60
  ↓
页面显示:
  已选择: [30分钟 ×] [1天 ×] [1周 ×]
  自动生成速度图: 开启 ✅
  速度图生成间隔: 60分钟 ✅
  自动生成加速度图: 关闭
```

### 流程2: 保存配置

```
用户修改配置:
  - 选择时间单位: 30分钟, 1小时, 1天
  - 开启速度图自动生成
  - 设置间隔: 30分钟
  ↓
点击"保存配置"
  ↓
commitUpdate() 执行
  ↓
准备参数:
  {
    projectId: "PRJ001",
    timeUnit: ['00', '01', '02'],
    autoGenSpeedImage: true,
    speedImageInterval: 30,
    autoGenAccelerationImage: false,
    accelerationImageInterval: 120
  }
  ↓
POST /api/protocol/update/speed/target
  ↓
后端处理:
  查询记录
  ├─ 有 → UPDATE speed_indices SET
  │        time_units = '00,01,02',
  │        enable_30min = 1,
  │        enable_1hour = 1,
  │        enable_1day = 1,
  │        auto_gen_speed_image = 1,
  │        speed_image_interval = 30
  │        WHERE project_id = 'PRJ001'
  │
  └─ 无 → INSERT INTO speed_indices (...)
  ↓
✅ 保存成功
  ↓
提示"速度指标配置保存成功"
  ↓
记录操作日志
```

### 流程3: 项目切换

```
从PRJ001切换到PRJ002
  ↓
watch监听触发
  ↓
loadSpeedConfig()
  ↓
GET /api/protocol/query/speed/target/PRJ002
  ↓
  ├─ 200 → 显示PRJ002的配置
  └─ 404 → 显示默认值（空）
  ↓
✅ 页面显示PRJ002的配置
```

---

## 🎯 使用场景

### 场景1: 标准监测配置

**需求**: 监测边坡，每天、每周生成速度图

**配置**:
```
时间单位: 1天, 1周
自动生成速度图: 开启
速度图生成间隔: 60分钟
自动生成加速度图: 关闭
```

**效果**:
- 计算1天和1周的速度
- 每60分钟自动生成一次速度图
- 不生成加速度图

---

### 场景2: 高频监测配置

**需求**: 快速变化区域，需要高频监测

**配置**:
```
时间单位: 30分钟, 1小时, 1天
自动生成速度图: 开启
速度图生成间隔: 30分钟
自动生成加速度图: 开启
加速度图生成间隔: 60分钟
```

**效果**:
- 计算30分钟、1小时、1天的速度
- 每30分钟生成速度图
- 每60分钟生成加速度图

---

### 场景3: 长期监测配置

**需求**: 变化缓慢，长期监测

**配置**:
```
时间单位: 1天, 1周, 1月
自动生成速度图: 开启
速度图生成间隔: 240分钟（4小时）
自动生成加速度图: 关闭
```

**效果**:
- 计算1天、1周、1月的速度
- 每4小时生成速度图
- 节省存储空间

---

## 📊 前后端字段映射

| 前端字段 | 后端字段 | 数据库字段 | 说明 |
|---------|---------|-----------|------|
| `selectedTimeUnits` | `timeUnit` | `time_units` | 时间单位数组 |
| - | - | `enable_30min` | 30分钟启用状态 |
| - | - | `enable_1hour` | 1小时启用状态 |
| - | - | `enable_1day` | 1天启用状态 |
| - | - | `enable_3day` | 3天启用状态 |
| - | - | `enable_1week` | 1周启用状态 |
| - | - | `enable_1month` | 1月启用状态 |
| `autoGenSpeedImage` | `AutoGenSpeedImage` | `auto_gen_speed_image` | 自动生成速度图 ✨ |
| `speedImageInterval` | `SpeedImageInterval` | `speed_image_interval` | 速度图间隔 ✨ |
| `autoGenAccelerationImage` | `AutoGenAccelerationImage` | `auto_gen_acceleration_image` | 自动生成加速度图 ✨ |
| `accelerationImageInterval` | `AccelerationImageInterval` | `acceleration_image_interval` | 加速度图间隔 ✨ |

---

## 🚀 后端接口

### 接口1: 更新速度配置

**端点**: `POST /api/protocol/update/speed/target`

**请求参数**:
```json
{
  "projectId": "PRJ001",
  "timeUnit": ["00", "01", "02"],
  "autoGenSpeedImage": true,
  "speedImageInterval": 60,
  "autoGenAccelerationImage": false,
  "accelerationImageInterval": 120
}
```

**后端处理**:
```csharp
// 解析timeUnit数组
var timeUnitArray = ["00", "01", "02"];

// 保存到数据库
speedIndex.TimeUnits = "00,01,02";
speedIndex.Enable30Min = true;   // 包含"00"
speedIndex.Enable1Hour = true;   // 包含"01"
speedIndex.Enable1Day = true;    // 包含"02"
speedIndex.Enable3Day = false;
speedIndex.Enable1Week = false;
speedIndex.Enable1Month = false;

// ✅ 新增字段
speedIndex.AutoGenSpeedImage = true;
speedIndex.SpeedImageInterval = 60;
speedIndex.AutoGenAccelerationImage = false;
speedIndex.AccelerationImageInterval = 120;
```

**返回**:
```json
{
  "code": 200,
  "message": "速度指标配置更新成功",
  "data": "配置已保存"
}
```

---

### 接口2: 获取速度配置

**端点**: `GET /api/protocol/query/speed/target/{projectId}`

**返回**:
```json
{
  "code": 200,
  "data": {
    "projectId": "PRJ001",
    "timeUnits": "00,01,02",
    "enable30Min": true,
    "enable1Hour": true,
    "enable1Day": true,
    "enable3Day": false,
    "enable1Week": false,
    "enable1Month": false,
    "autoGenSpeedImage": true,
    "speedImageInterval": 60,
    "autoGenAccelerationImage": false,
    "accelerationImageInterval": 120,
    "createTime": "2025-10-25T10:00:00",
    "updateTime": "2025-10-25T12:00:00"
  }
}
```

---

## 💡 新功能详解

### 功能1: 自动生成速度图

**原理**:
```
速度 = 位移差 / 时间差

例如：1天速度
  今天位移: 10mm
  昨天位移: 5mm
  时间差: 1天
  速度 = (10 - 5) / 1 = 5mm/天
```

**生成间隔**:
- 30分钟：高频生成（数据量大）
- 60分钟：常规生成（推荐）
- 120分钟：低频生成（节省存储）

**应用**:
- 速度图可以显示变形趋势
- 红色区域：速度快（加剧）
- 绿色区域：速度慢（稳定）

---

### 功能2: 自动生成加速度图

**原理**:
```
加速度 = 速度差 / 时间差

例如：1天加速度
  今天速度: 5mm/天
  昨天速度: 3mm/天
  时间差: 1天
  加速度 = (5 - 3) / 1 = 2mm/天²
```

**生成间隔**:
- 60分钟：高频（捕捉快速变化）
- 120分钟：常规（推荐）
- 240分钟：低频（平稳监测）

**应用**:
- 加速度图显示变形加速或减速
- 红色区域：正加速（变形加剧）
- 蓝色区域：负加速（变形减缓）

---

## 🎯 配置建议

### 推荐配置1: 边坡监测

```
时间单位: ☑ 1天, ☑ 1周, ☑ 1月
自动生成速度图: 开启
速度图间隔: 60分钟
自动生成加速度图: 开启
加速度图间隔: 120分钟
```

**适用**: 常规边坡稳定性监测

---

### 推荐配置2: 应急监测

```
时间单位: ☑ 30分钟, ☑ 1小时, ☑ 1天
自动生成速度图: 开启
速度图间隔: 30分钟
自动生成加速度图: 开启
加速度图间隔: 60分钟
```

**适用**: 滑坡高风险区域

---

### 推荐配置3: 节能模式

```
时间单位: ☑ 1天, ☑ 1周
自动生成速度图: 开启
速度图间隔: 240分钟
自动生成加速度图: 关闭
```

**适用**: 稳定区域，降低计算和存储负担

---

## 🔄 数据一致性

### UNIQUE约束

```sql
UNIQUE (project_id)  -- 每个项目只有一条记录
```

### 更新或创建逻辑

```csharp
var speedIndex = await _dbContext.SpeedIndices
    .FirstOrDefaultAsync(s => s.ProjectId == projectId);

if (speedIndex == null) {
    // ✅ 创建新记录
    speedIndex = new SpeedIndexEntity { ... };
    _dbContext.Add(speedIndex);
} else {
    // ✅ 更新现有记录
    speedIndex.TimeUnits = ...;
    speedIndex.AutoGenSpeedImage = ...;
}

await _dbContext.SaveChangesAsync();
```

**保证**:
- ✅ 不会创建重复记录
- ✅ 每个项目只有一条配置
- ✅ 配置始终是最新的

---

## 📝 修改文件清单

### 前端文件

| 文件 | 修改内容 |
|-----|---------|
| `SpeedTarget.vue` | 1. 添加速度图像自动生成UI<br>2. 添加变量定义<br>3. 更新`loadSpeedConfig`方法<br>4. 更新`commitUpdate`方法<br>5. 添加watch监听 |

### 后端文件

| 文件 | 修改内容 |
|-----|---------|
| `SpeedIndexEntity.cs` | 添加4个新字段 |
| `Program.cs` | 添加4个新字段到表创建和迁移逻辑 |
| `ProtocolController.cs` | 1. `UpdateSpeedTarget`更新新字段<br>2. `GetSpeedTarget`返回新字段 |

---

## ✅ 功能对比

### 修改前

| 功能 | 状态 |
|-----|------|
| 时间单位选择 | ✅ 有（仅下拉框） |
| 复选框模式 | ❌ 无 |
| 已选择标签 | ❌ 无 |
| 项目加载配置 | ❌ 无 |
| 项目切换刷新 | ❌ 无 |
| 速度图自动生成 | ❌ 无 |
| 加速度图自动生成 | ❌ 无 |

### 修改后

| 功能 | 状态 | 说明 |
|-----|------|------|
| 时间单位选择 | ✅ 有 | 下拉框多选 |
| 复选框模式 | ✅ 有 | 可切换 ⭐ |
| 已选择标签 | ✅ 有 | 可视化显示 ⭐ |
| 项目加载配置 | ✅ 有 | 自动加载 ⭐ |
| 项目切换刷新 | ✅ 有 | watch监听 ⭐ |
| 速度图自动生成 | ✅ 有 | 开关+间隔 ⭐ |
| 加速度图自动生成 | ✅ 有 | 开关+间隔 ⭐ |

---

## 🚀 测试步骤

### 测试1: 页面加载

1. 打开"速度指标配置"
2. **预期**: 显示已保存的配置
   - 已选择的时间单位
   - 速度图开关状态
   - 生成间隔数值

### 测试2: 下拉框选择

1. 下拉框选择: 30分钟, 1天, 1周
2. **预期**: 已选择显示3个标签

### 测试3: 复选框选择

1. 切换到复选框模式
2. 勾选: 1小时, 1天
3. **预期**: 
   - 下拉框同步更新
   - 已选择标签更新

### 测试4: 速度图配置

1. 开启"自动生成速度图"
2. 设置间隔: 30分钟
3. **预期**: 
   - 间隔输入框显示
   - 可以输入数值

### 测试5: 保存和加载

1. 修改所有配置
2. 点击"保存配置"
3. **预期**: 提示"保存成功"
4. 刷新页面
5. **预期**: 配置保持 ✅
6. 切换到其他项目
7. **预期**: 显示其他项目的配置
8. 切换回来
9. **预期**: 显示原来的配置 ✅

---

## ✅ 总结

### 完成的工作

1. ✅ 数据库添加4个新字段
2. ✅ 数据库迁移逻辑更新
3. ✅ 后端接口支持新字段
4. ✅ 前端UI添加速度图像生成配置
5. ✅ 前端加载和保存逻辑完善
6. ✅ 编译成功（0错误0警告）

### 核心功能

- ✅ 速度时间单位选择（多选）
- ✅ 下拉框 + 复选框两种模式
- ✅ 速度图自动生成配置 ⭐
- ✅ 加速度图自动生成配置 ⭐
- ✅ 项目级配置管理
- ✅ 自动加载和保存
- ✅ 每个项目一条记录

### 编译状态

```
✅ 已成功生成
   0 个警告
   0 个错误
```

---

**实现完成！刷新前端页面测试！** 🎉

**现在速度配置页面支持：**
- ✅ 多种选择方式（下拉框+复选框）
- ✅ 速度图像自动生成 ⭐
- ✅ 加速度图像自动生成 ⭐
- ✅ 项目切换自动加载
- ✅ 每个项目独立配置

