# 色条配置页面完整实现说明

**实现时间**: 2025-10-25  
**页面**: 工具 → 项目配置 → 位移和散射色条配置 (ColorConfig.vue)

---

## 🎯 功能概述

### 核心功能

1. **位移色条配置** - 配置位移图的颜色映射
2. **散射色条配置** - 配置散射图的颜色映射
3. **预设色板** - 提供6种预设色板快速应用
4. **自定义颜色区间** - 支持定义不同数值范围使用不同颜色
5. **实时预览** - 修改参数时实时预览色条效果

---

## 🎨 页面布局

```
┌──────────────────────────────────────────┐
│  色条配置                                 │
├──────────────────────────────────────────┤
│ [加载预设]  [保存配置]                    │
├──────────────────────────────────────────┤
│ ○ 位移色条  ○ 散射色条                    │
│                                           │
│ 色板预设: [蓝绿渐变 ▼]                    │
│                                           │
│ 色条预览: [━━━━━━━━━━━]                   │
│            蓝→青→绿→黄→红                  │
│                                           │
│ ═══ 数值范围 ═══                          │
│ 位移最小值(mm): [-100]                    │
│ 位移最大值(mm): [100]                     │
│                                           │
│ ═══ HSL色相设置 ═══                       │
│ 色相范围: 0-360（0=红, 120=绿, 240=蓝）   │
│                                           │
│ 起始色相(H): [━━●━━━━━━━] 240 (蓝色)      │
│              红  黄 绿 青 蓝 紫 红         │
│              [240]                        │
│                                           │
│ 结束色相(H): [━━━━━━━━●━] 0 (红色)        │
│              红  黄 绿 青 蓝 紫 红         │
│              [0]                          │
│                                           │
│ ═══ 透明度滤波 ═══                        │
│ 启用透明度滤波: [开关] ☑                  │
│ 透明度系数: [━━━●━━━━━] 0.5              │
│             [0.5]                         │
│ 滤波最小值: [-50]                         │
│ 滤波最大值: [50]                          │
│                                           │
│ ═══ 颜色区间自定义 ═══                    │
│ 启用自定义区间: [开关] ☐                  │
│                                           │
│ [可以定义不同数值范围使用不同颜色]        │
│ ┌──────────────────────────────┐         │
│ │ [-100] ~ [-50] [🔵蓝色] [删除]│         │
│ │ [-50] ~ [-10] [🔵青色] [删除] │         │
│ │ [-10] ~ [10] [🟢绿色] [删除]  │         │
│ │ [10] ~ [50] [🟡黄色] [删除]   │         │
│ │ [50] ~ [100] [🔴红色] [删除]  │         │
│ └──────────────────────────────┘         │
│ [+ 添加颜色区间]                          │
└──────────────────────────────────────────┘
```

---

## 📋 功能详解

### 功能1: 位移/散射色条切换

**单选框**:
- ○ 位移色条 (displacement)
- ○ 散射色条 (scattering)

**功能**:
- 切换时自动加载对应模式的配置
- 每个模式独立保存配置

**数据库**:
- 位移: `mode='displacement'`
- 散射: `mode='scattering'`

---

### 功能2: 预设色板

**预设列表**:

| 预设名称 | 起始色相 | 结束色相 | 效果 | 适用场景 |
|---------|---------|---------|------|---------|
| 蓝绿渐变 | 240 (蓝) | 120 (绿) | 蓝→青→绿 | 默认，清晰 |
| 红黄渐变 | 0 (红) | 60 (黄) | 红→橙→黄 | 警示效果 |
| 彩虹色谱 | 0 (红) | 300 (紫) | 红→黄→绿→蓝→紫 | 丰富层次 |
| 冷暖色调 | 240 (蓝) | 0 (红) | 蓝→青→绿→黄→红 | 温度效果 |
| 黑白灰度 | 0 | 0 | 黑→灰→白 | 黑白打印 |
| 自定义 | - | - | 启用自定义区间 | 高级用户 |

**使用方法**:
1. 选择预设
2. 自动应用到当前色条
3. 实时预览效果
4. 可以微调参数

---

### 功能3: HSL色相设置

**HSL色彩空间**:
- **H (Hue)**: 色相，0-360度
  - 0° = 红色
  - 60° = 黄色
  - 120° = 绿色
  - 180° = 青色
  - 240° = 蓝色
  - 300° = 紫色
  - 360° = 红色
- **S (Saturation)**: 饱和度，固定100%
- **L (Lightness)**: 亮度，固定50%

**滑块功能**:
- 拖动滑块可视化选择颜色
- 显示关键色相标记
- 输入框精确输入数值

**示例**:
```
起始色相: 240 (蓝色)
结束色相: 0 (红色)
效果: 蓝→青→绿→黄→橙→红（冷暖渐变）
```

---

### 功能4: 透明度滤波

**功能**: 对特定范围的数值应用透明度，实现"滤波"效果

**参数**:
- **启用开关**: 是否启用透明度滤波
- **透明度系数** (0-1): 滤波区域的透明度
  - 0 = 完全透明（隐藏）
  - 0.5 = 半透明
  - 1 = 完全不透明
- **滤波最小值**: 低于此值的部分应用透明度
- **滤波最大值**: 高于此值的部分应用透明度

**应用场景**:
```
例1: 隐藏小变形
  滤波最小值: -10
  滤波最大值: 10
  透明度: 0.2
  → 位移在-10~10mm之间的区域变透明

例2: 突出大变形
  滤波最小值: -50
  滤波最大值: 50
  透明度: 1.0
  → 位移超过±50mm的区域完全不透明，其他半透明
```

---

### 功能5: 自定义颜色区间（高级）

**功能**: 为不同数值范围分配不同的固定颜色

**示例配置**:
```json
[
  { "min": -100, "max": -50, "color": "#0000FF", "label": "蓝色（负向大变形）" },
  { "min": -50, "max": -10, "color": "#00FFFF", "label": "青色（负向小变形）" },
  { "min": -10, "max": 10, "color": "#00FF00", "label": "绿色（稳定）" },
  { "min": 10, "max": 50, "color": "#FFFF00", "label": "黄色（正向小变形）" },
  { "min": 50, "max": 100, "color": "#FF0000", "label": "红色（正向大变形）" }
]
```

**应用效果**:
- -100~-50mm: 显示蓝色
- -50~-10mm: 显示青色
- -10~10mm: 显示绿色
- 10~50mm: 显示黄色
- 50~100mm: 显示红色

**操作**:
1. 启用"自定义区间"开关
2. 点击"+ 添加颜色区间"
3. 设置最小值、最大值
4. 选择颜色（颜色选择器）
5. 预览效果
6. 保存配置

---

## 🔄 数据流程

### 流程1: 页面加载

```
用户打开"色条配置"页面
  ↓
onMounted() 执行
  ↓
检查 store.radarInfo.defoColorBarSetting
  ├─ 有 → 加载到表单
  └─ 无 → 从数据库加载
  ↓
GET /api/protocol/query/colorBar/{projectId}/displacement
GET /api/protocol/query/colorBar/{projectId}/scattering
  ↓
后端查询 colorbar_configs表
  ↓
返回配置（包含customRanges）
  ↓
更新页面显示
  ↓
renderColorBar() 渲染色条预览
```

### 流程2: 应用预设

```
用户选择预设: "彩虹色谱"
  ↓
applyPreset() 执行
  ↓
从 colorPresets 获取预设配置
  ↓
Object.assign(currentColorConfig, preset)
  ↓
updateColorPreview()
  ↓
实时预览新色条
  ↓
提示"已应用预设: 彩虹色谱"
```

### 流程3: 调整参数

```
用户拖动"起始色相"滑块: 240 → 180
  ↓
@change="updateColorPreview"
  ↓
renderColorBar() 重新渲染
  ↓
色条预览实时更新
  ↓
用户继续调整其他参数...
```

### 流程4: 保存配置

```
用户点击"保存配置"
  ↓
saveColorConfig() 执行
  ↓
验证必填字段
  ↓
准备参数:
  {
    projectId: "xxx",
    mode: "displacement",
    minValue: -100,
    maxValue: 100,
    hslHStart: 240,
    hslHEnd: 0,
    filterEnable: 1,
    filterAlpha: 0.5,
    filterMin: -50,
    filterMax: 50,
    customRanges: "[{...}]"  // ✅ 自定义区间（如果启用）
  }
  ↓
POST /api/protocol/update/colorBar
  ↓
后端更新/创建 colorbar_configs表
  ↓
✅ 返回 { code: 200 }
  ↓
提示"色条配置保存成功"
  ↓
记录操作日志
  ↓
更新 store.radarInfo 中的色条设置
```

---

## 📊 数据库表结构

### colorbar_configs表

```sql
CREATE TABLE colorbar_configs (
    id TEXT PRIMARY KEY,
    project_id TEXT NOT NULL,
    mode TEXT NOT NULL,                  -- displacement(位移) / scattering(散射)
    
    -- 数值范围
    min_value REAL DEFAULT -100,         -- 最小值
    max_value REAL DEFAULT 100,          -- 最大值
    
    -- HSL色相
    hsl_h_start INTEGER DEFAULT 0,       -- 起始色相 (0-360)
    hsl_h_end INTEGER DEFAULT 240,       -- 结束色相 (0-360)
    
    -- 透明度滤波
    filter_enable INTEGER DEFAULT 0,     -- 启用滤波 (0/1)
    filter_alpha REAL DEFAULT 0.8,       -- 透明度系数 (0-1)
    filter_min REAL DEFAULT -1000,       -- 滤波最小值
    filter_max REAL DEFAULT 1000,        -- 滤波最大值
    
    -- 自定义颜色区间（JSON）
    custom_ranges TEXT,                  -- ✅ 新增字段
    
    create_time TEXT NOT NULL,
    update_time TEXT,
    
    FOREIGN KEY (project_id) REFERENCES Projects(ProjectId) ON DELETE RESTRICT,
    UNIQUE (project_id, mode)            -- 每个项目每种模式一个配置
);
```

### custom_ranges字段格式

```json
[
  {
    "min": -100,
    "max": -50,
    "color": "#0000FF",
    "label": "蓝色（负向大变形）"
  },
  {
    "min": -50,
    "max": -10,
    "color": "#00FFFF",
    "label": "青色（负向小变形）"
  },
  {
    "min": -10,
    "max": 10,
    "color": "#00FF00",
    "label": "绿色（稳定）"
  },
  {
    "min": 10,
    "max": 50,
    "color": "#FFFF00",
    "label": "黄色（正向小变形）"
  },
  {
    "min": 50,
    "max": 100,
    "color": "#FF0000",
    "label": "红色（正向大变形）"
  }
]
```

---

## 🚀 后端接口

### 接口1: 获取色条配置

**端点**: `GET /api/protocol/query/colorBar/{projectId}/{mode}`

**参数**:
- `projectId`: 项目ID
- `mode`: `displacement` 或 `scattering`

**返回**:
```json
{
  "code": 200,
  "data": {
    "projectId": "PRJ001",
    "mode": "displacement",
    "minValue": -100,
    "maxValue": 100,
    "hslHStart": 240,
    "hslHEnd": 0,
    "filterEnable": 1,
    "filterAlpha": 0.5,
    "filterMin": -50,
    "filterMax": 50,
    "customRanges": "[{...}]"
  }
}
```

**实现**: `ProtocolController.GetColorBar()`

---

### 接口2: 更新色条配置

**端点**: `POST /api/protocol/update/colorBar`

**请求参数**:
```json
{
  "projectId": "PRJ001",
  "mode": "displacement",
  "minValue": -100,
  "maxValue": 100,
  "hslHStart": 240,
  "hslHEnd": 0,
  "filterEnable": 1,
  "filterAlpha": 0.5,
  "filterMin": -50,
  "filterMax": 50,
  "customRanges": "[{\"min\":-100,\"max\":-50,\"color\":\"#0000FF\"}]"
}
```

**返回**:
```json
{
  "code": 200,
  "message": "色条配置更新成功",
  "data": "配置已保存"
}
```

**实现**: `ProtocolController.UpdateColorBar()`

---

## 🎨 预设色板详解

### 1. 蓝绿渐变（默认）

**配置**:
```javascript
{
  displacement: {
    minValue: -100,
    maxValue: 100,
    hslHStart: 240,    // 蓝色
    hslHEnd: 120,      // 绿色
    filterAlpha: 0.5
  }
}
```

**效果**: 蓝→青→绿（清晰、专业）

**适用**: 常规位移监测

---

### 2. 红黄渐变（警示）

**配置**:
```javascript
{
  hslHStart: 0,      // 红色
  hslHEnd: 60,       // 黄色
}
```

**效果**: 红→橙→黄（警示、醒目）

**适用**: 需要突出显示危险区域

---

### 3. 彩虹色谱

**配置**:
```javascript
{
  hslHStart: 0,      // 红色
  hslHEnd: 300,      // 紫色
}
```

**效果**: 红→橙→黄→绿→青→蓝→紫（层次丰富）

**适用**: 需要细分多个等级

---

### 4. 冷暖色调

**配置**:
```javascript
{
  hslHStart: 240,    // 蓝色（冷）
  hslHEnd: 0,        // 红色（暖）
}
```

**效果**: 蓝→青→绿→黄→橙→红（温度感）

**适用**: 温度、应力等物理量

---

### 5. 黑白灰度

**配置**:
```javascript
{
  hslHStart: 0,
  hslHEnd: 0,        // 相同色相，形成灰度
  filterAlpha: 0.8
}
```

**效果**: 黑→灰→白

**适用**: 黑白打印、文档

---

## 💡 使用场景示例

### 场景1: 标准位移监测

**需求**: 监测边坡位移，-100mm到100mm

**配置**:
1. 选择"位移色条"
2. 选择预设"冷暖色调"
3. 设置:
   - 最小值: -100mm
   - 最大值: 100mm
   - 起始色相: 240 (蓝色，表示负向位移)
   - 结束色相: 0 (红色，表示正向位移)
4. 启用透明度滤波:
   - 滤波范围: -5mm ~ 5mm
   - 透明度: 0.3
   - 效果: 微小变形半透明显示

**效果**:
- 负向大变形: 深蓝色（警示）
- 负向小变形: 浅蓝色
- 稳定区域: 半透明绿色
- 正向小变形: 浅黄色
- 正向大变形: 深红色（警示）

---

### 场景2: 散射强度分析

**需求**: 分析雷达散射强度，-50dB到10dB

**配置**:
1. 选择"散射色条"
2. 选择预设"蓝绿渐变"
3. 设置:
   - 最小值: -50dB
   - 最大值: 10dB
   - 起始色相: 240 (蓝色，表示弱散射)
   - 结束色相: 120 (绿色，表示强散射)

**效果**:
- 弱散射: 蓝色
- 中散射: 青色
- 强散射: 绿色

---

### 场景3: 自定义分级显示

**需求**: 将位移分为5个等级，每个等级固定颜色

**配置**:
1. 启用"自定义区间"
2. 添加5个区间:
   - 级别1: -100~-20mm → 深蓝色 (严重负向)
   - 级别2: -20~-5mm → 浅蓝色 (轻微负向)
   - 级别3: -5~5mm → 绿色 (稳定)
   - 级别4: 5~20mm → 黄色 (轻微正向)
   - 级别5: 20~100mm → 红色 (严重正向)

**效果**: 清晰的分级显示，便于快速判断

---

## 📋 前后端字段映射

| 前端字段 | 后端字段 | 数据库字段 | 说明 |
|---------|---------|-----------|------|
| `minValue` | `MinValue` | `min_value` | 最小值 |
| `maxValue` | `MaxValue` | `max_value` | 最大值 |
| `hslHStart` | `HslHStart` | `hsl_h_start` | 起始色相 |
| `hslHEnd` | `HslHEnd` | `hsl_h_end` | 结束色相 |
| `filterEnable` | `FilterEnable` | `filter_enable` | 启用滤波 |
| `filterAlpha` | `FilterAlpha` | `filter_alpha` | 透明度 |
| `filterMin` | `FilterMin` | `filter_min` | 滤波最小值 |
| `filterMax` | `FilterMax` | `filter_max` | 滤波最大值 |
| `customRanges` | `CustomRanges` | `custom_ranges` | 自定义区间 ✅ |

---

## 📝 修改文件清单

### 前端文件

| 文件 | 修改内容 |
|-----|---------|
| `RadarContrl/src/components/ToolBar/Tool/ProjectConfig/ColorConfig.vue` | 完全重构，新增功能 |
| `RadarContrl/src/axios/apiRadar.js` | 添加`getColorBar`方法 |

### 后端文件

| 文件 | 修改内容 |
|-----|---------|
| `RadarSystem.Data/Models/ColorBarConfigEntity.cs` | 添加`CustomRanges`字段 |
| `RadarSystem.WebAPI/Program.cs` | 添加`custom_ranges`字段到表结构和迁移逻辑 |
| `RadarSystem.WebAPI/Controllers/ProtocolController.cs` | 更新`UpdateColorBar`和`GetColorBar`接口 |

---

## ✅ 功能对比

### 修改前

| 功能 | 状态 |
|-----|------|
| 位移/散射切换 | ✅ 有 |
| 数值范围设置 | ✅ 有 |
| HSL色相设置 | ✅ 有（简单输入框） |
| 透明度滤波 | ✅ 有 |
| 预设色板 | ❌ 无 |
| 可视化滑块 | ❌ 无 |
| 自定义区间 | ❌ 无 |
| 实时预览 | ✅ 有 |

### 修改后

| 功能 | 状态 | 改进 |
|-----|------|------|
| 位移/散射切换 | ✅ 有 | 优化UI |
| 数值范围设置 | ✅ 有 | 数字输入框 |
| HSL色相设置 | ✅ 有 | **滑块+标记+输入框** ⭐ |
| 透明度滤波 | ✅ 有 | **滑块可视化** ⭐ |
| 预设色板 | ✅ 有 | **6种预设快速应用** ⭐ |
| 可视化滑块 | ✅ 有 | **色相滑块** ⭐ |
| 自定义区间 | ✅ 有 | **支持多区间，颜色选择器** ⭐ |
| 实时预览 | ✅ 有 | 保持 |

---

## 🚀 测试步骤

### 测试1: 预设色板

1. 打开"色条配置"页面
2. 选择"位移色条"
3. 色板预设选择"彩虹色谱"
4. **预期**: 色条预览显示彩虹渐变

### 测试2: 滑块调整

1. 拖动"起始色相"滑块
2. **预期**: 色条预览实时更新

### 测试3: 自定义区间

1. 启用"自定义区间"
2. 点击"+ 添加颜色区间"
3. 设置: -20~20, 绿色
4. **预期**: 新区间添加成功

### 测试4: 保存配置

1. 修改参数
2. 点击"保存配置"
3. **预期**: 提示"色条配置保存成功"
4. 刷新页面，配置保持

---

## 💡 便捷性设计

### 1. 预设色板

- **优势**: 一键应用专业配色
- **场景**: 快速配置，无需调试

### 2. HSL滑块

- **优势**: 可视化选择颜色
- **场景**: 精确调整色相

### 3. 自定义区间

- **优势**: 灵活分级显示
- **场景**: 特殊需求，如地质分级

### 4. 实时预览

- **优势**: 所见即所得
- **场景**: 所有配置调整

---

**实现完成！后端编译成功，刷新前端页面测试！** 🎉

**色条配置现在支持：预设色板 + HSL滑块 + 自定义区间 + 实时预览！**

