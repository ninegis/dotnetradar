# 雷达图像生成配置加载问题修复

**问题**: 页面默认加载了项目，但没有加载对应的类型和配置

---

## 🐛 问题分析

### 现象

```
打开"雷达图像生成配置"页面
  ↓
项目下拉框: 显示了项目 ✅
生成类型: 没有显示选中值 ❌
形变图配置: 没有显示数值 ❌
散射图配置: 没有显示数值 ❌
```

### 根本原因

#### 原因1: 响应式对象更新问题

**问题代码**:
```javascript
// ❌ 直接赋值，可能导致响应式失效
store.radarInfo.imageAnalysisConfig = {
  genImageType: res.data.data.genImageType,
  followDefoInterval: res.data.data.defoInterval,
  ...
};
```

**问题**:
- Vue的响应式系统可能没有正确追踪
- 页面上的`v-model`绑定没有更新

#### 原因2: 异步加载时机

**问题代码**:
```javascript
onMounted(async () => {
  // 先设置默认值
  if (!store.radarInfo.imageAnalysisConfig) {
    store.radarInfo.imageAnalysisConfig = { genImageType: '01', ... };
  }
  
  // 再加载配置（覆盖默认值）
  await loadImageConfig();  // ❌ 但页面可能已经渲染完成
});
```

#### 原因3: 字段名不一致

**后端返回**:
```json
{
  "genImageType": "01",
  "defoInterval": 60,      // ← 字段名
  "scatInterval": 60,
  "defoNumber": 10,        // ← 字段名
  "scatNumber": 10
}
```

**前端绑定**:
```vue
<el-select v-model="store.radarInfo.imageAnalysisConfig['genImageType']">  ✅
<el-input v-model.number="store.radarInfo.imageAnalysisConfig['followDefoInterval']">  ❌ 不匹配
<el-input v-model.number="store.radarInfo.imageAnalysisConfig['scatInterval']">  ✅
<el-select v-model="store.radarInfo.imageAnalysisConfig['followDefoNumber']">  ❌ 不匹配
```

---

## ✅ 修复方案

### 修复1: 使用Object.assign强制更新

**修复前**:
```javascript
store.radarInfo.imageAnalysisConfig = {
  genImageType: data.genImageType,
  ...
};
```

**修复后**:
```javascript
// ✅ 使用Object.assign，保持响应式
Object.assign(store.radarInfo.imageAnalysisConfig, {
  genImageType: String(data.genImageType || '01'),
  followDefoInterval: Number(data.defoInterval || 60),
  scatInterval: Number(data.scatInterval || 60),
  followDefoNumber: Number(data.defoNumber || 10),
  scatNumber: Number(data.scatNumber || 10)
});
```

**优势**:
- 保持对象引用不变
- 触发Vue的响应式更新
- 确保页面刷新

### 修复2: 添加详细日志

```javascript
console.log('ImageGenerate.onMounted');
console.log('当前projectSelected:', store.projectInfo.projectSelected);
console.log('当前imageAnalysisConfig:', store.radarInfo.imageAnalysisConfig);
console.log('开始加载图像配置...');
await loadImageConfig();
console.log('加载完成，当前配置:', store.radarInfo.imageAnalysisConfig);
```

**好处**:
- 方便排查问题
- 确认数据加载流程
- 验证配置是否正确

### 修复3: 类型转换

```javascript
// ✅ 明确类型转换
genImageType: String(data.genImageType || '01'),
followDefoInterval: Number(data.defoInterval || 60),
scatInterval: Number(data.scatInterval || 60),
followDefoNumber: Number(data.defoNumber || 10),
scatNumber: Number(data.scatNumber || 10)
```

**原因**:
- 后端返回的可能是数字或字符串
- `el-select`需要字符串值
- `el-input.number`需要数字值

### 修复4: 默认选择第一个项目

```javascript
if (!store.projectInfo.projectSelected && store.projectInfo.projectData.length > 0) {
  // ✅ 自动选择第一个项目
  store.projectInfo.projectSelected = store.projectInfo.projectData[0].projectId;
  store.radarInfo.projectId = String(store.projectInfo.projectSelected);
  await loadImageConfig();
}
```

---

## 🔄 修复后的完整流程

```
用户打开"雷达图像生成配置"页面
  ↓
onMounted执行
  ↓
console.log('ImageGenerate.onMounted')
console.log('当前projectSelected:', 'PRJ001')
  ↓
初始化默认配置:
  store.radarInfo.imageAnalysisConfig = {
    genImageType: '01',
    followDefoInterval: 60,
    scatInterval: 60,
    followDefoNumber: 10,
    scatNumber: 10
  }
  ↓
console.log('初始化默认配置')
  ↓
设置projectId = 'PRJ001'
  ↓
console.log('开始加载图像配置...')
  ↓
GET /api/protocol/query/project/imageAnalysisConfig/PRJ001
  ↓
后端返回:
  {
    code: 200,
    data: {
      genImageType: "02",
      defoInterval: 30,
      scatInterval: 60,
      defoNumber: 10,
      scatNumber: 10
    }
  }
  ↓
console.log('加载图像配置响应:', res)
  ↓
Object.assign更新配置:
  genImageType: "02" ✅
  followDefoInterval: 30 ✅
  scatInterval: 60 ✅
  followDefoNumber: 10 ✅
  scatNumber: 10 ✅
  ↓
console.log('图像配置已加载:', {...})
  ↓
页面显示:
  生成类型下拉框: "按时间间隔" ✅
  形变图间隔: 30 ✅
  散射图间隔: 60 ✅
  ↓
✅ 配置正确显示
```

---

## 🎯 验证步骤

### 测试1: 打开页面

1. 刷新前端页面
2. 打开浏览器控制台（F12）
3. 打开"雷达图像生成配置"
4. 查看控制台输出

**预期日志**:
```
ImageGenerate.onMounted
当前projectSelected: PRJ001
当前imageAnalysisConfig: { genImageType: "01", ... }
初始化默认配置
开始加载图像配置...
加载图像配置响应: { data: { code: 200, ... } }
图像配置已加载: { genImageType: "02", followDefoInterval: 30, ... }
加载完成，当前配置: { genImageType: "02", ... }
```

**预期页面显示**:
- 项目下拉框: "项目1" ✅
- 生成类型: "按时间间隔" ✅
- 形变图生成间隔: 30 ✅
- 散射图生成间隔: 60 ✅

### 测试2: 切换项目

1. 切换项目下拉框
2. 查看控制台输出

**预期日志**:
```
ImageGenerate: 监听到项目切换 PRJ001 → PRJ002
开始加载图像配置...
加载图像配置响应: ...
```

**预期页面**:
- 自动加载PRJ002的配置
- 显示PRJ002的类型和数值

### 测试3: 保存配置

1. 修改生成类型
2. 点击"保存配置"
3. 刷新页面

**预期**:
- 保存成功提示
- 刷新后显示刚才保存的配置

---

## 📋 修复清单

| 修复项 | 问题 | 解决方案 |
|-------|------|---------|
| 响应式更新 | 直接赋值不触发 | 使用Object.assign |
| 类型转换 | 类型不匹配 | String()/Number()转换 |
| 调试日志 | 无法排查 | 添加详细console.log |
| 默认项目 | 没有选中 | 自动选择第一个 |
| 字段映射 | 后端defoInterval vs 前端followDefoInterval | 映射正确 |

---

## ✅ 修改文件

| 文件 | 修改内容 |
|-----|---------|
| `ImageGenerate.vue` | 1. 使用Object.assign更新配置<br>2. 添加类型转换<br>3. 添加详细日志<br>4. 默认选择第一个项目 |

---

**修复完成！刷新前端页面，打开控制台查看日志！** 🔧

**现在应该能正确显示：**
- ✅ 项目名称
- ✅ 生成类型（按帧号/按时间）
- ✅ 形变图配置
- ✅ 散射图配置

