# 配置页面自动加载功能完成报告

**完成时间**: 2025-10-25  
**涉及页面**: 
1. 雷达图像生成配置 (ImageGenerate.vue)
2. 算法参数配置 (AlgorithmParams.vue)
3. 速度指标配置 (SpeedTarget.vue)

---

## ✅ 完成的功能

### 功能1: 雷达图像生成配置

#### 实现的功能

1. ✅ **页面打开时自动加载配置**
   - `onMounted` → 调用`loadImageConfig()`
   - 从数据库读取该项目的配置
   - 显示到页面

2. ✅ **项目切换时自动更新**
   - 添加`watch`监听`store.projectInfo.projectSelected`
   - 项目改变时自动调用`loadImageConfig()`
   - 显示新项目的配置

3. ✅ **无记录时使用默认值**
   - 404响应 → 使用默认配置
   - 不报错，用户体验好

4. ✅ **每个项目一条记录**
   - 数据库UNIQUE约束
   - 后端智能判断UPDATE或INSERT

#### 完整流程

```
打开页面
  ↓
onMounted执行
  ↓
GET /api/protocol/query/project/imageAnalysisConfig/PRJ001
  ↓
  ├─ 200 → 加载配置 ✅
  └─ 404 → 使用默认值 ✅
  ↓
用户修改配置
  ↓
点击"保存配置"
  ↓
POST /api/protocol/update/project/imageAnalysisConfig
  ↓
  ├─ 有记录 → UPDATE ✅
  └─ 无记录 → INSERT ✅
  ↓
下次打开显示已保存的配置 ✅
  ↓
切换到PRJ002
  ↓
watch监听触发
  ↓
GET /api/protocol/query/project/imageAnalysisConfig/PRJ002
  ↓
显示PRJ002的配置 ✅
```

---

### 功能2: 算法参数配置

#### 实现的功能

1. ✅ **选择设备时从数据库获取参数**
   - `selectOnChange` → 调用`ApiRadar.getAlgorithmParam()`
   - 优先从数据库读取
   - 数据库没有时从设备对象读取
   - 都没有时使用默认值

2. ✅ **三级加载策略**
   ```
   第一优先: 数据库algorithm_configs表 ✅
     ↓ (404)
   第二优先: 设备对象device.algorithmParam ✅
     ↓ (空)
   第三优先: 默认值 ✅
   ```

3. ✅ **后端返回字段统一**
   - 使用大写开头（`FilterType`，不是`filterType`）
   - 兼容下划线版本（`Monitor_Mode`和`MonitorMode`都支持）

#### 完整流程

```
打开页面 → 选择项目PRJ001
  ↓
选择设备DEV001
  ↓
selectOnChange执行
  ↓
步骤1: 尝试从数据库获取
  GET /api/protocol/algorithm/PRJ001/DEV001
  ↓
  ├─ 200 → 找到参数 ✅
  │   ↓
  │   Object.assign(algorithmParams, data)
  │   ✅ 显示数据库中的参数
  │
  └─ 404 → 未找到
      ↓
      步骤2: 从设备对象获取
      device.algorithmParam
      ↓
      ├─ 有数据 → 使用设备对象的参数
      └─ 无数据 → 使用默认值
  ↓
页面显示参数 ✅
  ↓
用户修改参数
  ↓
点击"下发指令"
  ↓
先发送指令到设备 → 成功后保存到数据库
  ↓
下次选择该设备时，从数据库读取最新参数 ✅
```

---

### 功能3: 速度指标配置

#### 实现的功能

1. ✅ **支持两种选择方式**
   - 下拉框多选（默认）
   - 复选框（可选）

2. ✅ **项目切换自动加载**
   - 添加`watch`监听
   - 自动加载新项目的配置

3. ✅ **已选择标签显示**
   - 可视化显示选中的时间单位
   - 点击×快速删除

4. ✅ **时间单位代码统一**
   - 前后端都使用`00-05`
   - 不再有映射错误

#### 完整流程

```
打开页面
  ↓
onMounted执行
  ↓
GET /api/protocol/query/speed/target/PRJ001
  ↓
  ├─ 200 → timeUnits = "00,02,04"
  │   ↓
  │   selectedTimeUnits = ['00', '02', '04']
  │   ✅ 显示: [30分钟] [1天] [1周]
  │
  └─ 404 → selectedTimeUnits = []
      ✅ 显示: 未选择
  ↓
用户修改选择
  ├─ 下拉框: 选择30分钟、1小时、1天
  └─ 或复选框: 勾选30分钟、1小时、1天
  ↓
selectedTimeUnits = ['00', '01', '02']
  ↓
点击"保存配置"
  ↓
POST /api/protocol/update/speed/target
  {
    projectId: "PRJ001",
    timeUnit: ['00', '01', '02']
  }
  ↓
后端保存:
  time_units = "00,01,02"
  enable_30min = 1
  enable_1hour = 1
  enable_1day = 1
  ↓
✅ 保存成功
  ↓
切换到PRJ002
  ↓
watch监听触发
  ↓
自动加载PRJ002的配置 ✅
```

---

## 🔄 数据加载优先级

### 图像生成配置

```
数据库 image_analysis_configs表 (优先)
  ↓ (404)
默认值
```

### 算法参数配置

```
数据库 algorithm_configs表 (优先)
  ↓ (404)
设备对象 device.algorithmParam (次优先)
  ↓ (空)
默认值
```

### 速度指标配置

```
数据库 speed_indices表 (优先)
  ↓ (404)
空数组（未选择任何单位）
```

---

## 📊 数据一致性保证

### UNIQUE约束

```sql
-- ✅ 每个项目一条记录
ALTER TABLE image_analysis_configs ADD CONSTRAINT unique_project UNIQUE (project_id);
ALTER TABLE speed_indices ADD CONSTRAINT unique_project UNIQUE (project_id);
ALTER TABLE colorbar_configs ADD CONSTRAINT unique_project_mode UNIQUE (project_id, mode);
```

### 智能更新或创建

```csharp
var config = await _dbContext.Configs.FirstOrDefaultAsync(c => c.ProjectId == projectId);

if (config != null) {
    // ✅ 更新
    config.Field1 = value1;
    config.Field2 = value2;
} else {
    // ✅ 创建
    var newConfig = new ConfigEntity { ... };
    _dbContext.Add(newConfig);
}

await _dbContext.SaveChangesAsync();
```

**效果**:
- 不会创建重复记录
- 配置始终是最新的

---

## 🎯 测试场景

### 场景1: 首次使用（无配置）

```
项目PRJ001首次使用
  ↓
打开"图像生成配置"
  ✅ 显示默认值（按帧号, 10圈）
  ↓
修改: 按时间间隔, 30分钟
  ↓
保存 → INSERT新记录
  ✅ 数据库创建记录
```

### 场景2: 再次打开（有配置）

```
再次打开"图像生成配置"
  ✅ 显示上次保存的配置（按时间间隔, 30分钟）
  ↓
修改: 60分钟
  ↓
保存 → UPDATE记录
  ✅ 数据库更新记录（不创建新记录）
```

### 场景3: 切换项目

```
从PRJ001切换到PRJ002
  ↓
watch监听触发
  ↓
自动加载PRJ002的配置
  ├─ PRJ002有配置 → 显示PRJ002的配置
  └─ PRJ002无配置 → 显示默认值
  ✅ 不同项目显示不同配置
```

### 场景4: 算法参数 - 设备切换

```
选择设备DEV001
  ↓
selectOnChange执行
  ↓
GET /api/protocol/algorithm/PRJ001/DEV001
  ↓
  ├─ 200 → 显示数据库中的参数 ✅
  └─ 404 → 尝试从device对象加载
      ├─ 有 → 显示设备对象的参数
      └─ 无 → 使用默认值
```

---

## 📋 修改文件清单

| 文件 | 修改内容 |
|-----|---------|
| `ImageGenerate.vue` | 添加`watch`监听项目切换 |
| `AlgorithmParams.vue` | `selectOnChange`改为async，从数据库获取参数 |
| `SpeedTarget.vue` | 添加`watch`监听项目切换 |
| `ProtocolController.cs` | `GetAlgorithmParam`返回字段名统一为大写开头 |

---

## ✅ 完整的配置加载逻辑

### 雷达图像生成配置

```javascript
// 页面加载
onMounted → loadImageConfig()

// 项目切换
watch(projectSelected) → loadImageConfig()

// 手动切换
onProjectChange → loadImageConfig()

// 加载逻辑
loadImageConfig() {
  GET /api/protocol/query/project/imageAnalysisConfig/{projectId}
  ↓
  200 → 加载到页面
  404 → 使用默认值
}
```

### 算法参数配置

```javascript
// 项目切换
onProjectChange → 加载设备列表 → selectOnChange()

// 设备切换
selectOnChange() {
  步骤1: GET /api/protocol/algorithm/{projectId}/{deviceId}
  ↓
  200 → 从数据库加载 ✅
  404 → 从设备对象加载 → 无 → 默认值
}
```

### 速度指标配置

```javascript
// 页面加载
onMounted → loadSpeedConfig()

// 项目切换
watch(projectSelected) → loadSpeedConfig()

// 手动切换
onProjectChange → loadSpeedConfig()

// 加载逻辑
loadSpeedConfig() {
  GET /api/protocol/query/speed/target/{projectId}
  ↓
  200 → 解析timeUnits → 显示标签
  404 → selectedTimeUnits = []
}
```

---

## 🚀 测试验证

### 测试清单

| 页面 | 测试项 | 预期结果 |
|-----|-------|---------|
| 图像生成 | 首次打开 | 显示默认值或已保存配置 ✅ |
| 图像生成 | 项目切换 | 自动加载新项目配置 ✅ |
| 图像生成 | 保存后重开 | 显示上次保存的配置 ✅ |
| 算法参数 | 选择设备 | 从数据库加载参数 ✅ |
| 算法参数 | 无数据库记录 | 从设备对象或默认值 ✅ |
| 算法参数 | 保存后重选 | 显示上次保存的参数 ✅ |
| 速度指标 | 首次打开 | 显示已保存配置或空 ✅ |
| 速度指标 | 项目切换 | 自动加载新项目配置 ✅ |
| 速度指标 | 下拉框选择 | 多选工作正常 ✅ |
| 速度指标 | 复选框选择 | 勾选工作正常 ✅ |
| 速度指标 | 标签显示 | 已选择单位可见可删 ✅ |

---

## 💡 关键技术点

### 1. Vue Watch监听

```javascript
watch(() => store.projectInfo.projectSelected, async (newVal, oldVal) => {
  if (newVal && newVal !== oldVal) {
    console.log('监听到项目切换', oldVal, '→', newVal);
    await loadConfig();  // 自动重新加载
  }
});
```

**优势**:
- 自动响应项目切换
- 无需用户手动刷新
- 不同项目配置自动隔离

### 2. 三级加载策略（算法参数）

```javascript
try {
  // 第一优先: 数据库
  const res = await ApiRadar.getAlgorithmParam(projectId, deviceId);
  if (res.data.code === 200) {
    Object.assign(algorithmParams, res.data.data);
  }
} catch {
  // 第二优先: 设备对象
  if (device.algorithmParam) {
    Object.assign(algorithmParams, device.algorithmParam);
  }
  // 第三优先: 默认值（已在reactive初始化时设置）
}
```

**优势**:
- 容错性强
- 灵活适应不同情况
- 用户体验好

### 3. 智能更新或创建（后端）

```csharp
var config = await _dbContext.Configs.FirstOrDefaultAsync(...);

if (config != null) {
    // UPDATE
} else {
    // INSERT
}

await _dbContext.SaveChangesAsync();
```

**优势**:
- 防止重复记录
- UNIQUE约束保证一致性
- 操作幂等性

---

## 📊 数据流向图

### 完整的配置生命周期

```
用户首次打开页面（PRJ001）
  ↓
onMounted → loadConfig()
  ↓
GET /api/.../{PRJ001}
  ↓
404（无记录）
  ↓
使用默认值显示
  ↓
用户修改配置
  ↓
点击"保存"
  ↓
POST /api/...
  ↓
后端: 无记录 → INSERT
  ↓
数据库创建记录 ✅
  ↓
关闭页面
  ↓
再次打开
  ↓
onMounted → loadConfig()
  ↓
GET /api/.../{PRJ001}
  ↓
200（找到记录）✅
  ↓
显示已保存的配置 ✅
  ↓
切换到PRJ002
  ↓
watch触发 → loadConfig()
  ↓
GET /api/.../{PRJ002}
  ↓
404（PRJ002无记录）
  ↓
显示默认值 ✅
  ↓
修改并保存PRJ002的配置
  ↓
数据库: PRJ001一条, PRJ002一条 ✅
```

---

## ✅ 总结

### 完成的工作

1. ✅ 图像生成配置 - 添加watch监听
2. ✅ 算法参数配置 - 从数据库获取参数
3. ✅ 速度指标配置 - 添加watch监听
4. ✅ 后端字段名统一 - 大写开头
5. ✅ 三级加载策略 - 数据库→设备→默认
6. ✅ 编译成功 - 0错误0警告

### 数据一致性

- ✅ 每个项目一条记录（UNIQUE约束）
- ✅ 更新不创建重复记录
- ✅ 不同项目配置独立

### 用户体验

- ✅ 打开即显示已保存配置
- ✅ 切换项目自动刷新
- ✅ 无记录时不报错
- ✅ 灵活的选择方式（下拉框+复选框）

### 编译状态

```
✅ 已成功生成
   0 个警告
   0 个错误
```

---

**修复完成！刷新前端页面测试！** 🎉

**现在支持：**
- ✅ 雷达图像生成 - 项目切换自动更新
- ✅ 算法参数 - 选择设备时从数据库获取
- ✅ 速度指标 - 项目切换自动更新
- ✅ 所有配置 - 每个项目一条记录

