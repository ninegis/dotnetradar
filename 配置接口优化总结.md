# 雷达系统配置接口优化总结

## 概述
本次优化将所有配置数据从JSON存储方式改为独立数据库字段存储，确保数据的一致性和可维护性。

## 已完成的配置接口优化

### 1. 速度指标配置 (SpeedIndexEntity)
**表名**: `speed_indices`
**API接口**: `POST /api/protocol/update/speed/target`

**优化前**: 使用JSON存储时间单位数组
**优化后**: 使用独立字段存储
- `time_units`: 时间单位字符串（逗号分隔）
- `enable_30min`: 是否启用30分钟
- `enable_1hour`: 是否启用1小时  
- `enable_1day`: 是否启用1天
- `enable_3day`: 是否启用3天
- `enable_1week`: 是否启用1周
- `enable_1month`: 是否启用1月

### 2. 色条配置 (ColorBarConfigEntity)
**表名**: `colorbar_configs`
**API接口**: `POST /api/protocol/update/colorBar`

**优化前**: 使用JSON存储完整配置
**优化后**: 使用独立字段存储
- `mode`: 模式（defo/scat）
- `min_value`: 最小值
- `max_value`: 最大值
- `hsl_h_start`: HSL色相起始值
- `hsl_h_end`: HSL色相结束值
- `filter_alpha`: 透明度
- `filter_min`: 过滤最小值
- `filter_max`: 过滤最大值
- `filter_enable`: 是否启用透明通道

### 3. 隐患区域分析配置 (HiddenAreaAnalysisConfigEntity)
**表名**: `hidden_area_analysis_configs`
**API接口**: `POST /api/protocol/update/hidden/analysis`

**优化前**: 使用JSON存储配置
**优化后**: 使用独立字段存储
- `threshold`: 隐患点阈值（mm）
- `area_threshold`: 隐患区域面积阈值（m²）
- `analysis_dec`: 隐患生成设置（间隔帧）
- `auto_analysis_flag`: 是否开启隐患区域分析

### 4. 预警规则配置 (AlarmRuleEntity)
**表名**: `alarm_rules`
**API接口**: `POST /api/protocol/add/ruleBatch`, `POST /api/protocol/update/ruleBatch`

**优化前**: 使用JSON存储复杂的预警规则配置
**优化后**: 使用独立字段存储所有指标和阈值
- 基本信息字段：`rule_name`, `alarm_content`, `enable`, `devices`, `geo_mark_array`, `data_source`, `target_flag`
- 位移指标字段：`enable_displacement`, `displacement_blue`, `displacement_yellow`, `displacement_orange`, `displacement_red`
- 速度指标字段：`enable_speed`, `speed_time_unit`, `speed_blue`, `speed_yellow`, `speed_orange`, `speed_red`
- 加速度指标字段：`enable_acceleration`, `acceleration_time_unit`, `acceleration_blue`, `acceleration_yellow`, `acceleration_orange`, `acceleration_red`

### 5. 短信模板配置 (SmsConfigEntity)
**表名**: `sms_configs`
**API接口**: `POST /api/protocol/update/smsConfig`

**优化前**: 使用JSON存储短信配置
**优化后**: 使用独立字段存储
- `enable`: 是否启用短信通知
- `notify_channel`: 短信推送通道（00:无模板, 01:阿里云）
- `access_key_id`: 访问密钥ID
- `access_key_secret`: 访问密钥
- `sign_name`: 短信签名
- `template_code`: 模板代码
- `provider`: 短信服务商（保留字段）
- `api_key`: API密钥（保留字段）
- `api_secret`: API密钥Secret（保留字段）
- `template_content`: 模板内容（保留字段）

### 6. 算法配置 (AlgorithmConfigEntity)
**表名**: `algorithm_configs`
**API接口**: `POST /api/protocol/update/radar/algorithmParam`, `POST /api/protocol/update/mimoLite/algorithmParam`

**优化前**: 使用JSON存储算法参数
**优化后**: 使用独立字段存储
- `filter_type`: 滤波类型
- `alpha_filter`: Alpha滤波参数
- `beta_filter`: Beta滤波参数
- `de_noise_thread`: 去噪阈值
- `sens_coef`: 灵敏度系数
- `defo_image_dec`: 形变图像抽取
- `scat_image_dec`: 散射图像抽取
- `win_coheren`: 窗口相干
- `atm_pha_err_est_func_switch`: 大气相位误差估计开关
- `filter_width`: 滤波宽度
- `monitor_mode`: 监测模式
- `ipv4`: IP地址

## 数据库迁移优化

### 新增表结构
1. `speed_indices` - 速度指标配置表
2. `colorbar_configs` - 色条配置表
3. `hidden_area_analysis_configs` - 隐患区域分析配置表
4. `algorithm_configs` - 算法配置表
5. `command_records` - 指令下发记录表
6. `sms_configs` - 短信配置表

### 表结构特点
- 所有配置表都包含 `project_id` 字段，支持多项目隔离
- 移除了所有 `config_json` 字段，改为独立字段存储
- 添加了适当的索引和外键约束
- 支持软删除（`is_deleted` 字段）

## API接口优化

### 接口更新
1. **速度指标配置**: 解析前端时间单位数组，保存到独立字段
2. **色条配置**: 直接保存各个色条参数到独立字段
3. **隐患区域分析**: 保存阈值和分析参数到独立字段
4. **预警规则**: 解析复杂的JSON结构，提取各指标阈值保存到独立字段
5. **短信模板**: 保存短信服务商配置到独立字段
6. **算法配置**: 保存算法参数到独立字段

### 数据解析优化
- 移除了所有 `JsonSerializer.Serialize()` 调用
- 直接解析前端JSON数据到对应实体字段
- 支持复杂嵌套结构的解析（如预警规则的 `alarmTargetThresholds` 数组）

## 前端接口对应关系

| 前端配置 | 后端API | 数据库表 | 主要字段 |
|---------|---------|----------|----------|
| `imageDiffAnalysisConfig` | `/api/protocol/update/speed/target` | `speed_indices` | `time_units`, `enable_*` |
| `colorBarConfig` | `/api/protocol/update/colorBar` | `colorbar_configs` | `min_value`, `max_value`, `hsl_*` |
| `autoAnalysisHiddenAreaConfig` | `/api/protocol/update/hidden/analysis` | `hidden_area_analysis_configs` | `threshold`, `area_threshold` |
| `alarmRules` | `/api/protocol/add/ruleBatch` | `alarm_rules` | `displacement_*`, `speed_*`, `acceleration_*` |
| `smsNotifyConfig` | `/api/protocol/update/smsConfig` | `sms_configs` | `access_key_*`, `sign_name`, `template_code` |
| `algorithmParam` | `/api/protocol/update/radar/algorithmParam` | `algorithm_configs` | `filter_*`, `sens_coef`, `defo_image_dec` |

## 优化效果

### 数据一致性
- 消除了JSON序列化/反序列化可能导致的数据不一致问题
- 所有配置数据都有明确的字段类型和约束

### 查询性能
- 可以直接基于字段进行查询和索引
- 避免了JSON字段的复杂查询

### 维护性
- 配置结构清晰，易于理解和维护
- 支持数据库级别的数据验证和约束

### 扩展性
- 新增配置项只需添加字段，无需修改JSON结构
- 支持数据库迁移和版本控制

## 注意事项

1. **数据迁移**: 现有JSON数据需要迁移到新的字段结构
2. **向后兼容**: 需要确保前端接口调用的一致性
3. **数据验证**: 需要在API层面添加数据验证逻辑
4. **索引优化**: 根据查询需求添加适当的数据库索引

## 总结

通过本次优化，所有配置数据都从JSON存储改为独立字段存储，提高了数据的一致性、查询性能和可维护性。所有配置接口都已更新，支持直接保存到数据库字段，避免了JSON序列化可能带来的问题。
