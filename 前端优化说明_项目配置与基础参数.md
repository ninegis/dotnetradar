# 前端优化说明 - 项目配置与基础参数

## 优化概述

本次优化主要解决了以下问题：
1. 项目配置页面字段显示错误（项目ID显示了项目名称，项目名称为空）
2. 项目配置页面增加了项目信息提交功能
3. 项目配置页面新增显示项目的经度、纬度、高程
4. 基础参数页面默认加载第一个设备信息

## 修改清单

### 1. ProjectConfig.vue（项目配置页面）

**优化内容：**

#### 1.1 修复字段显示问题
- 原问题：`store.radarInfo.projectId` 和 `store.radarInfo.projectConfig['name']` 显示不正确
- 解决方案：使用计算属性 `currentProjectInfo` 直接从 `store.projectInfo.projectData` 获取当前选中项目的完整信息

```vue
// 新增计算属性
const currentProjectInfo = computed(() => {
  const currentProject = store.projectInfo.projectData.find(
    p => p.projectId === store.projectInfo.projectSelected
  );
  return currentProject || {
    projectId: '',
    projectName: '',
    description: '',
    longitude: 0,
    latitude: 0,
    elevation: 0,
    contactPerson: '',
    contactPhone: '',
    contactEmail: '',
    devices: []
  };
});
```

#### 1.2 新增地理位置信息显示
在项目配置页面添加了三个字段：
- **经度** (longitude) - 支持6位小数精度
- **纬度** (latitude) - 支持6位小数精度
- **高程** (elevation) - 支持2位小数精度（单位：米）

```vue
<el-divider content-position="left">地理位置信息</el-divider>
<el-form-item :label="$t('common.longitude')">
  <el-input-number v-model="currentProjectInfo.longitude" :precision="6" :step="0.000001" style="width: 100%" placeholder="120.6"/>
</el-form-item>
<el-form-item :label="$t('common.latitude')">
  <el-input-number v-model="currentProjectInfo.latitude" :precision="6" :step="0.000001" style="width: 100%" placeholder="31.3"/>
</el-form-item>
<el-form-item label="高程(m)">
  <el-input-number v-model="currentProjectInfo.elevation" :precision="2" :step="0.1" style="width: 100%" placeholder="0"/>
</el-form-item>
```

#### 1.3 优化提交功能
- 使用 `ApiRadar.UpdateProject` API 方法
- 提交时包含所有项目信息（名称、描述、联系信息、地理位置）
- 添加了表单验证（项目名称不能为空）
- 优化了错误处理和成功提示

```javascript
const commitUpdate = ()=>{
  if (!currentProjectInfo.value.projectName) {
    showMessage('项目名称不能为空', 'warning');
    return;
  }
  
  ApiRadar.UpdateProject(currentProjectInfo.value.projectId, {
    projectName: currentProjectInfo.value.projectName,
    projectDescribe: currentProjectInfo.value.description,
    contact: currentProjectInfo.value.contactPerson,
    phone: currentProjectInfo.value.contactPhone,
    email: currentProjectInfo.value.contactEmail,
    lon: currentProjectInfo.value.longitude,
    lat: currentProjectInfo.value.latitude,
    alt: currentProjectInfo.value.elevation
  }).then(res=>{
    // 处理响应...
  })
}
```

### 2. RadarParams.vue（基础参数页面）

**优化内容：**

#### 2.1 默认加载第一个设备
- 原问题：打开基础参数页面时，如果没有选中设备，不会加载任何设备信息
- 解决方案：在 `onMounted` 钩子中检查，如果没有选中设备且设备列表不为空，自动选择第一个设备

```javascript
onMounted(() => {
  // ✅ 如果当前没有选中设备，默认选择第一个设备
  if (!store.radarInfo.deviceId && store.projectInfo.deviceData.length > 0) {
    store.radarInfo.deviceId = store.projectInfo.deviceData[0].id;
  }
  
  if (store.radarInfo.deviceId) {
    currentRadar.value = store.radarInfo.deviceId.substring(0, 8);
    selectOnChange(store.radarInfo.deviceId);
  }
});
```

### 3. radartool.js（数据映射工具）

**优化内容：**

#### 3.1 增强数据映射
在 `projectDataInit` 函数中，同时保留原始字段名和映射字段名，确保新旧组件都能正常工作：

```javascript
store.projectInfo.projectData = projects.map(p => ({
  // 保留原始字段名（用于新组件）
  projectId: p.projectId,
  projectName: p.projectName,
  // 映射字段名（用于旧组件兼容）
  id: p.projectId,
  name: p.projectName,
  // 其他字段...
  longitude: p.longitude,
  latitude: p.latitude,
  elevation: p.elevation,
  devices: (p.devices || []).map(d => ({
    deviceId: d.deviceId,
    deviceName: d.deviceName,
    id: d.deviceId,
    name: d.deviceName,
    coordinates: [d.longitude || 0, d.latitude || 0, d.elevation || 0],
    params: d.params || {},
    // 其他字段...
  }))
}));
```

## 页面展示效果

### 项目配置页面

```
┌─────────────────────────────────────┐
│ 项目配置                              │
├─────────────────────────────────────┤
│ 项目选择: [下拉框] [+] [-]            │
│ 项目ID: project001 (禁用)            │
│ 项目名称: 苏州边坡监测项目             │
│ 项目描述: [文本框]                    │
│                                       │
│ ─── 地理位置信息 ───                  │
│ 经度: [120.612345]                   │
│ 纬度: [31.345678]                    │
│ 高程(m): [100.50]                    │
│                                       │
│ ─── 联系信息 ───                      │
│ 联系人: [张三]                        │
│ 联系电话: [13812345678]               │
│ 联系邮箱: [zhangsan@example.com]     │
│                                       │
│ ─── 项目设备列表 ───                  │
│ [添加设备] [查看设备列表]              │
│ • Online  设备1 (ARC_20241024_001)   │
│ • Offline 设备2 (MIMO_20241024_002)  │
│                                       │
│ [初始化相机设置] [提交修改]            │
└─────────────────────────────────────┘
```

### 基础参数页面

```
┌─────────────────────────────────────┐
│ 基础参数                              │
├─────────────────────────────────────┤
│ 设备: [设备1 ▼] [+] [-]              │
│ 设备ID: ARC_20241024_001             │
│ 设备名称: [边坡雷达1号]               │
│ 出厂ID: [12345]                      │
│ 经度: [120.612345] [📍]             │
│ 纬度: [31.345678]                    │
│ 高度: [100.5]                        │
│ 边界显示: [开关]                      │
│ 零点朝向: [45.5°] [📐]              │
│ ...                                  │
│ [参数设置]                            │
└─────────────────────────────────────┘
```

## 数据流说明

### 1. 项目数据流

```
后端API (GetRadarData)
  ↓ 返回 projectId, projectName, longitude, latitude, elevation 等
projectDataInit()
  ↓ 映射为同时包含原始字段名和兼容字段名的对象
store.projectInfo.projectData
  ↓
ProjectConfig.vue (currentProjectInfo computed)
  ↓ 绑定到表单
用户修改
  ↓
commitUpdate()
  ↓ 调用 UpdateProject API
后端更新
  ↓
重新加载数据
```

### 2. 设备数据流

```
后端API (GetRadarData)
  ↓ 返回 devices 数组
projectDataInit()
  ↓ 映射设备字段（deviceId, deviceName, coordinates等）
store.projectInfo.deviceData
  ↓
RadarParams.vue onMounted
  ↓ 自动选择第一个设备（如果没有选中）
selectOnChange()
  ↓ 加载设备详细信息
显示在表单中
```

## 向后兼容性

### 字段映射策略

为了确保新旧代码都能正常工作，采用了双字段映射策略：

| 后端字段 | 映射字段1（新） | 映射字段2（旧） |
|---------|---------------|---------------|
| projectId | projectId | id |
| projectName | projectName | name |
| contactPerson | contactPerson | contact |
| contactPhone | contactPhone | phone |
| contactEmail | contactEmail | email |
| deviceId | deviceId | id |
| deviceName | deviceName | name |

这样做的好处：
1. 新组件可以使用语义更清晰的字段名（projectId, projectName）
2. 旧组件继续使用简短的字段名（id, name）
3. 不需要修改现有的大量代码

## 测试建议

### 1. 项目配置页面测试
- [ ] 打开项目配置页面，检查项目ID和项目名称是否正确显示
- [ ] 检查经度、纬度、高程是否正确显示
- [ ] 修改项目名称，点击"提交修改"，检查是否保存成功
- [ ] 修改联系信息，点击"提交修改"，检查是否保存成功
- [ ] 修改地理位置信息，点击"提交修改"，检查是否保存成功
- [ ] 切换不同项目，检查信息是否正确切换

### 2. 基础参数页面测试
- [ ] 打开基础参数页面，检查是否自动加载第一个设备信息
- [ ] 如果没有设备，页面应正常显示而不报错
- [ ] 切换设备，检查信息是否正确更新
- [ ] 修改设备参数，点击"参数设置"，检查是否保存成功

### 3. 集成测试
- [ ] 在项目配置页面修改项目信息后，刷新页面，检查修改是否持久化
- [ ] 添加新设备后，在基础参数页面应该能看到新设备
- [ ] 删除设备后，基础参数页面应该切换到第一个剩余设备

## 注意事项

1. **数据一致性**：项目配置页面的修改会立即反映到全局状态（store），其他组件会自动更新
2. **表单验证**：目前只验证了项目名称不能为空，后续可根据需求添加更多验证规则
3. **坐标精度**：经纬度使用6位小数（约0.11米精度），高程使用2位小数（0.01米精度）
4. **默认值**：所有数值字段都有默认值0，确保不会出现undefined或null

## 后续优化建议

1. **表单验证增强**
   - 添加经纬度范围验证（经度：-180~180，纬度：-90~90）
   - 添加电话号码格式验证
   - 添加邮箱格式验证

2. **用户体验优化**
   - 添加地图选点功能（点击按钮在地图上选择项目位置）
   - 添加修改确认提示（避免误操作）
   - 添加修改历史记录

3. **性能优化**
   - 使用防抖处理表单输入
   - 优化数据映射逻辑，减少重复映射

## 相关文件

- `RadarContrl/src/components/ToolBar/Tool/ProjectConfig/ProjectConfig.vue`
- `RadarContrl/src/components/ToolBar/Tool/RadarParams/RadarParams.vue`
- `RadarContrl/src/utils/radartool.js`
- `RadarContrl/src/axios/apiRadar.js`

## 更新日期

2024-10-24

