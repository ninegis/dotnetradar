# 雷达图像生成配置完整实现说明

**实现时间**: 2025-10-25  
**页面**: 工具 → 雷达参数 → 雷达图像生成 (ImageGenerate.vue)

---

## 🎯 功能概述

### 核心功能

配置雷达如何自动生成形变图和散射图：

1. **生成方式选择**
   - 按帧号生成（转几圈生成一次图像）
   - 按时间间隔生成（每隔N分钟生成一次图像）

2. **形变图配置**
   - 按帧号：设置转几圈生成一次（1/2/5/10/20圈）
   - 按时间：设置生成间隔（分钟）

3. **散射图配置**
   - 按帧号：设置转几圈生成一次（1/2/5/10/20圈）
   - 按时间：设置生成间隔（分钟）

---

## 📋 数据库表结构

### image_analysis_configs表

```sql
CREATE TABLE image_analysis_configs (
    id TEXT PRIMARY KEY,
    project_id TEXT NOT NULL UNIQUE,
    
    -- 图像参数
    standard_image_side_pixel INTEGER DEFAULT 16384,  -- 标准图像边长
    compress_image_side_pixel INTEGER DEFAULT 1024,   -- 压缩图像边长
    matrix_tile_rng_num INTEGER DEFAULT 1203,         -- 矩阵距离数
    matrix_tile_ang_num INTEGER DEFAULT 61,           -- 矩阵角度数
    
    -- 生成开关
    gen_defo INTEGER DEFAULT 0,                       -- 生成形变图
    gen_scat INTEGER DEFAULT 1,                       -- 生成散射图
    gen_speed INTEGER DEFAULT 0,                      -- 生成速度图
    gen_acceleration INTEGER DEFAULT 0,               -- 生成加速度图
    
    -- ✅ 生成配置（独立字段，不再使用JSON）
    gen_image_type TEXT DEFAULT '01',                 -- 生成类型 (01:按帧号, 02:按时间)
    defo_interval INTEGER DEFAULT 60,                 -- 形变图生成间隔（分钟）
    scat_interval INTEGER DEFAULT 60,                 -- 散射图生成间隔（分钟）
    defo_number INTEGER DEFAULT 10,                   -- 形变图生成帧数（圈数）
    scat_number INTEGER DEFAULT 10,                   -- 散射图生成帧数（圈数）
    
    config_json TEXT,                                 -- 保留用于扩展配置
    create_time TEXT NOT NULL,
    update_time TEXT,
    
    FOREIGN KEY (project_id) REFERENCES Projects(ProjectId) ON DELETE CASCADE
);
```

---

## 🔄 数据流程

### 流程1: 页面加载

```
用户打开"雷达图像生成"页面
  ↓
onMounted() 执行
  ↓
设置默认配置（如果不存在）
  genImageType: '01'
  followDefoInterval: 60
  scatInterval: 60
  followDefoNumber: 10
  scatNumber: 10
  ↓
检查当前选中的项目
  ↓
loadImageConfig()
  ↓
GET /api/protocol/query/project/imageAnalysisConfig/{projectId}
  ↓
后端查询 image_analysis_configs表
  ↓
返回配置:
  {
    genImageType: "01",
    defoInterval: 60,
    scatInterval: 60,
    defoNumber: 10,
    scatNumber: 10
  }
  ↓
更新页面显示
  ✅ 生成类型下拉框显示"按帧号"
  ✅ 形变图帧数显示"10圈"
  ✅ 散射图帧数显示"10圈"
```

### 流程2: 切换生成方式

```
用户切换: "按帧号" → "按时间间隔"
  ↓
genImageType: '01' → '02'
  ↓
页面显示变化:
  隐藏: "转几圈生成1次图像"相关配置
  显示: "以下单位为分钟"相关配置
  ↓
显示:
  - 形变图生成间隔: [60分钟]
  - 散射图生成间隔: [60分钟]
```

### 流程3: 保存配置

```
用户修改配置
  ↓
点击"保存配置"
  ↓
commitUpdate() 执行
  ↓
验证项目ID
  ↓
调用 ApiRadar.updateImageAnalysisConfig()
  ↓
POST /api/protocol/update/project/imageAnalysisConfig
  {
    projectId: "PRJ001",
    genImageType: "02",
    defoInterval: 30,
    scatInterval: 60,
    defoNumber: 10,
    scatNumber: 10
  }
  ↓
后端更新 image_analysis_configs表:
  UPDATE image_analysis_configs SET
    gen_image_type = '02',
    defo_interval = 30,
    scat_interval = 60,
    defo_number = 10,
    scat_number = 10,
    update_time = NOW()
  WHERE project_id = 'PRJ001';
  ↓
✅ 返回 { code: 200 }
  ↓
提示"图像生成配置保存成功"
  ↓
记录操作日志
```

---

## 📊 配置说明

### 配置项1: 生成类型

| 值 | 名称 | 说明 | 适用场景 |
|----|------|------|---------|
| `01` | 按帧号生成 | 雷达转N圈生成一次图像 | 雷达持续旋转扫描 |
| `02` | 按时间间隔生成 | 每隔N分钟生成一次图像 | 固定时间间隔监测 |

### 配置项2: 形变图配置

**按帧号生成** (genImageType='01'):
- `defoNumber`: 转几圈生成一次
  - 1圈：快速生成，数据量大
  - 10圈：常规生成（推荐）
  - 20圈：降低频率，节省存储

**按时间生成** (genImageType='02'):
- `defoInterval`: 生成间隔（分钟）
  - 30分钟：高频监测
  - 60分钟：常规监测（推荐）
  - 120分钟：低频监测

### 配置项3: 散射图配置

**按帧号生成**:
- `scatNumber`: 转几圈生成一次
  - 通常比形变图频率低（散射图变化慢）

**按时间生成**:
- `scatInterval`: 生成间隔（分钟）
  - 通常比形变图间隔长

---

## 🔄 前后端字段映射

### 前端 → 后端 → 数据库

| 前端字段 | 后端字段 | 数据库字段 | 说明 |
|---------|---------|-----------|------|
| `genImageType` | `GenImageType` | `gen_image_type` | 生成类型 |
| `followDefoInterval` | `DefoInterval` | `defo_interval` | 形变图间隔 |
| `scatInterval` | `ScatInterval` | `scat_interval` | 散射图间隔 |
| `followDefoNumber` | `DefoNumber` | `defo_number` | 形变图帧数 |
| `scatNumber` | `ScatNumber` | `scat_number` | 散射图帧数 |

---

## 🚀 后端接口

### 接口1: 更新图像生成配置

**端点**: `POST /api/protocol/update/project/imageAnalysisConfig`

**请求参数**:
```json
{
  "projectId": "PRJ001",
  "genImageType": "02",
  "defoInterval": 30,
  "scatInterval": 60,
  "defoNumber": 10,
  "scatNumber": 10
}
```

**返回**:
```json
{
  "code": 200,
  "message": "图像分析配置更新成功",
  "data": {
    "projectId": "PRJ001",
    "genDefo": true,
    "genScat": true,
    "genImageType": "02",
    "defoInterval": 30,
    "scatInterval": 60,
    "defoNumber": 10,
    "scatNumber": 10
  }
}
```

---

### 接口2: 获取图像生成配置

**端点**: `GET /api/protocol/query/project/imageAnalysisConfig/{projectId}`

**返回**:
```json
{
  "code": 200,
  "data": {
    "projectId": "PRJ001",
    "genDefo": true,
    "genScat": true,
    "genSpeed": false,
    "genAcceleration": false,
    "genImageType": "01",
    "defoInterval": 60,
    "scatInterval": 60,
    "defoNumber": 10,
    "scatNumber": 10,
    "createTime": "2025-10-25T10:00:00",
    "updateTime": "2025-10-25T12:00:00"
  }
}
```

---

## 📝 修改文件清单

### 前端文件

| 文件 | 修改内容 |
|-----|---------|
| `ImageGenerate.vue` | 1. 添加项目选择<br>2. 添加`onProjectChange`方法<br>3. 添加`loadImageConfig`方法<br>4. 优化`commitUpdate`方法<br>5. 添加错误处理和日志 |
| `apiRadar.js` | 前端接口已存在，无需修改 |

### 后端文件

| 文件 | 修改内容 |
|-----|---------|
| `ConfigEntities.cs` | `ImageAnalysisConfigEntity`添加独立字段 |
| `Program.cs` | 添加`image_analysis_configs`表创建和字段迁移逻辑 |
| `ProtocolController.cs` | `UpdateImageAnalysisConfig`和`GetImageAnalysisConfig`使用独立字段 |

---

## ✅ 优化对比

### 修改前

**问题**:
- ❌ 使用`ConfigJson`存储参数
- ❌ 没有项目选择
- ❌ 没有加载配置逻辑
- ❌ 错误提示不明确

**数据存储**:
```json
{
  "configJson": "{\"genImageType\":\"01\",\"defoInterval\":60,...}"
}
```

### 修改后

**优势**:
- ✅ 使用独立字段存储参数
- ✅ 添加项目选择下拉框
- ✅ 页面加载时自动加载配置
- ✅ 项目切换时自动加载配置
- ✅ 完善的错误处理
- ✅ 详细的调试日志

**数据存储**:
```sql
gen_image_type = '01'
defo_interval = 60
scat_interval = 60
defo_number = 10
scat_number = 10
```

---

## 🎨 页面布局

```
┌──────────────────────────────────────────┐
│  雷达图像生成                             │
├──────────────────────────────────────────┤
│ [保存配置]                                │
├──────────────────────────────────────────┤
│ 项目: [项目1 ▼]                           │
│                                           │
│ ══════ 雷达图像生成配置 ══════            │
│                                           │
│ 形变图像生成配置                          │
│                                           │
│ 生成类型: [按帧号提取 ▼]                  │
│           • 按帧号提取                    │
│           • 按时间间隔                    │
│                                           │
│ ┌─ 按帧号生成 ─────────────┐             │
│ │ 转几圈生成1次图像                │      │
│ │                                  │      │
│ │ 形变图像生成: [10圈 ▼]           │      │
│ │   • 不生成                       │      │
│ │   • 1圈 / 2圈 / 5圈 / 10圈 / 20圈│      │
│ │                                  │      │
│ │ 散射图像生成: [10圈 ▼]           │      │
│ │   • 不生成                       │      │
│ │   • 1圈 / 2圈 / 5圈 / 10圈 / 20圈│      │
│ └──────────────────────────┘             │
│                                           │
│ ┌─ 按时间间隔生成 ─────────┐             │
│ │ 以下单位为"分钟"                │       │
│ │                                  │       │
│ │ 形变图像生成: [60]分钟           │       │
│ │ 散射图像生成: [60]分钟           │       │
│ └──────────────────────────┘             │
└──────────────────────────────────────────┘
```

---

## 🔄 使用场景

### 场景1: 快速变化监测（按帧号）

**需求**: 监测快速变化的边坡，需要高频率生成图像

**配置**:
```
生成类型: 按帧号提取
形变图生成: 1圈
散射图生成: 2圈
```

**效果**:
- 雷达每转1圈生成一次形变图
- 雷达每转2圈生成一次散射图
- 生成频率高，数据量大

---

### 场景2: 常规监测（按帧号）

**需求**: 常规边坡监测

**配置**:
```
生成类型: 按帧号提取
形变图生成: 10圈
散射图生成: 10圈
```

**效果**:
- 雷达每转10圈生成一次图像
- 平衡数据质量和存储空间
- **推荐配置**

---

### 场景3: 定时监测（按时间）

**需求**: 每小时生成一次监测报告

**配置**:
```
生成类型: 按时间间隔
形变图生成: 60分钟
散射图生成: 60分钟
```

**效果**:
- 每60分钟生成一次形变图
- 每60分钟生成一次散射图
- 时间固定，便于报表统计

---

### 场景4: 低频监测（按时间）

**需求**: 变化缓慢的区域，节省存储

**配置**:
```
生成类型: 按时间间隔
形变图生成: 120分钟
散射图生成: 240分钟
```

**效果**:
- 每2小时生成一次形变图
- 每4小时生成一次散射图
- 数据量小，适合长期监测

---

## 📊 字段说明

### genImageType (生成类型)

| 值 | 说明 | 显示配置 |
|----|------|---------|
| `01` | 按帧号提取 | 形变图帧数 + 散射图帧数 |
| `02` | 按时间间隔 | 形变图间隔 + 散射图间隔 |

### defoNumber / scatNumber (帧数)

**genImageType='01'时有效**

| 值 | 说明 | 生成频率 |
|----|------|---------|
| `0` | 不生成 | 关闭 |
| `1` | 1圈 | 最高频 |
| `2` | 2圈 | 高频 |
| `5` | 5圈 | 中频 |
| `10` | 10圈 | 常规（推荐） |
| `20` | 20圈 | 低频 |

### defoInterval / scatInterval (间隔)

**genImageType='02'时有效**

| 值 | 说明 |
|----|------|
| `30` | 30分钟 |
| `60` | 1小时（推荐） |
| `120` | 2小时 |
| `240` | 4小时 |

---

## 🎯 关键改进

### 1. 数据存储优化

**修改前**:
```json
config_json: "{\"genImageType\":\"01\",\"defoInterval\":60,\"scatInterval\":60}"
```

**修改后**:
```sql
gen_image_type = '01'
defo_interval = 60
scat_interval = 60
defo_number = 10
scat_number = 10
```

**优势**:
- ✅ 每个字段可独立查询
- ✅ 更新单个字段无需反序列化
- ✅ 数据类型明确
- ✅ 可使用数据库约束

### 2. 页面功能增强

- ✅ 添加项目选择
- ✅ 自动加载配置
- ✅ 项目切换时重新加载
- ✅ 完善的错误处理
- ✅ 详细的调试日志

### 3. 用户体验提升

- ✅ 清晰的分段提示
- ✅ 根据生成类型动态显示配置
- ✅ 明确的保存反馈

---

## 🚀 测试步骤

### 测试1: 页面加载

1. 打开"雷达图像生成"页面
2. **预期**: 显示当前项目的配置
3. 查看控制台日志
4. **预期**: 
   ```
   ImageGenerate.onMounted
   ImageGenerate: 加载图像配置
   图像配置已加载: { genImageType: "01", ... }
   ```

### 测试2: 切换生成方式

1. 切换: "按帧号" → "按时间间隔"
2. **预期**: 
   - 隐藏帧数选择
   - 显示时间间隔输入框

3. 切换: "按时间间隔" → "按帧号"
4. **预期**:
   - 隐藏时间间隔
   - 显示帧数选择

### 测试3: 保存配置

1. 修改配置
2. 点击"保存配置"
3. **预期**: 提示"图像生成配置保存成功"
4. 刷新页面
5. **预期**: 配置保持

### 测试4: 切换项目

1. 切换项目下拉框
2. **预期**: 自动加载新项目的配置
3. 不同项目应该有不同的配置

---

## ✅ 总结

### 完成的工作

1. ✅ 数据库表添加独立字段
2. ✅ 数据库迁移逻辑更新
3. ✅ 后端接口使用独立字段
4. ✅ 前端页面添加项目选择
5. ✅ 前端添加配置加载逻辑
6. ✅ 前端添加错误处理
7. ✅ 编译成功

### 数据流

- ✅ 页面加载 → 从数据库加载配置
- ✅ 项目切换 → 重新加载配置
- ✅ 保存配置 → 更新数据库独立字段
- ✅ 刷新页面 → 配置保持

---

**实现完成！编译成功，刷新前端页面测试！** 🎉

