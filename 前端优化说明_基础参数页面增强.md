# 前端优化说明 - 基础参数页面增强

## 优化概述

本次优化主要对基础参数页面（RadarParams.vue）进行了全面增强，增加了项目选择功能，优化了设备信息显示，确保所有必要字段都正确显示并使用独立的数据库字段。

## 主要改进

### 1. 新增项目选择器
- 在设备选择器之前添加了项目选择下拉框
- 默认选中第一个项目
- 切换项目时自动加载该项目下的设备列表
- 自动选择第一个设备并加载其详细信息

### 2. 优化设备信息显示
使用独立的响应式对象 `currentDevice` 存储设备信息，替代原来从 `store.radarInfo.params` 中读取：

```javascript
const currentDevice = reactive({
  factoryId: '',      // 出厂ID
  longitude: 0,       // 经度
  latitude: 0,        // 纬度
  elevation: 0,       // 高程
  orientation: 0      // 零点朝向
});
```

### 3. 字段独立化
将以下字段从 `params` 对象中提取为独立字段：
- **出厂ID** (factoryId) - 原来存储在 `params['slaveId']`
- **经度** (longitude) - 原来存储在 `coordinates[0]`
- **纬度** (latitude) - 原来存储在 `coordinates[1]`
- **高程** (elevation) - 原来存储在 `coordinates[2]`
- **零点朝向** (orientation) - 原来存储在 `params['radarOri']`

### 4. 表单字段完整性
确保所有必要字段都在表单中正确显示：

| 字段名称 | 字段类型 | 默认值 | 说明 |
|---------|---------|-------|------|
| 项目选择 | 下拉框 | 第一个项目 | 新增 |
| 设备选择 | 下拉框 | 第一个设备 | 优化 |
| 设备ID | 文本框（只读） | - | 自动填充 |
| 设备名称 | 文本框 | - | 可编辑 |
| 出厂ID | 文本框 | '' | 新增独立字段 |
| 经度 | 数字输入框 | 120.6 | 精度6位小数 |
| 纬度 | 数字输入框 | 31.3 | 精度6位小数 |
| 高程(m) | 数字输入框 | 100 | 精度2位小数 |
| 零点朝向(度) | 数字输入框 | 0 | 0-360度，精度2位小数 |
| 起始角度 | 数字输入框 | 0 | 0-360度 |
| 终止角度 | 数字输入框 | 360 | 0-360度 |
| 最近距离 | 数字输入框 | 0 | 最小值0 |
| 最远距离 | 数字输入框 | 1000 | 最小值0 |
| 频段选择 | 下拉框 | 0 | 0/1/2 |
| 天线波束角 | 下拉框 | 60° | 30°/60°/90° (圆弧雷达) |
| 刷新时间 | 文本框 | - | MIMO雷达专用 |
| 监测模式 | 下拉框 | - | MIMO雷达专用 |

## 详细修改清单

### 1. 模板部分 (template)

#### 1.1 新增项目选择器
```vue
<!-- ✅ 新增：项目选择 -->
<el-form-item :label="$t('common.project')">
  <el-select v-model="store.projectInfo.projectSelected" @change="onProjectChange">
    <el-option v-for="item in store.projectInfo.projectData" :key="item.projectId" 
      :label="item.projectName" :value="item.projectId" />
  </el-select>
</el-form-item>
```

#### 1.2 优化设备选择器
- 设备列表来源从 `store.projectInfo.deviceData` 改为 `currentProjectDevices` 计算属性
- 只显示当前项目下的设备

#### 1.3 独立字段显示
```vue
<!-- 出厂ID -->
<el-form-item :label="$t('backend.factoryId')">
  <el-input v-model="currentDevice.factoryId" placeholder="设备出厂ID"/>
</el-form-item>

<!-- 经度 -->
<el-form-item :label="$t('common.longitude')">
  <el-input-number v-model="currentDevice.longitude" :precision="6" :step="0.000001" style="width: 100%" placeholder="120.6"/>
</el-form-item>

<!-- 纬度 -->
<el-form-item :label="$t('common.latitude')">
  <el-input-number v-model="currentDevice.latitude" :precision="6" :step="0.000001" style="width: 100%" placeholder="31.3"/>
</el-form-item>

<!-- 高程 -->
<el-form-item :label="$t('common.altitude')">
  <el-input-number v-model="currentDevice.elevation" :precision="2" :step="0.1" style="width: 100%" placeholder="100"/>
</el-form-item>

<!-- 零点朝向 -->
<el-form-item :label="$t('backend.radarOriAngle')">
  <el-input-number v-model="currentDevice.orientation" :min="0" :max="360" :precision="2" placeholder="零点朝向(度)"/>
</el-form-item>
```

#### 1.4 添加默认值和范围限制
```vue
<!-- 起始角度 -->
<el-input-number v-model="store.radarInfo.params['ImgAngleStart']" :min="0" :max="360" placeholder="0"/>

<!-- 终止角度 -->
<el-input-number v-model="store.radarInfo.params['ImgAngleEnd']" :min="0" :max="360" placeholder="360"/>

<!-- 最近距离 -->
<el-input-number v-model="store.radarInfo.params['RngMin']" :min="0" placeholder="0"/>

<!-- 最远距离 -->
<el-input-number v-model="store.radarInfo.params['RngMax']" :min="0" placeholder="1000"/>
```

### 2. 脚本部分 (script)

#### 2.1 新增响应式数据
```javascript
// 当前设备信息（用于表单绑定）
const currentDevice = reactive({
  factoryId: '',
  longitude: 0,
  latitude: 0,
  elevation: 0,
  orientation: 0
});

// 当前项目的设备列表（计算属性）
const currentProjectDevices = computed(() => {
  const currentProject = store.projectInfo.projectData.find(
    p => p.projectId === store.projectInfo.projectSelected
  );
  return currentProject?.devices || [];
});
```

#### 2.2 新增项目切换事件
```javascript
const onProjectChange = () => {
  // 切换项目后，重新绑定数据
  staticDataBind();
  // 选择第一个设备
  if (currentProjectDevices.value.length > 0) {
    store.radarInfo.deviceId = currentProjectDevices.value[0].id;
    selectOnChange(store.radarInfo.deviceId);
  } else {
    // 没有设备，清空表单
    store.radarInfo.deviceId = null;
    store.radarInfo.deviceName = '';
    currentDevice.factoryId = '';
    currentDevice.longitude = 0;
    currentDevice.latitude = 0;
    currentDevice.elevation = 0;
    currentDevice.orientation = 0;
  }
};
```

#### 2.3 优化设备切换事件
```javascript
const selectOnChange = (e) => {
  const device = currentProjectDevices.value.find(d => d.id === e);
  if (!device) return;
  
  store.radarInfo.deviceName = device.name;
  store.radarInfo.coordinates = device.coordinates || [0, 0, 0];
  store.radarInfo.params = device.params || {};
  
  // ✅ 加载设备的独立字段到表单
  currentDevice.factoryId = device.factoryId || '';
  currentDevice.longitude = device.longitude || device.coordinates?.[0] || 0;
  currentDevice.latitude = device.latitude || device.coordinates?.[1] || 0;
  currentDevice.elevation = device.elevation || device.coordinates?.[2] || 0;
  currentDevice.orientation = device.orientation || store.radarInfo.params['radarOri'] || 0;
  
  // ✅ 设置默认值（如果字段为空或0）
  if (!store.radarInfo.params['ImgAngleStart']) store.radarInfo.params['ImgAngleStart'] = 0;
  if (!store.radarInfo.params['ImgAngleEnd']) store.radarInfo.params['ImgAngleEnd'] = 360;
  if (!store.radarInfo.params['RngMin']) store.radarInfo.params['RngMin'] = 0;
  if (!store.radarInfo.params['RngMax']) store.radarInfo.params['RngMax'] = 1000;
  if (!store.radarInfo.params['FreqBand']) store.radarInfo.params['FreqBand'] = '0';
  if (!store.radarInfo.params['AnteBeam_half']) store.radarInfo.params['AnteBeam_half'] = 60;
  
  // ... 其他逻辑
};
```

#### 2.4 更新坐标选择功能
```javascript
const getCoordinate = () => {
  CesiumUtils.DrawPoint('请在地图上选择一个点并鼠标左击确认').then((result) => {
    // ✅ 更新独立坐标字段
    currentDevice.longitude = result[0][0];
    currentDevice.latitude = result[0][1];
    currentDevice.elevation = result[0][2];
    store.radarInfo.coordinates = result[0];
    // ... 其他逻辑
  })
}
```

#### 2.5 优化参数提交
```javascript
const commitUpdate = () => {
  const params = store.radarInfo.params;
  if (currentDevice.orientation < 0 || params['RngMax'] < 0 || params['RngMin'] < 0 || params['ImgAngleStart'] < 0 || params['ImgAngleEnd'] < 0) {
    showMessage('探测角度和距离不可小于0', 'error');
    return;
  }
  
  // ✅ 同步独立字段到params
  params['radarOri'] = currentDevice.orientation;
  params['longitude'] = currentDevice.longitude;
  params['latitude'] = currentDevice.latitude;
  params['height'] = currentDevice.elevation;
  params['factoryId'] = currentDevice.factoryId;
  params['orientation'] = currentDevice.orientation;
  
  // ... 其他参数设置和API调用
};
```

#### 2.6 优化页面初始化
```javascript
onMounted(() => {
  // ✅ 如果当前没有选中项目，默认选择第一个项目
  if (!store.projectInfo.projectSelected && store.projectInfo.projectData.length > 0) {
    store.projectInfo.projectSelected = store.projectInfo.projectData[0].projectId;
    staticDataBind();
  }
  
  // ✅ 如果当前没有选中设备，默认选择第一个设备
  if (!store.radarInfo.deviceId && currentProjectDevices.value.length > 0) {
    store.radarInfo.deviceId = currentProjectDevices.value[0].id;
  }
  
  if (store.radarInfo.deviceId) {
    currentRadar.value = store.radarInfo.deviceId.substring(0, 8);
    selectOnChange(store.radarInfo.deviceId);
  }
});
```

#### 2.7 更新相关函数
所有引用 `store.radarInfo.params['radarOri']` 和 `store.radarInfo.coordinates` 的地方都改为使用 `currentDevice.orientation` 和独立坐标字段：

- `computeOriAngle()` - 计算零点朝向
- `computeAngle()` - 计算角度
- `computeRealZero()` - 计算真实零点
- `drawLine()` - 绘制线条
- `inputOnChange()` - 输入变化处理

## 页面展示效果

```
┌─────────────────────────────────────┐
│ 基础参数                              │
├─────────────────────────────────────┤
│ [参数设置]                            │
├─────────────────────────────────────┤
│ 项目: [苏州边坡监测项目 ▼]            │
│                                       │
│ 设备: [边坡雷达1号 ▼] [+] [-]         │
│ 设备ID: ARC_20241024_001 (只读)      │
│ 设备名称: [边坡雷达1号]               │
│ 出厂ID: [12345]                      │
│                                       │
│ 经度: [120.612345] [📍]             │
│ 纬度: [31.345678]                    │
│ 高程: [100.50]                       │
│                                       │
│ 边界显示: [开关]                      │
│ 零点朝向: [45.50°] [📐]             │
│                                       │
│ 起始角度: [0]                         │
│ 终止角度: [360]                       │
│ 最近距离: [0]                         │
│ 最远距离: [1000]                      │
│                                       │
│ 频段选择: [0 ▼]                       │
│ 天线波束角: [60° ▼]                  │
│                                       │
│ [刷新时间 (MIMO专用)]                 │
│ [监测模式 (MIMO专用)]                 │
└─────────────────────────────────────┘
```

## 数据流说明

### 页面加载流程
```
onMounted
  ↓
检查项目选择
  ↓
没有选中 → 选择第一个项目 → staticDataBind()
  ↓
检查设备选择
  ↓
没有选中 → 选择第一个设备
  ↓
selectOnChange() → 加载设备详细信息
  ↓
填充表单（使用currentDevice和params）
```

### 项目切换流程
```
用户选择项目
  ↓
onProjectChange()
  ↓
staticDataBind() → 重新加载项目数据
  ↓
获取当前项目的设备列表（currentProjectDevices）
  ↓
选择第一个设备
  ↓
selectOnChange() → 加载设备信息
```

### 参数提交流程
```
用户点击[参数设置]
  ↓
commitUpdate()
  ↓
验证输入
  ↓
同步currentDevice到params
  ↓
根据雷达类型调用对应API
  ↓
成功 → 重新加载数据 → staticDataBind()
```

## 默认值设置规则

| 字段 | 新建设备默认值 | 说明 |
|-----|--------------|------|
| 经度 | 120.6 | 苏州坐标 |
| 纬度 | 31.3 | 苏州坐标 |
| 高程 | 100 | 米 |
| 零点朝向 | 0 | 度 |
| 起始角度 | 0 | 度 |
| 终止角度 | 360 | 度 |
| 最近距离 | 0 | 米 |
| 最远距离 | 1000 | 米 |
| 频段选择 | 0 | - |
| 天线波束角 | 60 | 度 |

## 向后兼容性

### 数据同步机制
为确保与后端和其他模块的兼容性，采用双向同步机制：

1. **加载时**：从后端数据（device对象）加载到 `currentDevice`
2. **提交时**：从 `currentDevice` 同步到 `params` 对象，再提交到后端

```javascript
// 加载时
currentDevice.factoryId = device.factoryId || '';
currentDevice.longitude = device.longitude || device.coordinates?.[0] || 0;
currentDevice.orientation = device.orientation || store.radarInfo.params['radarOri'] || 0;

// 提交时
params['factoryId'] = currentDevice.factoryId;
params['longitude'] = currentDevice.longitude;
params['orientation'] = currentDevice.orientation;
```

### 字段映射表

| currentDevice字段 | params字段 | 后端字段 | 说明 |
|------------------|-----------|---------|------|
| factoryId | factoryId | factoryId | 新增独立字段 |
| longitude | longitude | longitude | 新增独立字段 |
| latitude | latitude | latitude | 新增独立字段 |
| elevation | height | elevation | 新增独立字段 |
| orientation | radarOri / orientation | orientation | 新增独立字段 |

## 测试建议

### 功能测试
- [ ] 打开基础参数页面，检查是否自动选择第一个项目
- [ ] 检查是否自动加载第一个设备的信息
- [ ] 切换项目，检查设备列表是否正确更新
- [ ] 切换设备，检查所有字段是否正确加载
- [ ] 修改各个字段，检查是否能正常保存
- [ ] 在地图上选点，检查坐标是否正确更新
- [ ] 检查默认值是否正确应用

### 边界测试
- [ ] 项目列表为空时的处理
- [ ] 设备列表为空时的处理
- [ ] 字段值为空或null时的处理
- [ ] 超出范围的数值输入

### 兼容性测试
- [ ] 圆弧雷达（ER）的参数设置
- [ ] MIMO雷达的参数设置
- [ ] 旧数据的加载和显示
- [ ] 新数据的保存和读取

## 注意事项

1. **数据一致性**：`currentDevice` 和 `store.radarInfo.params` 需要保持同步
2. **默认值应用**：只在字段为空或0时应用默认值，避免覆盖已有数据
3. **坐标精度**：经纬度6位小数（约0.11米），高程2位小数（0.01米）
4. **角度范围**：零点朝向、起始角度、终止角度都限制在0-360度
5. **MIMO特殊字段**：只在雷达类型为MIMOLITE时显示刷新时间和监测模式

## 相关文件

- `RadarContrl/src/components/ToolBar/Tool/RadarParams/RadarParams.vue`
- `RadarContrl/src/utils/radartool.js`
- `RadarContrl/src/axios/apiRadar.js`

## 更新日期

2024-10-24

