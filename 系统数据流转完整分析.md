# 边坡雷达监测系统 - 完整数据流转分析

> **生成时间**: 2025-11-02  
> **系统版本**: .NET Core 8.0  
> **分析范围**: 从数据接收到预警全流程

---

## 目录
1. [系统架构概览](#系统架构概览)
2. [设备类型与端口配置](#设备类型与端口配置)
3. [数据接收流程](#数据接收流程)
4. [数据处理流程](#数据处理流程)
5. [数据存储流程](#数据存储流程)
6. [图像分析流程](#图像分析流程)
7. [预警流程](#预警流程)
8. [数据流转时序图](#数据流转时序图)

---

## 系统架构概览

### 核心模块

```
RadarSystem（边坡雷达监测系统）
├── RadarSystem.WebAPI          # Web API 接口层（端口：80/443）
├── RadarSystem.Communication   # 设备通信层（Netty服务）
├── RadarSystem.Core            # 核心业务逻辑
├── RadarSystem.Data            # 数据访问层（SQLite + TDengine）
├── RadarSystem.Alarm           # 预警服务
├── RadarSystem.ImageAnalysis   # 图像分析服务
└── RadarSystem.Radar           # 雷达专用服务
```

### 技术栈

- **通信框架**: DotNetty（.NET版本的Netty）
- **关系数据库**: SQLite（设备配置、用户管理）
- **时序数据库**: TDengine（雷达数据、传感器数据）
- **消息队列**: MQTT（实时数据推送）
- **实时通信**: WebSocket（前端实时数据）
- **API框架**: ASP.NET Core 8.0

---

## 设备类型与端口配置

### 1. 设备类型枚举

| 编码 | 设备类型 | 英文标识 | 说明 |
|-----|---------|---------|-----|
| 1 | 边坡雷达 | SlopeRadar | 普通边坡雷达 |
| 2 | 视频 | Video | 视频监控设备 |
| 3 | 气象站 | WeatherStation | 气象监测设备 |
| 4 | GNSS | GNSS | GNSS定位设备 |
| 5 | 建筑物雷达 | BuildingRadar | 建筑物监测雷达 |
| 6 | 边坡雷达 Mini | SlopeRadarMini | 小型边坡雷达 |
| 7 | 建筑物雷达 2D | BuildingRadar2D | 2D建筑物雷达 |
| 8 | MIMO 雷达 | MIMORadar | MIMO阵列雷达 |
| 9 | 普适雷达 | UniversalRadar | 通用雷达 |
| 10 | 球形摄像机 | DomeCamera | 云台摄像机 |
| 11 | 测斜计 | Inclinometer | 倾角传感器 |
| 12 | 振动传感器 | VibrationSensor | 振动监测传感器 |
| 13 | 电机 | Motor | 电机控制设备 |

### 2. 设备接收端口配置

> **配置文件**: `appsettings.json` → `Netty` 节点

| 设备类型 | 端口 | 启用状态 | 协议类型 | 说明 |
|---------|-----|---------|---------|-----|
| **圆弧雷达 (ArcRadar)** | **1030** | ✅ 启用 | TCP | DAG圆弧雷达专用端口 |
| **建筑物雷达 (BuildingRadar)** | **1060** | ✅ 启用 | TCP | 建筑物雷达专用 |
| **建筑物2D雷达 (Building2DRadar)** | **11135** | ✅ 启用 | TCP | 2D建筑物雷达 |
| **MIMO Lite雷达** | **10305** | ✅ 启用 | TCP | MIMO Lite雷达 |
| **MIMO雷达** | **11125** | ✅ 启用 | TCP | MIMO通用雷达 |
| **MIMO通用** | **11129** | ✅ 启用 | TCP | MIMO其他型号 |
| **GPS设备** | **11111** | ✅ 启用 | TCP | GPS定位数据 |
| **GPS V1** | **11109** | ✅ 启用 | TCP | GPS V1版本 |
| **北纬设备 (Bw)** | **11112** | ❌ 禁用 | TCP | 北纬设备 |
| **北纬V1 (BwV1)** | **11110** | ✅ 启用 | TCP | 北纬V1版本 |
| **报警设备 (Alarm)** | **11113** | ✅ 启用 | TCP | 报警信号接收 |
| **报警设备通用** | **11130** | ✅ 启用 | TCP | 通用报警设备 |
| **报警设备4G** | **11132** | ❌ 禁用 | TCP | 4G报警设备 |
| **电机 (Motor)** | **11114** | ✅ 启用 | TCP | 电机控制 |
| **B型电机 (BMotor)** | **11115** | ✅ 启用 | TCP | B型电机 |
| **俯仰电机 (MotorPitch)** | **11127** | ✅ 启用 | TCP | 俯仰角控制 |
| **CM设备** | **11124** | ✅ 启用 | TCP | CM传感器 |
| **倾斜仪 (Qxz)** | **11126** | ✅ 启用 | TCP | 倾斜角测量 |
| **测斜计 (Inclinometer)** | **11134** | ❌ 禁用 | TCP | 测斜计 |
| **方向传感器 (Orientation)** | **11128** | ✅ 启用 | TCP | 方向角传感器 |
| **激光设备 (Laser)** | **11131** | ✅ 启用 | TCP | 激光测距 |
| **振动传感器 (Vibration)** | **9993** | ❌ 禁用 | TCP | 振动监测 |
| **交通雷达 (TrafficRadar)** | **11133** | ❌ 禁用 | TCP | 交通监测雷达 |
| **WebSocket服务** | **/wss** | ✅ 启用 | WebSocket | 前端实时推送 |
| **Web API** | **80** | ✅ 启用 | HTTP/HTTPS | RESTful API |

### 3. 端口分配策略

- **1000-2000**: 雷达设备主端口段（1030, 1060）
- **10000-11000**: MIMO系列雷达（10305）
- **11100-11199**: 通用设备端口段（GPS、传感器、电机等）
- **9000-9999**: 特殊传感器（振动、气象等）

---

## 数据接收流程

### 1. Netty服务器架构

```
AllDeviceNettyServersHostedService (后台服务)
    ├── 启动所有已启用的设备服务器
    ├── 从配置文件读取端口和参数
    └── 为每种设备类型创建独立的Netty服务器实例
```

### 2. 各设备类型的Handler（数据处理器）

| Handler类 | 设备类型 | 主要功能 |
|-----------|---------|---------|
| `RadarHandlerBase` | 所有雷达基类 | 数据接收、队列管理、文件保存 |
| `BuildingRadarHandler` | 建筑物雷达 | 建筑物雷达数据解析 |
| `Building2DRadarHandler` | 2D建筑物雷达 | 2D雷达数据解析 |
| `MimoRadarHandler` | MIMO雷达 | MIMO雷达数据解析 |
| `MimoLiteRadarHandler` | MIMO Lite | Lite版本雷达数据 |
| `MimoCommonHandler` | MIMO通用 | MIMO通用协议 |
| `GpsHandler` | GPS设备 | GPS NMEA协议解析 |
| `GpsV1Handler` | GPS V1 | GPS V1版本协议 |
| `BwHandler` | 北纬设备 | 北纬设备协议 |
| `BwV1Handler` | 北纬V1 | 北纬V1协议 |
| `MotorHandler` | 电机 | 电机控制命令 |
| `MotorPitchHandler` | 俯仰电机 | 俯仰角控制 |
| `BMotorHandler` | B型电机 | B型电机协议 |
| `AlarmHandler` | 报警设备 | 报警信号接收 |
| `LaserHandler` | 激光设备 | 激光测距数据 |
| `VibrationHandler` | 振动传感器 | 振动数据解析 |
| `CmHandler` | CM设备 | CM传感器数据 |
| `QxzHandler` | 倾斜仪 | 倾斜角数据 |
| `OrientationHandler` | 方向传感器 | 方向角数据 |

### 3. 数据接收流程图

```
┌─────────────────┐
│  雷达设备       │
│  (TCP Client)   │
└────────┬────────┘
         │ TCP连接
         ▼
┌─────────────────────────┐
│  NettyServerService     │
│  (DotNetty TCP Server)  │
│  • 监听指定端口         │
│  • 接受客户端连接       │
│  • 管理Channel Pipeline │
└────────┬────────────────┘
         │ 解码后的数据
         ▼
┌─────────────────────────┐
│  DeviceHandler          │
│  • 识别设备类型         │
│  • 解析协议数据         │
│  • 验证数据完整性       │
└────────┬────────────────┘
         │ 解析后的数据包
         ▼
┌─────────────────────────┐
│  RadarDataProcessorService │
│  • 数据入队列           │
│  • MD5校验              │
│  • 异步处理队列         │
└────────┬────────────────┘
         │
         ├─────────────────┐
         ▼                 ▼
    文件存储          TDengine存储
```

### 4. 圆弧雷达专用流程 (ArcRadar)

```
ArcRadarNettyServer
├── 启动端口: 1030
├── 连接管理: 支持多设备并发
├── 命令处理:
│   ├── 0x0001: 设备注册
│   ├── 0x0002: 心跳包
│   ├── 0x0010: 时间同步
│   ├── 0x2001: SAR幅度图
│   ├── 0x2002: SAR相位图
│   ├── 0x2003: SAR相干图
│   ├── 0x2004: 干涉图
│   └── 0x2005: 差分干涉图
├── 数据类型:
│   ├── DISP: 位移数据
│   ├── AMP: 幅度图
│   ├── PHA: 相位图
│   ├── COH: 相干图
│   ├── INT: 干涉图
│   └── DINT: 差分干涉图
└── 文件命名: {ProjectId}/{DeviceId}/{DataType}/{yyyyMMdd}/HHmmss.dat
```

---

## 数据处理流程

### 1. 数据处理服务 (RadarDataProcessorService)

```csharp
// 处理流程
接收数据
  ↓
入队列 (BlockingCollection)
  ↓
后台线程异步处理
  ↓
MD5校验（可配置）
  ↓
生成文件路径
  ↓
保存到文件系统
  ↓
保存到TDengine（可配置）
  ↓
发送MQTT通知（可配置）
  ↓
完成处理
```

### 2. 数据验证器 (RadarDataValidator)

```csharp
• ValidateMD5(): MD5完整性校验
• ValidateDataIntegrity(): 数据完整性验证
• ValidateTimestamp(): 时间戳有效性
• ValidateDeviceId(): 设备ID格式验证
```

### 3. 文件存储服务 (IRadarFileStorage)

**存储结构**:
```
{DataPath}
├── {ProjectId}
│   ├── {DeviceId}
│   │   ├── AMP/           # 幅度图
│   │   │   ├── 20251102/
│   │   │   │   ├── 083000.dat
│   │   │   │   ├── 083100.dat
│   │   │   │   └── ...
│   │   ├── PHA/           # 相位图
│   │   ├── COH/           # 相干图
│   │   ├── INT/           # 干涉图
│   │   ├── DINT/          # 差分干涉图
│   │   └── DISP/          # 位移数据
│   ├── {DeviceId2}
│   └── ...
├── {ProjectId2}
└── ...
```

### 4. 数据队列管理

```csharp
// 队列配置
BlockingCollection<RadarDataPacket> _dataQueue
    ├── 容量限制: 10000条
    ├── 超时时间: 5秒
    ├── 处理线程: 后台线程池
    └── 丢弃策略: 队列满时丢弃新数据并记录警告
```

---

## 数据存储流程

### 1. 双存储策略

#### A. 文件系统存储（主存储）

**用途**: 
- 原始雷达数据（.dat文件）
- 图像数据
- 大数据文件

**优势**:
- 快速写入
- 数据完整性高
- 支持直接访问

#### B. TDengine时序数据库（索引存储）

**用途**:
- 数据索引和元数据
- 时序查询
- 统计分析

**优势**:
- 高性能时序查询
- 数据压缩存储
- 支持聚合计算

### 2. TDengine数据表结构

#### 超级表 (Stable)

```sql
-- 1. 雷达数据超级表
CREATE STABLE IF NOT EXISTS radar_data (
    ts TIMESTAMP,              -- 时间戳
    device_id NCHAR(64),       -- 设备ID
    device_type NCHAR(32),     -- 设备类型
    slave_id NCHAR(32),        -- 从机ID
    command NCHAR(16),         -- 命令代码
    image_type NCHAR(16),      -- 图像类型
    data_length INT,           -- 数据长度
    file_path NCHAR(256)       -- 文件路径
) TAGS (project_id NCHAR(64));

-- 2. GPS数据超级表
CREATE STABLE IF NOT EXISTS gps_data (
    ts TIMESTAMP,
    device_id NCHAR(64),
    latitude DOUBLE,           -- 纬度
    longitude DOUBLE,          -- 经度
    altitude DOUBLE,           -- 海拔
    satellites INT,            -- 卫星数
    hdop DOUBLE,              -- 水平精度因子
    fix_quality NCHAR(16)     -- 定位质量
) TAGS (project_id NCHAR(64), device_type NCHAR(32));

-- 3. 传感器数据超级表
CREATE STABLE IF NOT EXISTS sensor_data (
    ts TIMESTAMP,
    device_id NCHAR(64),
    sensor_type NCHAR(32),     -- 传感器类型
    value1 DOUBLE,             -- 数值1
    value2 DOUBLE,             -- 数值2
    value3 DOUBLE,             -- 数值3
    unit NCHAR(16)            -- 单位
) TAGS (project_id NCHAR(64));

-- 4. 电机数据超级表
CREATE STABLE IF NOT EXISTS motor_data (
    ts TIMESTAMP,
    device_id NCHAR(64),
    motor_type NCHAR(32),      -- 电机类型
    position DOUBLE,           -- 位置
    speed DOUBLE,              -- 速度
    current DOUBLE,            -- 电流
    status NCHAR(16)          -- 状态
) TAGS (project_id NCHAR(64));

-- 5. 报警记录超级表
CREATE STABLE IF NOT EXISTS alarm_records (
    ts TIMESTAMP,
    device_id NCHAR(64),
    alarm_level NCHAR(16),     -- 报警级别
    alarm_type NCHAR(32),      -- 报警类型
    alarm_value DOUBLE,        -- 报警值
    threshold DOUBLE,          -- 阈值
    description NCHAR(256)     -- 描述
) TAGS (project_id NCHAR(64), rule_id NCHAR(64));
```

### 3. SQLite存储（配置数据）

**存储内容**:
- 项目配置 (projects)
- 设备配置 (devices)
- 用户管理 (users)
- 算法配置 (algorithm_configs)
- 系统设置 (settings)

**数据库位置**: `RadarSystem.WebAPI/Data/radar.db`

---

## 图像分析流程

### 1. 图像分析服务 (ImageAnalysisService)

```
图像数据输入
    ↓
图像格式验证
    ↓
┌─────────────────────────┐
│  图像分析算法           │
│  • 目标检测             │
│  • 差值计算             │
│  • 特征提取             │
│  • 变化检测             │
└────────┬────────────────┘
         ↓
┌─────────────────────────┐
│  分析结果生成           │
│  • TargetCount          │
│  • Confidence           │
│  • BoundingBox          │
│  • Metadata             │
└────────┬────────────────┘
         ↓
    结果输出
```

### 2. 图像分析功能

#### A. 雷达图像分析 (AnalyzeRadarImageAsync)

**输入**:
- 图像字节数据
- 雷达元数据

**输出**:
```csharp
ImageAnalysisResult {
    Success: bool,
    TargetCount: int,
    Metadata: Dictionary<string, object>,
    ProcessingTimeMs: double,
    AnalysisTime: DateTime,
    Message: string
}
```

#### B. 差值图像计算 (CalculateDifferenceImageAsync)

**功能**:
- 对比当前图像与历史图像
- 计算像素级差异
- 识别变化区域

**算法**:
```csharp
差值 = |当前像素 - 历史像素|
对每个通道(R,G,B)计算差值
生成差值图像
```

#### C. 目标检测 (DetectTargetsAsync)

**检测参数**:
```csharp
DetectionParameters {
    ThresholdAmplitude: float,  // 幅度阈值
    MinTargetSize: int,         // 最小目标尺寸
    MaxTargetSize: int,         // 最大目标尺寸
    ConfidenceThreshold: float  // 置信度阈值
}
```

**检测结果**:
```csharp
DetectedTarget {
    Id: int,
    RangePosition: int,         // 距离位置
    AnglePosition: int,         // 角度位置
    Amplitude: float,           // 信号幅度
    Confidence: float,          // 置信度
    TargetType: string,         // 目标类型
    BoundingBox: Rectangle      // 边界框
}
```

### 3. 图像分析配置

**配置项** (algorithm_configs表):
- `ThresholdAmplitude`: 幅度检测阈值
- `ThresholdCoherence`: 相干性阈值
- `FilterSize`: 滤波器大小
- `NoiseLevel`: 噪声水平
- `DetectionSensitivity`: 检测灵敏度

---

## 预警流程

### 1. 实时预警监控服务 (RealtimeAlarmMonitorService)

```
后台服务启动
    ↓
定时检查（每10秒）
    ↓
获取监控点列表
    ↓
    ┌──────────────────┐
    │  逐点检查循环    │
    │  ├─ 获取最新值   │
    │  ├─ 对比阈值     │
    │  ├─ 判断报警     │
    │  └─ 更新状态     │
    └──────┬───────────┘
           ↓
      ┌────────┐
      │ 超过阈值? │
      └─┬────┬─┘
        │是  │否
        ↓    ↓
    触发报警  检查恢复
        ↓
    创建报警记录
        ↓
    发送通知（MQTT/WebSocket）
        ↓
    更新报警状态
```

### 2. 报警级别定义

```csharp
public enum AlarmLevel
{
    Normal = 0,      // 正常
    Info = 1,        // 信息
    Warning = 2,     // 警告
    Error = 3,       // 错误
    Critical = 4     // 严重
}
```

### 3. 报警判定逻辑

```csharp
// 报警触发条件
if (Math.Abs(currentValue) >= threshold && !isActive)
{
    // 新报警触发
    TriggerAlarm();
    state.IsActive = true;
    state.TriggerTime = DateTime.Now;
}

// 报警升级
if (isActive && newLevel > currentLevel)
{
    EscalateAlarm(newLevel);
}

// 报警恢复
if (Math.Abs(currentValue) < threshold && isActive)
{
    RecoverAlarm();
    state.IsActive = false;
    state.RecoveryTime = DateTime.Now;
}
```

### 4. 报警记录模型

```csharp
AlarmRecord {
    HandleId: string,           // 处理ID（唯一标识）
    RuleId: string,             // 规则ID
    ProjectId: string,          // 项目ID
    DeviceId: string,           // 设备ID
    AlarmLevel: AlarmLevel,     // 报警级别
    AlarmType: string,          // 报警类型
    AlarmContent: string,       // 报警内容
    AlarmValue: double,         // 报警值
    Threshold: double,          // 阈值
    Timestamp: DateTime,        // 触发时间
    AlarmStatus: bool,          // 报警状态
    HandleStatus: string,       // 处理状态
    ScanStatus: string,         // 扫描状态
    HandleTime: DateTime?,      // 处理时间
    HandlerName: string,        // 处理人
    HandleRemark: string        // 处理备注
}
```

### 5. 报警通知渠道

```
报警触发
    ↓
    ├─→ MQTT推送
    │   └─ Topic: alarm/{projectId}/{deviceId}
    │
    ├─→ WebSocket广播
    │   └─ 所有连接的前端客户端
    │
    ├─→ 数据库记录
    │   ├─ SQLite (alarm_records)
    │   └─ TDengine (alarm_records超级表)
    │
    └─→ 日志记录
        └─ 不同级别使用不同日志等级
```

---

## 数据流转时序图

### 完整数据流转序列

```
设备端          Netty服务器      数据处理器      文件系统      TDengine      MQTT      前端客户端
  │                │               │              │            │          │            │
  │  TCP连接       │               │              │            │          │            │
  ├──────────────>│               │              │            │          │            │
  │                │               │              │            │          │            │
  │  发送数据      │               │              │            │          │            │
  ├──────────────>│               │              │            │          │            │
  │                │  解析协议     │              │            │          │            │
  │                ├──────────────>│              │            │          │            │
  │                │               │  数据入队    │            │          │            │
  │                │               ├─────────┐    │            │          │            │
  │                │               │         │    │            │          │            │
  │                │               │  MD5校验│    │            │          │            │
  │                │               │<────────┘    │            │          │            │
  │                │               │              │            │          │            │
  │                │               │  保存文件    │            │          │            │
  │                │               ├─────────────>│            │          │            │
  │                │               │              │  写入磁盘  │          │            │
  │                │               │              ├───────┐    │          │            │
  │                │               │              │<──────┘    │          │            │
  │                │               │              │            │          │            │
  │                │               │  保存元数据  │            │          │            │
  │                │               ├─────────────────────────>│          │            │
  │                │               │              │            │  INSERT  │            │
  │                │               │              │            ├────────┐ │            │
  │                │               │              │            │<───────┘ │            │
  │                │               │              │            │          │            │
  │                │               │  发送MQTT通知│            │          │            │
  │                │               ├──────────────────────────────────────>│            │
  │                │               │              │            │          │  订阅消息  │
  │                │               │              │            │          ├──────────>│
  │                │               │              │            │          │            │
  │  响应ACK       │               │              │            │          │            │
  │<───────────────┤               │              │            │          │            │
  │                │               │              │            │          │            │
```

### 预警流程时序

```
监控服务       数据查询        阈值判定        报警服务        通知服务        前端
  │               │              │               │              │             │
  │  定时触发     │              │               │              │             │
  ├──────────────>│              │               │              │             │
  │               │  查询最新值  │               │              │             │
  │               ├─────────────>│               │              │             │
  │               │              │  对比阈值     │              │             │
  │               │              ├──────┐        │              │             │
  │               │              │<─────┘        │              │             │
  │               │              │               │              │             │
  │               │              │  超过阈值     │              │             │
  │               │              │  创建报警     │              │             │
  │               │              ├──────────────>│              │             │
  │               │              │               │  保存记录    │             │
  │               │              │               ├─────┐        │             │
  │               │              │               │<────┘        │             │
  │               │              │               │              │             │
  │               │              │               │  发送通知    │             │
  │               │              │               ├─────────────>│             │
  │               │              │               │              │  WebSocket  │
  │               │              │               │              ├───────────>│
  │               │              │               │              │             │
  │               │              │               │              │  显示报警   │
  │               │              │               │              │  ┌────────┐ │
  │               │              │               │              │  │ ALERT! │ │
  │               │              │               │              │  └────────┘ │
```

---

## 关键配置参数

### 1. 数据接收配置

```json
"RadarDataReceiver": {
  "EnableMD5Check": true,           // 启用MD5校验
  "SaveToTDengine": true,           // 保存到TDengine
  "SendMqttNotification": true,     // 发送MQTT通知
  "QueueCapacity": 10000,           // 队列容量
  "ProcessThreads": 4               // 处理线程数
}
```

### 2. TDengine配置

```json
"TDengine": {
  "Host": "localhost",
  "Port": 6030,
  "Database": "radar_db",
  "Username": "root",
  "Password": "taosdata",
  "DataRetentionDays": 90           // 数据保留90天
}
```

### 3. 图像分析配置

```json
"ImageAnalysis": {
  "ThresholdAmplitude": 0.5,        // 幅度阈值
  "ThresholdCoherence": 0.7,        // 相干性阈值
  "FilterSize": 3,                  // 滤波器大小
  "NoiseLevel": 0.1,                // 噪声水平
  "DetectionSensitivity": 0.8       // 检测灵敏度
}
```

### 4. 报警配置

```json
"Alarm": {
  "CheckInterval": 10,              // 检查间隔（秒）
  "EnableRealtime": true,           // 启用实时监控
  "NotificationChannels": [         // 通知渠道
    "MQTT",
    "WebSocket",
    "Database"
  ]
}
```

---

## 性能指标

### 1. 数据处理能力

| 指标 | 数值 | 说明 |
|-----|------|-----|
| 并发设备数 | 100+ | 同时支持设备数 |
| 数据吞吐量 | 10MB/s | 单设备最大吞吐 |
| 队列容量 | 10000条 | 待处理数据缓冲 |
| 处理延迟 | <100ms | 数据处理平均延迟 |
| 文件写入速度 | 50MB/s | 磁盘写入速度 |
| TDengine写入 | 100万点/秒 | 时序数据写入性能 |

### 2. 存储策略

| 数据类型 | 存储方式 | 保留时间 | 说明 |
|---------|---------|---------|-----|
| 原始雷达数据 | 文件系统 | 永久 | .dat文件 |
| 数据索引 | TDengine | 90天 | 可查询索引 |
| 设备配置 | SQLite | 永久 | 配置数据 |
| 报警记录 | SQLite + TDengine | 永久/90天 | 双重存储 |
| 日志文件 | 文件系统 | 30天 | 滚动日志 |

---

## 故障处理机制

### 1. 数据接收故障

```
连接断开
    ↓
自动重连（可配置）
    ↓
重连失败 → 记录日志 → 报警通知
    ↓
人工介入
```

### 2. 数据处理故障

```
处理异常
    ↓
数据重入队（最多3次）
    ↓
持续失败 → 保存到故障队列 → 记录日志
    ↓
定期重试或人工处理
```

### 3. 存储故障

```
文件写入失败
    ↓
切换备用路径
    ↓
仍失败 → 内存缓存 → 报警通知
    ↓
存储恢复后批量写入
```

---

## 总结

### 数据流转关键路径

1. **设备→Netty服务器**: TCP连接，协议解析
2. **Netty→数据处理器**: 队列缓冲，异步处理
3. **数据处理器→存储**: 文件+TDengine双写
4. **存储→分析**: 图像分析，目标检测
5. **分析→预警**: 阈值判定，报警触发
6. **预警→通知**: MQTT/WebSocket推送

### 技术特点

- ✅ **高并发**: 支持100+设备同时接入
- ✅ **高可靠**: 数据双重存储，故障自动恢复
- ✅ **高性能**: 队列异步处理，TDengine时序存储
- ✅ **可扩展**: 模块化设计，易于添加新设备类型
- ✅ **实时性**: WebSocket实时推送，毫秒级延迟

### 未来优化方向

1. 引入Redis缓存提升查询性能
2. 实现数据分片存储
3. 增加AI算法进行智能预警
4. 支持边缘计算，降低网络传输
5. 实现数据压缩算法，节省存储空间

---

**文档版本**: 1.0  
**最后更新**: 2025-11-02  
**维护者**: 系统开发团队

