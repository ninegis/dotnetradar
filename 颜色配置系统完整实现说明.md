# 颜色配置系统完整实现说明

**实现时间**: 2025-10-25  
**页面**: 工具 → 项目配置 → 色条配置 (ColorConfig.vue)

---

## 🎯 功能概述

### 完整的颜色配置系统

支持三种图像类型的颜色配置：
1. **位移色条** - 形变图颜色映射
2. **散射色条** - 散射强度图颜色映射
3. **高程图** ⭐（新增）- 地形高程颜色映射

每种类型支持两种配色方案：
1. **线性配色** - 连续渐变
2. **分类配色** - 分级显示

---

## 🎨 核心功能

### 功能1: 线性配色方案

**原理**: 给定最小值、最大值和起始/结束色相，颜色在两者之间线性渐变

**配置参数**:
- 最小值/最大值
- 起始色相（0-360°）
- 结束色相（0-360°）

**适用场景**:
- 连续变化的数据
- 需要平滑过渡
- 如位移、散射强度、高程

**示例**:
```
位移配置:
  最小值: -100mm
  最大值: 100mm
  起始色相: 240° (蓝色)
  结束色相: 0° (红色)

效果:
  -100mm → 深蓝色
  -50mm  → 青色
  0mm    → 绿色
  50mm   → 黄色
  100mm  → 深红色
```

---

### 功能2: 分类配色方案

**原理**: 将数值范围分为N个类别，每个类别使用固定颜色

**配置参数**:
- 分类数量（2-10个）
- 每个类别的最小值、最大值
- 每个类别的颜色
- 每个类别的标签

**自动生成**:
1. 设置分类数量（如5个）
2. 点击"自动生成分类区间和颜色"
3. 系统自动:
   - 将数值范围平均分为5段
   - 从起始色相到结束色相均匀分配颜色
   - 生成默认标签

**手动调整**:
- 修改每个类别的数值范围
- 使用颜色选择器修改颜色
- 编辑类别标签
- 添加/删除类别

**适用场景**:
- 需要明确分级的数据
- 风险等级（低、中、高、很高）
- 变形等级（稳定、警戒、危险、严重）

**示例**:
```
位移分级配置（5个类别）:
  类别1: -100~-20mm → 深蓝色 → "严重负向变形"
  类别2: -20~-5mm   → 浅蓝色 → "轻微负向变形"
  类别3: -5~5mm     → 绿色   → "稳定"
  类别4: 5~20mm     → 黄色   → "轻微正向变形"
  类别5: 20~100mm   → 红色   → "严重正向变形"

效果:
  每个类别内部颜色一致
  类别之间界限分明
```

---

### 功能3: 自适应范围

**原理**: 根据实际数据的最小值和最大值自动调整色条范围

**配置参数**:
- 启用开关
- 缓冲比例（0-50%）

**工作方式**:
```
实际数据范围: -85mm ~ 92mm
缓冲比例: 10%

计算:
  数据范围 = 92 - (-85) = 177mm
  缓冲值 = 177 * 0.1 = 17.7mm
  
  自动范围:
    最小值 = -85 - 17.7 ≈ -103mm
    最大值 = 92 + 17.7 ≈ 110mm
```

**适用场景**:
- 数据范围未知
- 不同时期数据范围变化
- 需要动态调整

**优势**:
- 无需手动调整
- 自动适配数据
- 避免色条浪费

---

### 功能4: 透明通道（位移/散射专用）

**原理**: 对特定范围的数值应用透明度

**配置参数**:
- 启用开关
- 透明度最小值
- 透明度最大值
- 透明度系数（0-1）

**工作方式**:
```
filterMin = -10
filterMax = 10
filterAlpha = 0.3

数值 | 透明度
-----|--------
-100 | 1.0 (不透明)
-10  | 0.3 (半透明) ← 开始
0    | 0.3 (半透明)
10   | 0.3 (半透明) ← 结束
100  | 1.0 (不透明)
```

**适用场景**:
- 隐藏微小变形
- 过滤噪声数据
- 突出大变形区域

---

## 📊 数据库表结构

### colorbar_configs表（位移/散射）

```sql
CREATE TABLE colorbar_configs (
    id TEXT PRIMARY KEY,
    project_id TEXT NOT NULL,
    mode TEXT NOT NULL,  -- displacement / scattering
    
    -- 配色方案
    color_scheme_type INTEGER DEFAULT 0,    -- 0:线性, 1:分类 ✨
    class_count INTEGER DEFAULT 5,          -- 分类数量 ✨
    
    -- 数值范围
    min_value REAL DEFAULT -100,
    max_value REAL DEFAULT 100,
    auto_adapt_range INTEGER DEFAULT 0,     -- 自适应范围 ✨
    adapt_buffer_ratio REAL DEFAULT 0.1,    -- 缓冲比例 ✨
    
    -- HSL色相
    hsl_h_start INTEGER DEFAULT 0,
    hsl_h_end INTEGER DEFAULT 240,
    
    -- 透明通道
    filter_enable INTEGER DEFAULT 0,
    filter_alpha REAL DEFAULT 0.8,
    filter_min REAL DEFAULT -1000,
    filter_max REAL DEFAULT 1000,
    
    -- 分类配色
    custom_ranges TEXT,                     -- JSON格式 ✨
    
    create_time TEXT NOT NULL,
    update_time TEXT,
    
    FOREIGN KEY (project_id) REFERENCES Projects(ProjectId),
    UNIQUE (project_id, mode)
);
```

### terrain_color_configs表（高程图）✨ 新增

```sql
CREATE TABLE terrain_color_configs (
    id TEXT PRIMARY KEY,
    project_id TEXT NOT NULL UNIQUE,        -- 每个项目一条记录
    
    -- 配色方案
    color_scheme_type INTEGER DEFAULT 0,    -- 0:线性, 1:分类
    class_count INTEGER DEFAULT 5,          -- 分类数量
    
    -- 高程范围
    min_elevation REAL DEFAULT 0,           -- 最小高程(m)
    max_elevation REAL DEFAULT 1000,        -- 最大高程(m)
    auto_adapt_range INTEGER DEFAULT 1,     -- 自适应范围（默认开启）
    adapt_buffer_ratio REAL DEFAULT 0.1,    -- 缓冲比例
    
    -- HSL色相
    hsl_h_start INTEGER DEFAULT 120,        -- 绿色（低海拔）
    hsl_h_end INTEGER DEFAULT 0,            -- 红色（高海拔）
    hsl_s REAL DEFAULT 1.0,                 -- 饱和度
    hsl_l REAL DEFAULT 0.5,                 -- 亮度
    
    -- 分类配色
    custom_ranges TEXT,                     -- JSON格式
    
    -- 启用开关
    enable INTEGER DEFAULT 0,                -- 是否启用高程图
    
    create_time TEXT NOT NULL,
    update_time TEXT,
    
    FOREIGN KEY (project_id) REFERENCES Projects(ProjectId)
);
```

---

## 🎨 页面布局

```
┌──────────────────────────────────────────┐
│  色条配置                                 │
├──────────────────────────────────────────┤
│ [加载预设]  [保存配置]                    │
├──────────────────────────────────────────┤
│ ○ 位移色条  ○ 散射色条  ○ 高程图  ✨      │
│                                           │
│ ══════ 配色方案 ══════                    │
│                                           │
│ 配色方案类型:                             │
│ ○ 线性配色  ○ 分类配色                    │
│                                           │
│ ┌─ 线性配色说明 ───────────┐  (线性时显示)│
│ │ 给定最小值和最大值，颜色在两个色相之间线性渐变 │
│ │ 适用：连续变化的数据，如位移、散射强度 │ │
│ └───────────────────────────┘             │
│                                           │
│ 色板预设: [蓝绿渐变 ▼]                    │
│                                           │
│ ┌─ 分类配色说明 ───────────┐  (分类时显示)│
│ │ 将数值范围分为N个类别，每个类别使用不同颜色 │
│ │ 适用：需要明确分级的数据，如风险等级 │   │
│ └───────────────────────────┘             │
│                                           │
│ 分类数量: [5]  建议：3-7个类别            │
│ [自动生成分类区间和颜色]                  │
│                                           │
│ ┌─ 类别1 ──────────────────┐             │
│ │ [-100] ~ [-20] [🔵蓝色] [删除] │       │
│ │ [严重负向变形_______]          │       │
│ └──────────────────────────┘             │
│ ┌─ 类别2 ──────────────────┐             │
│ │ [-20] ~ [-5] [🔵青色] [删除]  │        │
│ │ [轻微负向变形_______]          │       │
│ └──────────────────────────┘             │
│ [+ 添加类别]                              │
│                                           │
│ 色条预览: [🟦🟦🟩🟩🟡🟡🟠🟠🔴🔴]            │
│                                           │
│ ══════ 数值范围 ══════                    │
│                                           │
│ 自适应范围: [●开启 ○关闭]  ✨            │
│ 开启后根据实际数据自动调整最小值和最大值  │
│                                           │
│ 缓冲比例: [━━●━━━━━━━] 10%  ✨           │
│           0% 10% 20% 30% 50%              │
│ 例如缓冲10%：实际数据-90~90，自动扩展为-100~100 │
│                                           │
│ 位移最小值(mm): [-100] (自适应时禁用)     │
│ 位移最大值(mm): [100]  (自适应时禁用)     │
│                                           │
│ ══════ HSL色相设置 ══════  (线性时显示)   │
│                                           │
│ 起始色相(H): [━━━━━●━━━━] 240           │
│              红 黄 绿 青 蓝 紫 红          │
│              [240]                        │
│                                           │
│ 结束色相(H): [●━━━━━━━━━] 0             │
│              [0]                          │
│                                           │
│ ══════ 透明通道设置 ══════  (位移/散射)   │
│                                           │
│ 启用透明通道: [○开启 ●关闭]              │
│ (展开时显示详细设置)                      │
└──────────────────────────────────────────┘
```

---

## 📋 配色方案对比

### 线性配色 vs 分类配色

| 特性 | 线性配色 | 分类配色 |
|-----|---------|---------|
| 颜色变化 | 连续渐变 | 分段固定 |
| 数值过渡 | 平滑过渡 | 明确界限 |
| 视觉效果 | 柔和 | 鲜明 |
| 适用场景 | 连续变化数据 | 分级数据 |
| 配置复杂度 | 简单 | 中等 |

### 配色方案选择建议

| 数据类型 | 推荐方案 | 原因 |
|---------|---------|------|
| 位移（-100~100mm） | 线性配色 | 连续变化，平滑过渡 |
| 散射强度 | 线性配色 | 强度连续变化 |
| 高程（0~1000m） | 线性配色或分类 | 线性：地形平滑<br>分类：地貌分区 |
| 风险等级 | 分类配色 | 明确的等级划分 |
| 变形速率 | 分类配色 | 突出不同速率等级 |

---

## 🔄 完整使用流程

### 流程1: 线性配色 - 位移色条

```
选择"位移色条"
  ↓
选择"线性配色"
  ↓
选择预设"冷暖色调"
  ↓
自动应用:
  最小值: -100mm
  最大值: 100mm
  起始色相: 240° (蓝色)
  结束色相: 0° (红色)
  ↓
微调参数:
  启用自适应范围: 开启
  缓冲比例: 10%
  ↓
启用透明通道:
  透明度范围: -5mm ~ 5mm
  透明度系数: 0.3
  ↓
实时预览色条
  ↓
点击"保存配置"
  ↓
保存到数据库:
  INSERT/UPDATE colorbar_configs
  WHERE project_id='PRJ001' AND mode='displacement'
```

---

### 流程2: 分类配色 - 风险等级

```
选择"位移色条"
  ↓
选择"分类配色"
  ↓
设置分类数量: 5
  ↓
点击"自动生成分类区间和颜色"
  ↓
系统自动生成:
  类别1: -100~-40mm → #0000FF (蓝)
  类别2: -40~-20mm  → #00FFFF (青)
  类别3: -20~20mm   → #00FF00 (绿)
  类别4: 20~60mm    → #FFFF00 (黄)
  类别5: 60~100mm   → #FF0000 (红)
  ↓
手动调整:
  修改类别3的范围: -10~10mm
  修改标签: "稳定" → "安全"
  调整颜色: 更亮的绿色
  ↓
点击"保存配置"
  ↓
保存customRanges:
  [
    {"min":-100,"max":-40,"color":"#0000FF","label":"严重"},
    {"min":-40,"max":-20,"color":"#00FFFF","label":"警戒"},
    {"min":-20,"max":20,"color":"#00FF00","label":"安全"},
    {"min":20,"max":60,"color":"#FFFF00","label":"警戒"},
    {"min":60,"max":100,"color":"#FF0000","label":"严重"}
  ]
```

---

### 流程3: 高程图配色

```
选择"高程图"
  ↓
选择"线性配色"
  ↓
设置:
  最小高程: 0m
  最大高程: 1000m
  起始色相: 120° (绿色 - 平原)
  结束色相: 0° (红色 - 高山)
  ↓
启用自适应: 开启
  缓冲比例: 10%
  ↓
效果:
  系统自动根据项目实际高程范围调整
  假设项目高程: 100m~850m
  自动范围: 25m~925m (扩展10%)
  ↓
保存到 terrain_color_configs表
```

---

## 🚀 后端接口

### 接口1: 更新位移/散射色条

**端点**: `POST /api/protocol/update/colorBar`

**请求**:
```json
{
  "projectId": "PRJ001",
  "mode": "displacement",
  "colorSchemeType": 1,
  "minValue": -100,
  "maxValue": 100,
  "hslHStart": 240,
  "hslHEnd": 0,
  "classCount": 5,
  "autoAdaptRange": true,
  "adaptBufferRatio": 0.1,
  "filterEnable": 1,
  "filterAlpha": 0.3,
  "filterMin": -5,
  "filterMax": 5,
  "customRanges": "[{\"min\":-100,\"max\":-20,\"color\":\"#0000FF\"}]"
}
```

---

### 接口2: 获取位移/散射色条

**端点**: `GET /api/protocol/query/colorBar/{projectId}/{mode}`

---

### 接口3: 更新高程图配置 ✨

**端点**: `POST /api/protocol/update/terrainColor`

**请求**:
```json
{
  "projectId": "PRJ001",
  "colorSchemeType": 0,
  "minValue": 0,
  "maxValue": 1000,
  "hslHStart": 120,
  "hslHEnd": 0,
  "autoAdaptRange": true,
  "adaptBufferRatio": 0.1
}
```

---

### 接口4: 获取高程图配置 ✨

**端点**: `GET /api/protocol/query/terrainColor/{projectId}`

---

## 📝 修改文件清单

### 前端

| 文件 | 修改内容 |
|-----|---------|
| `ColorConfig.vue` | 完全重构，支持线性/分类配色、自适应范围、高程图 |

### 后端

| 文件 | 修改内容 |
|-----|---------|
| `ColorBarConfigEntity.cs` | 添加5个新字段 |
| `TerrainColorConfigEntity.cs` | 新建高程图配置实体 ✨ |
| `RadarDbContext.cs` | 添加`TerrainColorConfigs` DbSet |
| `Program.cs` | 1. colorbar_configs添加5个新字段<br>2. 创建terrain_color_configs表 |
| `ProtocolController.cs` | 1. UpdateColorBar支持新字段<br>2. GetColorBar返回新字段<br>3. 添加UpdateTerrainColor接口<br>4. 添加GetTerrainColor接口 |

---

## ✅ 功能总结

### 支持的配色类型

| 类型 | 线性配色 | 分类配色 | 自适应范围 | 透明通道 |
|-----|---------|---------|-----------|---------|
| 位移色条 | ✅ | ✅ | ✅ | ✅ |
| 散射色条 | ✅ | ✅ | ✅ | ✅ |
| 高程图 | ✅ | ✅ | ✅ | ❌ |

### 配置选项

- ✅ 6种预设色板（蓝绿、红黄、彩虹、冷暖、灰度、自定义）
- ✅ HSL色相滑块（可视化选择）
- ✅ 线性渐变配色
- ✅ 分类分段配色
- ✅ 自动生成分类区间
- ✅ 自定义颜色区间
- ✅ 自适应数值范围
- ✅ 透明通道设置
- ✅ 实时预览

### 编译状态

```
✅ 已成功生成
   0 个警告
   0 个错误
```

---

**实现完成！刷新前端页面测试！** 🎨

**现在颜色配置系统支持：**
- ✅ 三种图像类型（位移、散射、高程）
- ✅ 两种配色方案（线性、分类）
- ✅ 自适应范围（自动调整）
- ✅ 透明通道（隐藏特定范围）
- ✅ 分类自动生成（智能分段）
- ✅ 项目级配置管理

