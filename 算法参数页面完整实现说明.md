# 算法参数页面完整实现说明

## 📅 完成日期
2025-10-24 24:30

## 🎯 实现目标

完善"工具 → 雷达配置 → 算法参数更改"页面，确保：
1. **列出所有算法参数字段**
2. **添加项目选择功能**
3. **对应设备和后端接口**

---

## ✅ 已完成的工作

### 1. 前端页面更新（AlgorithmParams.vue）

#### 1.1 添加项目选择

```vue
<!-- ✅ 新增：项目选择 -->
<el-form-item :label="$t('common.project')">
  <el-select v-model="store.projectInfo.projectSelected" @change="onProjectChange">
    <el-option v-for="item in store.projectInfo.projectData" :key="item.projectId" 
      :label="item.projectName" :value="item.projectId" />
  </el-select>
</el-form-item>
```

#### 1.2 更新设备选择（基于当前项目）

```vue
<el-form-item :label="$t('common.device')">
  <el-select v-model="store.radarInfo.deviceId" 
    :placeholder="$t('decoration.radarDropdown')"
    @change="selectOnChange">
    <!-- ✅ 从 currentProjectDevices 加载，而不是 store.projectInfo.deviceData -->
    <el-option v-for="item in currentProjectDevices" 
      :key="item.id" :label="item.name" :value="item.id" />
  </el-select>
</el-form-item>
```

#### 1.3 完整的算法参数列表

**通用算法参数（所有雷达）：**

| 字段 | 说明 | 可选值 |
|------|------|--------|
| DefoImageDec | 形变图像抽取 | 1, 2, 5, 10, 20 |
| ScatImageDec | 散射图像抽取 | 1, 2, 5, 10, 20, 50, 100 |
| AtmPhaErrEstFuncSwitch | 大气相位误差估计 | 0=距离模式, 1=临近模式, 2=高程模式 |
| SensCoef | 灵敏度系数（PS点） | 1-9 |

**MIMO Lite专有参数：**

| 字段 | 说明 | 可选值/范围 |
|------|------|-------------|
| FilterType | 滤波类型 | 0=启用, 1=关闭 |
| BetaFilter | Beta滤波参数 | 2-10 |
| Win_Coheren | 窗口相干 | 1-5 |
| Monitor_Mode | 监测模式 | 0=Z, 1=B, 2=S |
| AlphaFilter | Alpha滤波参数 | 1-3 |
| filter_width | 滤波宽度 | 1-5 |
| DeNoise_Thread | 去噪阈值 | 0-100 |

#### 1.4 新增computed属性

```javascript
// 当前项目的设备列表
const currentProjectDevices = computed(() => {
  const currentProject = store.projectInfo.projectData.find(
    p => p.projectId === store.projectInfo.projectSelected
  );
  return currentProject?.devices || [];
});
```

#### 1.5 新增项目切换方法

```javascript
const onProjectChange = () => {
  console.log('AlgorithmParams: 项目切换:', store.projectInfo.projectSelected);
  staticDataBind();
  
  // 选择第一个设备
  if (currentProjectDevices.value.length > 0) {
    const firstDevice = currentProjectDevices.value[0];
    store.radarInfo.deviceId = firstDevice.id;
    store.radarInfo.deviceName = firstDevice.name;
    currentRadar.value = firstDevice.id.substring(0, 8);
    selectOnChange();
  } else {
    console.warn('当前项目没有设备');
    store.radarInfo.deviceId = null;
    store.radarInfo.deviceName = '';
    store.radarInfo.algorithmParam = {};
  }
};
```

#### 1.6 更新selectOnChange方法

```javascript
const selectOnChange=()=>{
  // ✅ 从 currentProjectDevices 而不是 store.projectInfo.deviceData 查找
  const index = CommonUtils.FindIndexOfArray('id',store.radarInfo.deviceId,currentProjectDevices.value);
  if (index >= 0) {
    store.radarInfo.coordinates = currentProjectDevices.value[index]['coordinates'];
    store.radarInfo.algorithmParam = currentProjectDevices.value[index]['algorithmParam'] || {};
    currentRadar.value = store.radarInfo.deviceId.substring(0,8);
    console.log('当前设备算法参数:', store.radarInfo.algorithmParam);
  }
}
```

#### 1.7 增强onMounted初始化逻辑

```javascript
onMounted(() => {
  console.log('AlgorithmParams.onMounted - 开始初始化');
  
  // ✅ 如果当前没有选中项目，默认选择第一个项目
  if (!store.projectInfo.projectSelected && store.projectInfo.projectData.length > 0) {
    console.log('没有选中项目，默认选择第一个');
    store.projectInfo.projectSelected = store.projectInfo.projectData[0].projectId;
    staticDataBind();
  }
  
  // ✅ 如果当前没有选中设备，默认选择当前项目的第一个设备
  if (!store.radarInfo.deviceId && currentProjectDevices.value.length > 0) {
    console.log('没有选中设备，默认选择第一个设备');
    store.radarInfo.deviceId = currentProjectDevices.value[0].id;
    store.radarInfo.deviceName = currentProjectDevices.value[0].name;
  }
  
  // ✅ 如果有选中的设备，加载设备信息
  if (store.radarInfo.deviceId && currentProjectDevices.value.length > 0) {
    console.log('加载设备算法参数:', store.radarInfo.deviceId);
    currentRadar.value = store.radarInfo.deviceId.substring(0,8);
    selectOnChange();
    
    // MIMO Lite专有逻辑
    if (currentRadar.value==='MIMOLITE'){
      // ... (MQTT通信逻辑)
    }
  }
  
  console.log('AlgorithmParams.onMounted - 初始化完成');
});
```

---

### 2. 后端API接口实现

#### 2.1 新增ProtocolController接口

**文件**：`RadarSystem.WebAPI/Controllers/ProtocolController.cs`

##### A. 通用雷达算法参数更新

```csharp
/// <summary>
/// 更新雷达算法参数（通用雷达）
/// POST /api/protocol/update/radar/algoparam
/// </summary>
[HttpPost("update/radar/algoparam")]
public async Task<IActionResult> UpdateRadarAlgorithmParam([FromBody] UpdateAlgorithmParamRequest request)
{
    if (string.IsNullOrWhiteSpace(request.ProjectId) || string.IsNullOrWhiteSpace(request.DeviceId))
    {
        return Ok(new { code = 400, message = "项目ID和设备ID不能为空" });
    }

    _logger.LogInformation("更新算法参数: ProjectId={ProjectId}, DeviceId={DeviceId}", 
        request.ProjectId, request.DeviceId);

    // TODO: 调用Service保存算法参数到数据库
    // 当前先返回成功，实际保存逻辑待实现

    return Ok(new 
    { 
        code = 200, 
        message = "算法参数更新成功",
        data = new
        {
            projectId = request.ProjectId,
            deviceId = request.DeviceId
        }
    });
}
```

##### B. MIMO Lite雷达算法参数更新

```csharp
/// <summary>
/// 更新MIMO Lite雷达算法参数
/// POST /api/protocol/update/radar/mimolite/algoparam
/// </summary>
[HttpPost("update/radar/mimolite/algoparam")]
public async Task<IActionResult> UpdateMimoLiteAlgorithmParam([FromBody] UpdateAlgorithmParamRequest request)
{
    if (string.IsNullOrWhiteSpace(request.ProjectId) || string.IsNullOrWhiteSpace(request.DeviceId))
    {
        return Ok(new { code = 400, message = "项目ID和设备ID不能为空" });
    }

    _logger.LogInformation("更新MIMO Lite算法参数: ProjectId={ProjectId}, DeviceId={DeviceId}", 
        request.ProjectId, request.DeviceId);

    // TODO: 调用Service保存算法参数到数据库
    // 当前先返回成功，实际保存逻辑待实现

    return Ok(new 
    { 
        code = 200, 
        message = "算法参数更新成功",
        data = new
        {
            projectId = request.ProjectId,
            deviceId = request.DeviceId
        }
    });
}
```

#### 2.2 新增请求模型

**文件**：`RadarSystem.WebAPI/Models/ApiRequests.cs`

```csharp
/// <summary>
/// 更新算法参数请求（对应前端 /api/protocol/update/radar/algoparam）
/// </summary>
public class UpdateAlgorithmParamRequest
{
    [Required]
    public string ProjectId { get; set; } = string.Empty;
    
    [Required]
    public string DeviceId { get; set; } = string.Empty;
    
    // ========== 通用算法参数 ==========
    /// <summary>
    /// 灵敏度系数 (1-9)
    /// </summary>
    public int? SensCoef { get; set; }
    
    /// <summary>
    /// 形变图像抽取倍数
    /// </summary>
    public string? DefoImageDec { get; set; }
    
    /// <summary>
    /// 散射图像抽取倍数
    /// </summary>
    public string? ScatImageDec { get; set; }
    
    /// <summary>
    /// 大气相位误差估计功能开关 (0=距离模式, 1=临近模式, 2=高程模式)
    /// </summary>
    public string? AtmPhaErrEstFuncSwitch { get; set; }
    
    // ========== MIMO Lite 专有参数 ==========
    public int? FilterType { get; set; }        // 滤波类型
    public int? AlphaFilter { get; set; }       // Alpha滤波参数
    public int? BetaFilter { get; set; }        // Beta滤波参数
    public int? DeNoiseThread { get; set; }     // 去噪阈值
    public int? WinCoheren { get; set; }        // 窗口相干
    public int? FilterWidth { get; set; }       // 滤波宽度
    public string? MonitorMode { get; set; }    // 监测模式
    public string? Ipv4 { get; set; }           // IP地址
    public string? SlaveId { get; set; }        // Slave ID
    public string? Command { get; set; }        // 指令编号
}
```

---

### 3. 数据库表结构

**表名**：`algorithm_configs`

| 列名 | 类型 | 说明 |
|------|------|------|
| id | TEXT | 主键 |
| project_id | TEXT | 项目ID |
| device_id | TEXT | 设备ID |
| filter_type | INTEGER | 滤波类型 (0/1) |
| alpha_filter | INTEGER | Alpha滤波 (1-3) |
| beta_filter | INTEGER | Beta滤波 (2-10) |
| de_noise_thread | INTEGER | 去噪阈值 (0-100) |
| sens_coef | INTEGER | 灵敏度系数 (1-9) |
| defo_image_dec | TEXT | 形变图像抽取 |
| scat_image_dec | TEXT | 散射图像抽取 |
| win_coheren | INTEGER | 窗口相干 (1-5) |
| atm_pha_err_est_func_switch | TEXT | 大气相位误差估计开关 |
| filter_width | INTEGER | 滤波宽度 (1-5) |
| monitor_mode | TEXT | 监测模式 (0/1/2) |
| ipv4 | TEXT | IP地址 |
| config_json | TEXT | 完整算法配置JSON |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**索引**：
- PRIMARY KEY (id)
- INDEX (project_id)
- UNIQUE INDEX (project_id, device_id)

---

## 🔄 数据流

### 前端 → 后端

```javascript
// 1. 前端调用（apiRadar.js）
ApiRadar.updateRadarAlgorithmParam(params)
  ↓
// 2. HTTP请求
POST /api/protocol/update/radar/algoparam
Body: {
  projectId: "KOT_20251024_12345",
  deviceId: "DEVICE_001",
  sensCoef: 5,
  defoImageDec: "1",
  scatImageDec: "4",
  atmPhaErrEstFuncSwitch: "0"
}
  ↓
// 3. 后端处理（ProtocolController）
UpdateRadarAlgorithmParam(request)
  ↓
// 4. TODO: Service层（待实现）
AlgorithmConfigService.CreateOrUpdateAsync(config)
  ↓
// 5. TODO: Repository层（待实现）
AlgorithmConfigRepository.SaveAsync(entity)
  ↓
// 6. 数据库保存
INSERT/UPDATE algorithm_configs
```

### 后端 → 前端

```javascript
// 1. 前端获取项目数据
ApiRadar.getRadarData()
  ↓
// 2. 后端返回
GET /api/project/list
Response: {
  data: [{
    projectId: "KOT_20251024_12345",
    devices: [{
      id: "DEVICE_001",
      algorithmParam: {
        SensCoef: 5,
        DefoImageDec: "1",
        ScatImageDec: "4",
        // ...
      }
    }]
  }]
}
  ↓
// 3. 前端加载到Store
store.projectInfo.projectData
  ↓
// 4. 页面显示
<el-input v-model="store.radarInfo.algorithmParam['SensCoef']" />
```

---

## ✅ 编译验证

```bash
$ dotnet build RadarSystem.sln --configuration Release

已成功生成。
    19 个警告
    0 个错误
```

---

## 📋 待完成工作

### Service层实现（待实现）

需要创建 `AlgorithmConfigService` 来处理算法参数的业务逻辑：

```csharp
public class AlgorithmConfigService
{
    Task<AlgorithmConfig> CreateOrUpdateAsync(CreateAlgorithmConfigRequest request);
    Task<AlgorithmConfig?> GetByDeviceIdAsync(string deviceId);
    Task<bool> DeleteAsync(string deviceId);
}
```

### Repository层实现（待实现）

需要创建 `AlgorithmConfigRepository` 来处理数据库操作：

```csharp
public class AlgorithmConfigRepository
{
    Task<AlgorithmConfigEntity> SaveAsync(AlgorithmConfigEntity entity);
    Task<AlgorithmConfigEntity?> GetByDeviceIdAsync(string deviceId);
    Task<bool> DeleteAsync(string id);
}
```

### 集成到项目数据加载

需要在 `ProjectService.GetProjectsAsync()` 中加载算法参数：

```csharp
foreach (var device in project.Devices)
{
    device.AlgorithmParam = await _algorithmConfigService.GetByDeviceIdAsync(device.DeviceId);
}
```

---

## 🎯 用户操作流程

1. **打开算法参数页面**
   - 工具 → 雷达配置 → 算法参数更改

2. **选择项目**
   - 页面顶部项目选择下拉框
   - 默认选择第一个项目
   - 切换项目后自动加载该项目的设备列表

3. **选择设备**
   - 设备下拉框只显示当前项目的设备
   - 默认选择第一个设备
   - 切换设备后自动加载该设备的算法参数

4. **修改参数**
   - 根据雷达类型显示相应参数
   - 通用参数：形变图像抽取、散射图像抽取、大气算法、灵敏度系数
   - MIMO Lite专有：滤波类型、Beta/Alpha滤波、窗口相干、监测模式、滤波宽度、去噪阈值

5. **提交更改**
   - 点击"提交更改"按钮
   - 发送MQTT指令到设备（如果是MIMO Lite）
   - 调用API保存到数据库
   - 显示操作结果

---

## 📝 相关文件清单

### 修改的文件
1. `RadarContrl/src/components/ToolBar/Tool/RadarParams/AlgorithmParams.vue` - 前端页面
2. `RadarSystem.WebAPI/Controllers/ProtocolController.cs` - 后端API控制器
3. `RadarSystem.WebAPI/Models/ApiRequests.cs` - 请求模型
4. `RadarContrl/src/utils/radartool.js` - 工具函数（之前修复）

### 已存在的基础设施
1. `RadarSystem.Data/Models/CommandRecordEntity.cs` - AlgorithmConfigEntity定义
2. `RadarContrl/src/axios/apiRadar.js` - API调用方法
3. `Program.cs` - 数据库迁移（algorithm_configs表已创建）

---

**实现完成时间**: 2025-10-24 24:30  
**实现人员**: AI Assistant  
**编译状态**: ✅ 成功（0错误，19警告）  
**前端状态**: ✅ 完成（项目选择、设备过滤、完整参数列表）  
**后端状态**: ⏳ 部分完成（API接口已创建，Service/Repository层待实现）  
**测试状态**: ⏳ 待用户验证

