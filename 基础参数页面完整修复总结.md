# 基础参数页面完整修复总结

**修复时间**: 2025-10-25  
**页面**: 工具 → 基础参数 (RadarParams.vue)

---

## 🎯 用户需求回顾

### 原始问题

1. ❌ 设备下拉框显示不是设备名称
2. ❌ 设备信息显示不正确
3. ❌ 无法修改设备信息
4. ❌ 提交没保存入库成功

### 用户优化建议

**功能拆分**:
1. **保存基础信息** - 直接入库，不下发指令
   - 设备名称、出厂ID
   - 经度、纬度、高程
   - 零点朝向

2. **下发雷达参数** - 先下发指令，成功后再入库
   - 起始角度、终止角度
   - 最近距离、最远距离
   - 频段、天线波束角、刷新频率
   - **指令失败时保持原值**

---

## ✅ 完整修复清单

### 修复1: 数据加载问题

**问题**: 设备下拉框显示不正确，设备信息不完整

**原因**: 
- 页面使用`/api/project/list`接口的数据
- 该接口不返回完整的设备信息（缺少factoryId, params等）

**修复**:
- 页面加载时调用`GET /api/Device?projectId=xxx`
- 项目切换时也调用该接口
- 获取完整的设备信息（包含雷达参数）

**修改位置**:
- `onMounted()` - 添加设备查询逻辑
- `onProjectChange()` - 添加设备查询逻辑

---

### 修复2: 设备类型判断问题

**问题**: MIMO设备错误地调用了ArcSAR接口

**原因**:
```javascript
// ❌ 错误判断
currentRadar.value = deviceId.substring(0, 8);
// "MIMO_20251025_901".substring(0, 8) = "MIMO_202"
// "MIMO_202" !== "MIMOLITE"
```

**修复**:
```javascript
// ✅ 正确判断
currentRadar.value = device.type;  // "MIMOLITE" 或 "ER"
```

**修改位置**:
- `selectOnChange()` - 根据设备类型设置
- 删除了其他地方的`substring(0, 8)`调用

---

### 修复3: 功能拆分

**问题**: 一个按钮做两件事，错误提示不明确

**修复**: 拆分为两个按钮

#### 按钮1: 保存基础信息

**方法**: `saveBasicInfo()`

**功能**:
```javascript
验证字段
  ↓
准备数据: {
  deviceName,
  factoryId,
  longitude,
  latitude,
  elevation,
  orientation
}
  ↓
PUT /api/Device/{deviceId}
  ↓
更新 Devices表
  ↓
✅ 提示"基础信息保存成功"
  ↓
重新加载设备信息
```

#### 按钮2: 下发雷达参数

**方法**: `sendRadarParams()`

**功能**:
```javascript
验证参数（角度、距离 >= 0）
  ↓
准备数据: {
  ImgAngleStart,
  ImgAngleEnd,
  RngMin,
  RngMax,
  FreqBand,
  AnteBeam_half
}
  ↓
步骤1: 发送指令到雷达设备
  POST /api/{radarType}/command/.../11/qingqiangjia
  ├─ ✅ 成功 (200) → 继续步骤2
  └─ ❌ 失败 → 提示"指令发送失败"，结束 ⚠️
  ↓
步骤2: 保存到数据库
  POST /api/protocol/update/radar/param
  ↓
更新 radar_params表
  ↓
✅ 提示"雷达参数下发并保存成功"
  ↓
重新加载设备信息
```

---

### 修复4: 字段名大小写

**问题**: 前端发送的字段名和后端期望的不匹配

**修复**:
```javascript
// ✅ 统一使用大写开头
params['ImgAngleStart'] = ...  // 不是imgAngleStart
params['FreqBand'] = ...       // 不是freqBand
params['AnteBeam_half'] = ...  // 不是anteBeam_half
```

---

### 修复5: 错误处理

**问题**: 缺少调试日志和错误处理

**修复**:
```javascript
// ✅ 添加调试日志
console.log('准备保存的基础信息:', basicInfo);
console.log('保存基础信息响应:', res);

// ✅ 添加错误处理
try {
  // ...
} catch (err) {
  console.error('保存失败:', err);
  showMessage('保存失败: ' + err.message, 'error');
}
```

---

### 修复6: 导入axios

**问题**: `ReferenceError: axios is not defined`

**修复**:
```javascript
import axios from 'axios';  // ✅ 添加axios导入
```

---

## 📋 完整修改清单

| 修复项 | 问题 | 解决方案 | 文件 |
|-------|------|---------|------|
| 数据加载 | 设备信息不完整 | 调用设备查询接口 | RadarParams.vue |
| 设备类型 | MIMO设备判断错误 | 使用device.type | RadarParams.vue |
| 功能拆分 | 操作不明确 | 两个独立按钮 | RadarParams.vue |
| 字段名 | 大小写不匹配 | 统一大写开头 | RadarParams.vue |
| 错误处理 | 缺少日志 | 添加调试日志 | RadarParams.vue |
| 导入 | axios未导入 | 添加import | RadarParams.vue |
| 数据库表 | 雷达参数无表 | 创建radar_params表 | RadarParamEntity.cs |
| 后端接口 | 设备查询不完整 | 返回完整信息 | DeviceController.cs |

---

## 🔄 完整数据流程

### 流程1: 页面加载

```
用户打开"基础参数"页面
  ↓
onMounted() 执行
  ↓
选择项目（默认第一个）
  ↓
✅ GET /api/Device?projectId=xxx
  ↓
后端查询:
  - Devices表
  - radar_params表
  - algorithm_configs表
  ↓
返回完整设备信息:
  {
    deviceId: "MIMO_xxx",
    deviceName: "雷达1",  // ✅ 有名称
    factoryId: "F001",   // ✅ 有出厂ID
    params: {            // ✅ 有雷达参数
      ImgAngleStart: 0,
      ImgAngleEnd: 360,
      ...
    }
  }
  ↓
✅ 设备下拉框显示"雷达1"
✅ 表单显示完整信息
```

### 流程2: 保存基础信息

```
用户修改设备名称、出厂ID、坐标
  ↓
点击"保存基础信息"
  ↓
✅ PUT /api/Device/{deviceId}
  {
    deviceName: "新名称",
    factoryId: "F002",
    longitude: 120.123,
    latitude: 30.456,
    elevation: 100.5,
    orientation: 45.0
  }
  ↓
更新 Devices表
  ↓
✅ 返回 { code: 200 }
  ↓
✅ 提示"基础信息保存成功"
  ↓
重新加载设备信息
  ↓
✅ 页面显示最新值
```

### 流程3: 下发雷达参数

```
用户修改起始角度、最大距离
  ↓
点击"下发雷达参数"
  ↓
步骤1: 发送指令
  ├─ MIMO: POST /api/mimoLite/command/.../11/qingqiangjia
  └─ ArcSAR: POST /api/arcsar/command/.../11/qingqiangjia
  ↓
雷达硬件响应
  ├─ ✅ 接受 (200) → 继续步骤2
  └─ ❌ 拒绝 (405/500) → ⚠️ 提示"指令失败"，结束
  ↓
步骤2: 保存到数据库（只在指令成功时执行）
  POST /api/protocol/update/radar/param
  {
    ImgAngleStart: 10,
    ImgAngleEnd: 350,
    RngMax: 1500,
    ...
  }
  ↓
更新 radar_params表
  ↓
✅ 返回 { code: 200 }
  ↓
✅ 提示"雷达参数下发并保存成功"
  ↓
重新加载设备信息
  ↓
✅ 页面显示最新值
```

---

## 🎨 UI布局

```
┌──────────────────────────────────────────┐
│  [保存基础信息]  [下发雷达参数]            │
├──────────────────────────────────────────┤
│ 项目: [项目1 ▼]                           │
│ 设备: [雷达1 ▼] [+] [-]                   │
│                                           │
│ 设备ID: MIMO_20251025_901 (只读)         │
│ 设备名称: [雷达1____] ← 可改               │
│ 出厂ID: [F001____] ← 可改                 │
│ 经度: [120.123456] ← 可改                 │
│ 纬度: [30.654321] ← 可改                  │
│ 高程: [100.5] ← 可改                      │
│ 零点朝向: [45.0°] ← 可改                  │
│                                           │
│ 起始角度: [0°] ← 可改                     │
│ 终止角度: [360°] ← 可改                   │
│ 最小距离: [0m] ← 可改                     │
│ 最大距离: [1000m] ← 可改                  │
│ 频段: [0 ▼] ← 可改                        │
│ 天线波束角: [60°] ← 可改                  │
│ (MIMO专用) 刷新频率: [60s]               │
└──────────────────────────────────────────┘
```

---

## 📊 数据库表关系

### Devices表 (设备基础信息)

```sql
UPDATE Devices SET
  DeviceName = '新名称',  -- ← 保存基础信息按钮
  FactoryId = 'F002',     -- ← 保存基础信息按钮
  Longitude = 120.123,    -- ← 保存基础信息按钮
  Latitude = 30.456,      -- ← 保存基础信息按钮
  Elevation = 100.5,      -- ← 保存基础信息按钮
  Orientation = 45.0      -- ← 保存基础信息按钮
WHERE DeviceId = 'MIMO_xxx';
```

### radar_params表 (雷达参数)

```sql
UPDATE radar_params SET
  img_angle_start = 10,   -- ← 下发雷达参数按钮（指令成功后）
  img_angle_end = 350,    -- ← 下发雷达参数按钮（指令成功后）
  rng_min = 0,            -- ← 下发雷达参数按钮（指令成功后）
  rng_max = 1500,         -- ← 下发雷达参数按钮（指令成功后）
  freq_band = '1',        -- ← 下发雷达参数按钮（指令成功后）
  ante_beam_half = 70     -- ← 下发雷达参数按钮（指令成功后）
WHERE device_id = 'MIMO_xxx';
```

---

## 🚀 测试场景

### 场景1: 只修改设备名称

```
操作:
  1. 设备名称: "雷达1" → "新雷达1"
  2. 点击"保存基础信息"

预期结果:
  ✅ 提示"基础信息保存成功"
  ✅ 数据库Devices.DeviceName已更新
  ✅ 没有发送指令到雷达硬件
  ✅ 刷新页面后显示"新雷达1"
```

### 场景2: 只下发雷达参数

```
操作:
  1. 起始角度: 0 → 10
  2. 点击"下发雷达参数"

预期结果:
  ✅ 发送指令到雷达: POST /api/mimoLite/command/.../11/qingqiangjia
  ✅ 雷达确认成功 (200)
  ✅ 保存到数据库: radar_params.img_angle_start = 10
  ✅ 提示"雷达参数下发并保存成功"
  ✅ 刷新页面后显示10°
```

### 场景3: 雷达离线，指令失败

```
操作:
  1. 起始角度: 0 → 10
  2. 雷达离线或拒绝参数
  3. 点击"下发雷达参数"

预期结果:
  ⚠️ 发送指令失败 (405/500)
  ⚠️ 提示"指令发送失败，参数未保存到数据库"
  ✅ 数据库radar_params.img_angle_start保持原值 0
  ✅ 刷新页面后仍然显示0°
```

### 场景4: 同时修改基础信息和雷达参数

```
操作:
  1. 设备名称: "雷达1" → "新雷达1"
  2. 起始角度: 0 → 10
  3. 先点击"保存基础信息"
  4. 再点击"下发雷达参数"

预期结果:
  ✅ 设备名称已保存
  ✅ 起始角度已下发并保存
  ✅ 两个操作都成功
```

---

## 📝 修改文件清单

### 前端文件

| 文件 | 修改内容 | 行数 |
|-----|---------|------|
| `RadarParams.vue` | 添加两个按钮 | 22-27 |
| `RadarParams.vue` | 添加axios导入 | 175 |
| `RadarParams.vue` | 实现saveBasicInfo() | 570-644 |
| `RadarParams.vue` | 实现sendRadarParams() | 647-751 |
| `RadarParams.vue` | 修复设备类型判断 | 481-482 |
| `RadarParams.vue` | 修复onMounted | 754-769 |
| `RadarParams.vue` | 修复onProjectChange | 372-460 |
| `RadarParams.vue` | 添加调试日志 | 多处 |
| `apiRadar.js` | 添加getDevicesByProjectId | 27-36 |

### 后端文件

| 文件 | 修改内容 |
|-----|---------|
| `RadarParamEntity.cs` | 创建雷达参数实体 (新建) |
| `RadarDbContext.cs` | 添加RadarParams DbSet |
| `Program.cs` | 添加radar_params表创建逻辑 |
| `DeviceController.cs` | 增强GetDevices接口 |
| `ProjectController.cs` | 增强GetProjects接口 |
| `ProtocolController.cs` | 完善UpdateRadarParams接口 |
| `ProjectModels.cs` | Device DTO添加Params字段 |

---

## ✅ 最终验证

### 数据库验证

```sql
-- 检查基础信息
SELECT DeviceId, DeviceName, FactoryId, Longitude, Latitude, Elevation, Orientation, LastUpdateTime 
FROM Devices;

-- 检查雷达参数
SELECT device_id, img_angle_start, img_angle_end, rng_min, rng_max, freq_band, ante_beam_half, update_time
FROM radar_params;
```

### 网络请求验证

**F12 → Network标签**:

- ✅ GET `/api/Device?projectId=xxx` - 200 OK
- ✅ PUT `/api/Device/{deviceId}` - 200 OK
- ✅ POST `/api/mimoLite/command/.../11/qingqiangjia` - 200 OK (不再是405)
- ✅ POST `/api/protocol/update/radar/param` - 200 OK

### 控制台验证

**F12 → Console标签**:

```
页面加载-设备映射: { deviceId: "MIMO_xxx", deviceTypeCode: 2, deviceName: "雷达1" }
  → 页面加载-映射后的类型: MIMOLITE
设备类型: MIMOLITE, currentRadar.value: MIMOLITE
准备保存的基础信息: { deviceName: "新雷达1", ... }
保存基础信息响应: { code: 200 }
基础信息保存成功 ✅
```

---

## 🎉 总结

### 修复前的问题

1. ❌ 设备下拉框显示错误
2. ❌ 设备信息不完整
3. ❌ 无法修改保存
4. ❌ 错误提示不明确
5. ❌ MIMO设备调用错误接口

### 修复后的效果

1. ✅ 设备下拉框显示设备名称
2. ✅ 设备信息完整显示
3. ✅ 基础信息可以独立保存
4. ✅ 雷达参数先下发再保存
5. ✅ MIMO设备调用正确接口
6. ✅ 错误提示明确
7. ✅ 调试日志完善

### 架构优化

- ✅ 数据源统一（使用设备查询接口）
- ✅ 职责分离（基础信息 vs 雷达参数）
- ✅ 错误处理完善
- ✅ 数据库表结构优化
- ✅ 前后端字段名统一

---

**修复完成！刷新前端页面测试！** 🎉

**现在您会看到两个按钮，功能清晰明确！**

