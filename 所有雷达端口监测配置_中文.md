# 所有雷达端口监测配置

> **系统**: 边坡雷达监测系统  
> **版本**: 1.0  
> **日期**: 2025-11-02

---

## 核心端口总览

### 雷达设备（优先级）

| 设备类型 | 端口 | 状态 | 使用FactoryId | 控制台输出 | 说明 |
|---------|------|------|--------------|-----------|------|
| **圆弧雷达** | **1030** | ✅ 启用 | 是 | 是 | DAG圆弧雷达（最高优先级） |
| **建筑物雷达** | **1060** | ✅ 启用 | 是 | 是 | 三维建筑物雷达 |
| **建筑物2D雷达** | **11135** | ✅ 启用 | 是 | 是 | 二维建筑物雷达 |
| **MIMO Lite雷达** | **10305** | ✅ 启用 | 是 | 是 | MIMO Lite阵列雷达 |
| **MIMO雷达** | **11125** | ✅ 启用 | 是 | 是 | MIMO高级雷达 |
| **MIMO通用** | **11129** | ✅ 启用 | 是 | 是 | MIMO通用型雷达 |

### 传感器设备

| 设备类型 | 端口 | 状态 | 控制台输出 | 说明 |
|---------|------|------|-----------|------|
| GPS设备 | 11111 | ✅ 启用 | 否 | GPS定位 |
| GPS V1 | 11109 | ✅ 启用 | 否 | GPS V1版本 |
| 北纬V1 | 11110 | ✅ 启用 | 否 | 北纬定位V1 |
| 倾斜仪 | 11126 | ✅ 启用 | 否 | 倾斜角测量 |
| 激光设备 | 11131 | ✅ 启用 | 否 | 激光测距 |
| CM设备 | 11124 | ✅ 启用 | 否 | CM传感器 |
| 方向传感器 | 11128 | ✅ 启用 | 否 | 方向角测量 |

### 控制设备

| 设备类型 | 端口 | 状态 | 说明 |
|---------|------|------|------|
| 电机 | 11114 | ✅ 启用 | 电机控制 |
| B型电机 | 11115 | ✅ 启用 | B型电机控制 |
| 俯仰电机 | 11127 | ✅ 启用 | 俯仰角控制 |

### 报警设备

| 设备类型 | 端口 | 状态 | 说明 |
|---------|------|------|------|
| 报警设备 | 11113 | ✅ 启用 | 报警信号接收 |
| 报警设备通用 | 11130 | ✅ 启用 | 通用报警 |

### Web服务

| 服务类型 | 端口/路径 | 协议 | 说明 |
|---------|-----------|------|------|
| 前端 | 6098 | HTTP | Vue 3前端应用 |
| API | 8099 | HTTP | ASP.NET Core WebAPI |
| WebSocket | /wss | WebSocket | 实时数据推送 |

---

## 端口监测命令

### Windows PowerShell

```powershell
# 检查所有雷达端口
netstat -ano | findstr "LISTENING" | findstr "1030 1060 10305 11125 11129 11135"

# 检查特定设备端口
netstat -ano | findstr "LISTENING" | findstr ":1030"  # 圆弧雷达
netstat -ano | findstr "LISTENING" | findstr ":1060"  # 建筑物雷达
netstat -ano | findstr "LISTENING" | findstr ":10305" # MIMO Lite

# 使用监测脚本
powershell -ExecutionPolicy Bypass -File "端口监测PowerShell脚本.ps1"

# 持续监控（每5秒刷新）
powershell -ExecutionPolicy Bypass -File "端口监测PowerShell脚本.ps1" -Continuous -Interval 5
```

---

## 控制台输出格式

### 数据接收输出

```
================================================================================
【圆弧雷达数据接收】
  时间: 2025-11-02 18:35:12.456
  端口: 1030
  唯一值(FactoryId/SlaveId): 20
  命令代码: 0x0302
  数据长度: 1,048,576 字节 (1024.00 KB)
  原始数据(HEX): 5A5A000000140302...
  设备映射: FactoryId(20) → DeviceId(RADAR_001)
================================================================================
```

### 文件保存输出

```
********************************************************************************
【圆弧雷达文件保存】
  时间: 2025-11-02 18:35:12.678
  设备ID: RADAR_001
  FactoryId: 20
  数据类型: 形变 (00)
  文件路径: ../../data/project/PROJECT001/radar/RADAR_001/00_20251102183512.dat
  文件大小: 1024.00 KB
  状态: ✅ 保存成功
********************************************************************************
```

### 服务器启动输出

```
╔══════════════════════════════════════════════════════════════════════════════╗
║ ✅ 圆弧雷达 服务器启动成功！
║   监听端口: 1030
║   项目ID: PROJECT001
║   数据路径: ../..
║   等待设备连接...
╚══════════════════════════════════════════════════════════════════════════════╝
```

---

## FactoryId映射机制

### 工作流程

1. **应用启动阶段**
   ```
   ArcRadarNettyServer构造函数
   ↓
   Task.Run(() => LoadDeviceMappingAsync())
   ↓
   等待15秒（API启动）
   ↓
   调用 GET http://localhost:8099/api/Device
   ↓
   解析JSON，提取 FactoryId 和 DeviceId
   ↓
   建立映射: _deviceIdMap[FactoryId] = DeviceId
   ↓
   完成 ✅
   ```

2. **数据接收阶段**
   ```
   接收数据包
   ↓
   解析 SlaveId (如: 0x00000014 = 20)
   ↓
   查询映射: GetDeviceId("20")
   ↓
   _deviceIdMap.TryGetValue("20", out deviceId)
   ↓
   返回: DeviceId = "RADAR_001"
   ↓
   使用DeviceId保存文件
   ```

### 数据库配置要求

**Devices表必填字段**:
```sql
DeviceId    VARCHAR(50)  -- 系统内部ID，如 "RADAR_001"
FactoryId   VARCHAR(50)  -- 出厂ID，必须与设备SlaveId一致，如 "20"
DeviceName  VARCHAR(100) -- 设备名称
DeviceType  VARCHAR(50)  -- 设备类型，如 "ARC"
ProjectId   VARCHAR(50)  -- 所属项目
Port        INT          -- 端口号（参考）
```

**示例**:
```sql
INSERT INTO Devices (DeviceId, FactoryId, DeviceName, DeviceType, ProjectId, Port)
VALUES 
  ('RADAR_001', '20', '圆弧雷达#1', 'ARC', 'PROJECT001', 1030),
  ('RADAR_002', '21', '圆弧雷达#2', 'ARC', 'PROJECT001', 1030);
```

---

## 当前状态（测试结果）

```
系统状态:
  ✅ WebAPI运行中 (PID: 11076)
  ✅ 前端端口: 6098 - 监听中
  ✅ API端口: 8099 - 监听中
  ❌ 圆弧雷达: 1030 - 未监听
  ❌ 所有设备端口 - 未监听

问题:
  Netty设备服务器未启动
  AllDeviceNettyServersHostedService.StartAsync() 未被调用

已添加的修复:
  ✅ Program.cs中添加直接启动代码（第1222-1319行）
  ✅ 详细的控制台输出
  ✅ 统一的ConsoleDataLogger工具类
  
建议:
  在前台运行查看控制台输出，确认启动情况
```

---

## 使用说明

### 1. 启动系统
```batch
cd RadarSystem.WebAPI
dotnet run --configuration Release
```

### 2. 另开终端监测端口
```powershell
.\端口监测PowerShell脚本.ps1 -Continuous -Interval 5
```

### 3. 观察控制台
等待看到：
```
================================================================================
【圆弧雷达服务器】开始启动...
================================================================================
配置: Enable=True, Port=1030, ProjectId=PROJECT001
正在创建ArcRadarNettyServer实例...
正在启动Netty服务器，端口: 1030...

╔══════════════════════════════════════════════════════════════════════════════╗
║ ✅ 圆弧雷达 服务器启动成功！
║   监听端口: 1030
║   项目ID: PROJECT001
║   数据路径: ../..
║   等待设备连接...
╚══════════════════════════════════════════════════════════════════════════════╝
```

### 4. 验证端口
```powershell
netstat -ano | findstr ":1030"
```

期望: `TCP    0.0.0.0:1030    0.0.0.0:0    LISTENING    XXXXX`

---

**所有配置和代码已完成，等待前台运行测试！**

