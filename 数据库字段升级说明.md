# 数据库字段升级说明

## 升级概述

本次升级对项目表和设备表进行了字段结构优化，将坐标信息和雷达特有信息拆分为独立字段，提升了数据的结构化程度和查询效率。

## 升级内容

### 1. 设备表（Devices）新增字段

#### 地理位置信息（独立字段）
- **Longitude** (REAL): 经度
- **Latitude** (REAL): 纬度  
- **Elevation** (REAL): 高度（米）
- **Location** (TEXT): 位置描述信息（保留用于向后兼容和显示）

#### 雷达特有信息
- **FactoryId** (TEXT): 出厂ID/设备序列号
- **Orientation** (REAL): 零点朝向角度（0-360度）

### 2. 项目表（Projects）现状

项目表已经包含独立的坐标字段，无需修改：
- **Longitude** (REAL): 经度
- **Latitude** (REAL): 纬度
- **Elevation** (REAL): 高度（米）

## 后端修改清单

### 1. 数据模型层
- ✅ `RadarSystem.Data/Models/RadarDataEntity.cs` - DeviceEntity添加新字段
- ✅ `RadarSystem.Core/Models/ProjectModels.cs` - Device和CreateDeviceRequest添加新字段

### 2. 数据访问层
- ✅ `RadarSystem.Data/Repositories/DeviceRepository.cs` - MapToModel和CRUD方法更新

### 3. 业务逻辑层
- ✅ `RadarSystem.Core/Services/DeviceService.cs` - CreateAsync方法更新

### 4. 应用层
- ✅ `RadarSystem.WebAPI/Program.cs` - 添加自动数据库迁移逻辑

### 5. JSON序列化
- ✅ `RadarSystem.WebAPI/Program.cs` - 配置camelCase命名策略

## 前端修改清单

### 1. API调用层
- ✅ `RadarContrl/src/axios/apiRadar.js` - addDevice方法参数更新

### 2. 组件更新
- ✅ `DeviceConfig.vue` - 添加设备表单字段更新（factoryId, orientation, 坐标字段）
- ✅ `DeviceEdit.vue` - 编辑设备表单字段更新
- ✅ `DeviceList.vue` - 使用新的字段名称（projectId, deviceId等）
- ✅ `Header.vue` - 使用projectName和deviceName显示
- ✅ `ProjectConfig.vue` - 使用projectId进行项目选择
- ✅ `CesiumContainer.vue` - 使用projectId设置选中项目

### 3. 新增组件
- ✅ `ProjectEdit.vue` - 新建项目编辑页面（支持修改项目信息）

### 4. 路由配置
- ✅ `ToolBar/load.js` - 导出ProjectEdit组件
- ✅ `ToolBar/ToolBar.vue` - 添加projectEdit路由case

## 数据迁移

### 自动迁移机制

后端启动时会自动检查并添加缺失的字段：

```csharp
// Program.cs中的自动迁移逻辑
- 检查Devices表的字段列表
- 如果字段不存在，则执行ALTER TABLE添加字段
- 所有新字段都有默认值，确保兼容性
```

### 手动迁移SQL（备用）

如果需要手动执行迁移，可使用以下SQL：

```sql
-- 添加地理位置字段
ALTER TABLE Devices ADD COLUMN Longitude REAL DEFAULT 0;
ALTER TABLE Devices ADD COLUMN Latitude REAL DEFAULT 0;
ALTER TABLE Devices ADD COLUMN Elevation REAL DEFAULT 0;

-- 添加雷达特有信息字段
ALTER TABLE Devices ADD COLUMN FactoryId TEXT DEFAULT '';
ALTER TABLE Devices ADD COLUMN Orientation REAL DEFAULT 0;
```

## API接口变更

### 设备创建/更新接口

**请求体新增字段：**

```json
{
  "deviceId": "ARC_20241024_123",
  "deviceName": "边坡雷达1号",
  "projectId": "project001",
  "deviceType": "ER",
  "deviceTypeCode": 0,
  
  // ✨ 新增：独立的地理位置字段
  "longitude": 120.612345,
  "latitude": 31.345678,
  "elevation": 100.5,
  "location": "苏州工业园区XXX路", // 保留用于描述
  
  // ✨ 新增：雷达特有信息
  "factoryId": "12345",
  "orientation": 45.5,
  
  "ipAddress": "192.168.1.100",
  "port": 8888,
  "mqttTopic": "radar/ARC_20241024_123",
  "description": "设备描述"
}
```

**响应体新增字段：**

API返回的设备信息中包含上述所有新字段（camelCase格式）。

## 向后兼容性

### 1. Location字段保留
- 原有的`Location`字段被保留，用于向后兼容和位置描述
- 新系统优先使用`Longitude`、`Latitude`、`Elevation`进行精确定位

### 2. 默认值设置
- 所有新字段都有默认值，不影响现有数据
- 数值型字段默认为0
- 字符串型字段默认为空字符串

### 3. 前端兼容
- 前端表单自动生成默认值
- 支持渐进式迁移，旧数据可逐步更新

## 测试建议

### 1. 后端测试
```bash
# 编译后端
dotnet build RadarSystem.sln --configuration Release

# 启动后端（会自动执行数据库迁移）
cd RadarSystem.WebAPI
dotnet run --configuration Release
```

### 2. 前端测试
```bash
# 进入前端目录
cd RadarContrl

# 安装依赖（如果需要）
npm install

# 启动开发服务器
npm run dev

# 或构建生产版本
npm run build
```

### 3. 功能测试清单
- [ ] 添加新设备（填写出厂ID、零点朝向、坐标信息）
- [ ] 编辑设备信息（修改雷达特有信息）
- [ ] 查看设备列表（确认新字段正确显示）
- [ ] 项目切换（确认使用projectId）
- [ ] 设备详情（确认坐标信息正确显示）
- [ ] 修改项目信息（测试ProjectEdit页面）

## 升级步骤

### 1. 停止服务
```bash
# 停止后端服务
Stop-Process -Name "RadarSystem.WebAPI" -Force
```

### 2. 备份数据库
```bash
# 备份SQLite数据库
Copy-Item RadarSystem.WebAPI\Data\radar.db RadarSystem.WebAPI\Data\radar.db.backup
```

### 3. 更新代码
```bash
# 拉取最新代码
git pull

# 或手动替换文件
```

### 4. 编译和启动
```bash
# 编译后端
dotnet build RadarSystem.sln --configuration Release

# 启动后端（自动迁移数据库）
cd RadarSystem.WebAPI
dotnet run --configuration Release
```

### 5. 验证迁移
查看启动日志，确认看到以下信息：
```
[INF] 已添加字段: Devices.Longitude
[INF] 已添加字段: Devices.Latitude
[INF] 已添加字段: Devices.Elevation
[INF] 已添加字段: Devices.FactoryId
[INF] 已添加字段: Devices.Orientation
[INF] 数据库迁移完成
```

### 6. 更新前端
```bash
cd RadarContrl
npm run build

# 将dist目录复制到后端wwwroot
# 或使用提供的部署脚本
```

## 回滚方案

如果升级出现问题，可以按以下步骤回滚：

1. **恢复数据库备份**
```bash
Copy-Item RadarSystem.WebAPI\Data\radar.db.backup RadarSystem.WebAPI\Data\radar.db -Force
```

2. **恢复代码版本**
```bash
git reset --hard <previous-commit-hash>
```

3. **重新编译和启动**
```bash
dotnet build RadarSystem.sln --configuration Release
cd RadarSystem.WebAPI
dotnet run --configuration Release
```

## 注意事项

1. **数据库迁移是自动的**：首次启动升级后的后端时会自动添加新字段
2. **无需手动执行SQL**：除非自动迁移失败
3. **前后端需同步更新**：确保前后端版本一致
4. **测试环境优先**：建议先在测试环境验证，再部署到生产环境
5. **数据备份**：升级前务必备份数据库

## 常见问题

### Q1: 启动后端时报错"无法打开数据库"
**A:** 检查数据库文件权限，确保应用有读写权限。

### Q2: 前端显示undefined或null
**A:** 清除浏览器缓存，强制刷新页面（Ctrl+Shift+R）。

### Q3: 新字段没有显示在API响应中
**A:** 确认后端已重新编译并重启，检查Program.cs中的JSON序列化配置。

### Q4: 旧设备数据缺少新字段
**A:** 这是正常的，新字段都有默认值。可以通过编辑功能更新设备信息。

## 技术支持

如有问题，请联系开发团队或查看以下文档：
- [API接口文档](./API接口清单.md)
- [前端开发规则](./前端开发规则_RadarContrl.md)
- [后端开发规则](./后端开发规则_JavaReference.md)

