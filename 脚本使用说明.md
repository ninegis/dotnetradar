# 边坡雷达监测系统 - 脚本使用说明

## 📜 可用脚本

### 1. 启动系统.bat ⭐ 推荐
**用途**: 一键编译、部署、启动完整系统

**功能**:
- ✅ 检查 .NET 和 Node.js 环境
- ✅ 构建前端项目
- ✅ 还原后端依赖
- ✅ 编译后端项目
- ✅ 启动系统服务
- ✅ 自动打开浏览器

**使用场景**: 日常开发、测试、演示

**执行步骤**:
```
[1/6] 检查 .NET 环境
[2/6] 检查 Node.js 环境
[3/6] 前端构建
[4/6] 还原后端依赖
[5/6] 编译后端项目
[6/6] 启动系统服务
```

---

### 2. 构建并部署前端.bat
**用途**: 仅构建和部署前端项目

**功能**:
- ✅ 检查 Node.js 环境
- ✅ 检查前端项目完整性
- ✅ 构建前端项目
- ✅ 验证构建产物
- ✅ 构建后端项目

**使用场景**: 前端代码更新后需要重新构建

**执行步骤**:
```
[1/5] 检查Node.js环境
[2/5] 检查前端项目
[3/5] 构建前端项目
[4/5] 验证构建产物
[5/5] 构建后端项目
```

---

### 3. start.ps1 (PowerShell)
**用途**: PowerShell版启动脚本，支持更多选项

**功能**:
- ✅ 支持跳过前端构建
- ✅ 支持开发模式（热重载）
- ✅ 更详细的输出信息

**使用方法**:
```powershell
# 标准启动
.\start.ps1

# 跳过前端构建
.\start.ps1 -SkipBuild

# 开发模式（热重载）
.\start.ps1 -DevMode
```

---

## 📂 项目结构

### 前端项目
```
RadarSystem.WebAPI\ClientApp\
├── src\                          # 源代码
│   ├── views\                    # 页面组件
│   ├── components\               # 通用组件
│   ├── api\                      # API接口
│   ├── stores\                   # 状态管理
│   ├── router\                   # 路由配置
│   └── main.ts                   # 入口文件
├── package.json                  # 依赖配置
├── vite.config.ts                # Vite构建配置
└── index.html                    # HTML模板

构建输出:
RadarSystem.WebAPI\wwwroot\       # 静态文件目录
```

### 后端项目
```
RadarSystem.WebAPI\
├── Controllers\                  # API控制器
├── Services\                     # 业务服务
├── Middlewares\                  # 中间件
├── Data\                         # 数据库文件
├── logs\                         # 日志文件
└── Program.cs                    # 程序入口
```

---

## 🔄 构建流程

### 前端构建 (ClientApp)
```
npm install              # 安装依赖
  ↓
npm run build            # 执行构建
  ↓
vite build               # Vite编译
  ↓
输出到 ../wwwroot        # 构建产物
```

**Vite配置** (`vite.config.ts`):
```typescript
build: {
  outDir: '../wwwroot',      // 输出目录
  emptyOutDir: true,         // 清空输出目录
}
```

### 后端构建
```
dotnet restore           # 还原依赖
  ↓
dotnet build            # 编译项目
  ↓
dotnet run              # 运行应用
```

---

## 🚀 快速开始

### 首次运行
```batch
1. 确保已安装:
   - .NET 8.0 SDK
   - Node.js 18+

2. 双击运行:
   启动系统.bat

3. 等待构建完成（首次较慢）

4. 浏览器自动打开:
   http://localhost:6098
```

### 日常开发

**前端代码修改后**:
```batch
# 方式1: 重新构建前端
构建并部署前端.bat

# 方式2: 手动构建
cd RadarSystem.WebAPI\ClientApp
npm run build
```

**后端代码修改后**:
```batch
# 使用热重载模式
.\start.ps1 -DevMode

# 或手动重启
cd RadarSystem.WebAPI
dotnet run
```

---

## ⚙️ 环境要求

### 必需
- ✅ .NET 8.0 SDK (或更高版本)
  - 下载: https://dotnet.microsoft.com/download
  - 验证: `dotnet --version`

### 可选（前端开发）
- ⭕ Node.js 18+
  - 下载: https://nodejs.org
  - 验证: `node --version`

**说明**: 如果已有 `wwwroot` 构建产物，可以不安装 Node.js

---

## 🔧 常见问题

### Q1: 前端构建失败
**症状**: "Could not load .../views/XXX.vue"

**解决方案**:
```batch
cd RadarSystem.WebAPI\ClientApp
# 删除依赖
rd /s /q node_modules
del package-lock.json
# 重新安装
npm install
# 重新构建
npm run build
```

---

### Q2: 端口被占用
**症状**: "地址已在使用中"

**解决方案**:
```batch
# 查找占用进程
netstat -ano | findstr "6098"
netstat -ano | findstr "8099"

# 结束进程
taskkill /F /PID <进程ID>
```

---

### Q3: npm install 很慢
**解决方案**:
```batch
# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 或使用 pnpm（更快）
npm install -g pnpm
cd RadarSystem.WebAPI\ClientApp
pnpm install
```

---

### Q4: 后端编译失败
**解决方案**:
```batch
cd RadarSystem.WebAPI
# 清理
dotnet clean
# 还原
dotnet restore
# 详细构建
dotnet build --verbosity normal
```

---

## 📝 手动操作指南

### 仅构建前端
```batch
cd RadarSystem.WebAPI\ClientApp
npm install
npm run build
```

### 仅构建后端
```batch
cd RadarSystem.WebAPI
dotnet build --configuration Release
```

### 仅启动后端
```batch
cd RadarSystem.WebAPI
dotnet run --configuration Release
```

### 前端开发模式（热重载）
```batch
cd RadarSystem.WebAPI\ClientApp
npm run dev
# 访问: http://localhost:8080
```

---

## 🌐 访问地址

启动后访问:
- 🌐 **前端界面**: http://localhost:6098
- 📡 **API 服务**: http://localhost:8099
- 📖 **API 文档**: http://localhost:8099/swagger

默认账户:
- 👤 **用户名**: admin
- 🔑 **密码**: admin123

---

## 📚 相关文档

- `快速启动指南.txt` - 快速上手指南
- `系统开发规则总览.md` - 开发规范
- `后端开发规则_JavaReference.md` - 后端开发参考
- `前端开发规则_RadarContrl.md` - 前端开发参考
- `升级参考代码规定.md` - 升级参考说明
- `README.md` - 项目说明

---

**最后更新**: 2025-11-02

