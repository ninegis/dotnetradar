# 语言显示修复说明

## 修复时间
2025-11-02

## 修复的问题

### 问题 1: 工具栏工具默认显示英文
**症状**: 刚进入系统时，点击"工具"按钮，下拉菜单中的所有工具都显示英文（如 "Project Config" 而不是 "项目配置"）

**原因**: 
- Store 中 `language` 默认值是字符串 `"0"`（中文），这是正确的
- 但 i18n 的 `locale` 没有在组件初始化时根据 store 的值正确设置
- 导致判断 `store.sysinfo.config.language==="0"` 时，虽然值是 "0"，但 i18n locale 可能未初始化

**修复**:
1. **Header.vue**: 在 `onMounted` 时根据 store 的值初始化 i18n locale
2. **CesiumContainer.vue**: 确保默认语言是中文
3. **添加 watch**: 监听 store 语言变化，同步更新 i18n locale

**修改文件**:
- `RadarContrl/src/components/Header/Header.vue`
- `RadarContrl/src/components/CesiumContainer.vue`

---

### 问题 2: 语言切换按钮默认显示英文
**症状**: 默认页面是中文，但语言切换按钮旁边默认显示"English"而不是"中文"

**原因**: 
- 语言切换下拉菜单只显示图标，没有显示当前选中的语言
- 用户无法直观看到当前语言状态

**修复**:
1. 在语言切换按钮旁添加当前语言文本显示
2. 显示逻辑：`store.sysinfo.config.language==="0"` 显示"中文"，否则显示"English"
3. 下拉菜单项添加选中状态样式

**修改文件**:
- `RadarContrl/src/components/Header/Header.vue`

---

## 修复详情

### Header.vue 修改

#### 1. 添加 computed 属性
```javascript
const currentLanguage = computed(() => {
  return store.sysinfo.config.language === "0" ? 0 : 1;
});
```

#### 2. 在 onMounted 中初始化语言
```javascript
onMounted(() => {
  // ✅ 修复：根据 store 中的语言设置初始化 i18n locale
  if (store.sysinfo.config.language === "0") {
    locale.value = 'zh';
  } else {
    locale.value = 'en';
  }
});
```

#### 3. 添加 watch 监听语言变化
```javascript
watch(() => store.sysinfo.config.language, (newLang) => {
  if (newLang === "0") {
    locale.value = 'zh';
  } else {
    locale.value = 'en';
  }
});
```

#### 4. 显示当前语言文本
```html
<span style="font-size: 12px;">
  {{store.sysinfo.config.language==="0"?$t('common.chinese'):$t('common.english')}}
</span>
```

---

### CesiumContainer.vue 修改

#### 确保默认语言是中文
```javascript
if (store.sysinfo.config.language==="1"){
  locale.value = 'en';
  store.sysinfo.title = store.sysinfo.config.i18Title;
} else {
  // ✅ 确保默认语言是中文
  locale.value = 'zh';
}
```

---

## 语言判断逻辑

### Store 中的语言值
```javascript
language: "0"  // "0" = 中文, "1" = English
```

### i18n locale 值
```javascript
locale.value = 'zh'  // 'zh' = 中文, 'en' = English
```

### 工具栏语言判断
```javascript
// ToolBar.vue 第44行
{{store.sysinfo.config.language==="0"?item.title:item.englishTitle}}

// ToolBar.vue 第63行  
:content="store.sysinfo.config.language==='0'?item.content:item.englishName"
```

---

## 验证方法

### 验证问题 1 (工具栏中文显示)
1. 重新构建并部署前端
2. 刷新浏览器（硬刷新：Ctrl+Shift+R）
3. 登录系统
4. 点击"工具"按钮
5. 确认下拉菜单显示中文：
   - ✅ "项目配置"（不是 "Project Config"）
   - ✅ "雷达配置"（不是 "Radar Config"）
   - ✅ "图像配置"（不是 "Image Config"）
   - ✅ 等等...

### 验证问题 2 (语言切换按钮显示)
1. 刷新浏览器
2. 查看右上角语言切换按钮
3. 确认显示"中文"（不是 "English"）
4. 点击下拉菜单，确认"中文"项有选中样式

---

## 重新构建步骤

```batch
# 方式1: 使用 build-frontend.bat（会构建并启动）
build-frontend.bat

# 方式2: 仅构建前端
cd RadarContrl
npm run build
# 然后复制 dist 到 wwwroot

# 方式3: 使用构建脚本
构建并部署前端.bat
```

---

## 相关文件

### 修改的文件
1. `RadarContrl/src/components/Header/Header.vue` - 语言初始化和显示
2. `RadarContrl/src/components/CesiumContainer.vue` - 语言初始化
3. `RadarContrl/src/store/index.js` - 语言默认值（之前已修复）

### 相关组件
- `ToolBar.vue` - 工具栏组件，使用语言判断显示中英文
- `Header.vue` - 头部组件，包含语言切换按钮
- `CesiumContainer.vue` - 主容器组件，初始化时设置语言

---

## 技术细节

### 语言同步机制
1. **Store**: `store.sysinfo.config.language` = "0" | "1"
2. **i18n**: `locale.value` = 'zh' | 'en'
3. **同步**: Header 组件监听 store 变化，同步更新 i18n locale

### 初始化顺序
```
1. Store 初始化 (language = "0")
   ↓
2. CesiumContainer.onMounted
   - 设置 locale.value = 'zh'
   ↓
3. Header.onMounted
   - 再次确认 locale.value = 'zh'
   ↓
4. ToolBar 渲染
   - 使用 store.sysinfo.config.language==="0" 判断
   - 显示中文菜单项
```

---

**修复人**: AI Assistant  
**审核状态**: 待测试




