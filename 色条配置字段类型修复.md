# 色条配置字段类型修复

**错误**: `The requested operation requires an element of type 'String', but the target element has type 'Number'`

---

## 🐛 问题根源

### 后端字段解析

**ProtocolController.cs**:
```csharp
// ❌ 使用int.Parse和double.Parse，期望字符串
if (body.TryGetProperty("hslHStart", out var hslHStart))
    colorBarConfig.HslHStart = int.Parse(hslHStart.GetString() ?? "0");

if (body.TryGetProperty("filterAlpha", out var filterAlpha))
    colorBarConfig.FilterAlpha = double.Parse(filterAlpha.GetString() ?? "0.8");
```

### 前端发送数据

**ColorConfig.vue**:
```javascript
// ❌ 发送的是数字
const params = {
  hslHStart: 240,          // Number
  hslHEnd: 0,              // Number
  filterAlpha: 0.5,        // Number
  filterMin: -50,          // Number
  filterMax: 50            // Number
};
```

**问题**: 后端调用`.GetString()`获取数字值，返回null，导致错误

---

## ✅ 修复方案

### 方案: 前端转为字符串

**修复后**:
```javascript
const params = {
  // ✅ 转为字符串
  hslHStart: String(currentColorConfig.hslHStart),  // "240"
  hslHEnd: String(currentColorConfig.hslHEnd),      // "0"
  filterAlpha: String(currentColorConfig.filterAlpha),  // "0.5"
  filterMin: String(currentColorConfig.filterMin),      // "-50"
  filterMax: String(currentColorConfig.filterMax)       // "50"
};
```

**适用位置**:
- `saveDefaultConfig()` - 保存默认配置
- `saveColorConfig()` - 用户手动保存

---

## 🔄 完整的数据流程

```
前端发送:
{
  hslHStart: "240",    ← String
  filterAlpha: "0.5"   ← String
}
  ↓
后端接收:
body.TryGetProperty("hslHStart", out var hslHStart)
  ↓
hslHStart.GetString() → "240"  ✅
  ↓
int.Parse("240") → 240  ✅
  ↓
colorBarConfig.HslHStart = 240  ✅
  ↓
保存到数据库成功 ✅
```

---

## 📋 需要转换的字段

### HSL相关（int.Parse）

- `hslHStart` → String
- `hslHEnd` → String

### 透明通道相关（double.Parse）

- `filterAlpha` → String
- `filterMin` → String
- `filterMax` → String

### 不需要转换的字段

- `colorSchemeType` - GetInt32()
- `minValue` - GetDouble()
- `maxValue` - GetDouble()
- `hslDirection` - GetInt32()
- `hslS` - GetDouble()
- `hslL` - GetDouble()
- `classCount` - GetInt32()
- `autoAdaptRange` - GetBoolean()
- `adaptBufferRatio` - GetDouble()
- `filterEnable` - GetInt32()

---

## ✅ 修复清单

| 位置 | 字段 | 修复 |
|-----|------|------|
| `saveDefaultConfig()` | hslHStart | ✅ String() |
| `saveDefaultConfig()` | hslHEnd | ✅ String() |
| `saveDefaultConfig()` | filterAlpha | ✅ String() |
| `saveDefaultConfig()` | filterMin | ✅ String() |
| `saveDefaultConfig()` | filterMax | ✅ String() |
| `saveColorConfig()` | hslHStart | ✅ String() |
| `saveColorConfig()` | hslHEnd | ✅ String() |
| `saveColorConfig()` | filterAlpha | ✅ String() |
| `saveColorConfig()` | filterMin | ✅ String() |
| `saveColorConfig()` | filterMax | ✅ String() |

---

## 🚀 测试验证

**刷新前端页面测试！**

**预期日志**:
```
ColorConfig.onMounted - 页面已挂载
开始异步加载配置...
res.data.code: 404
未找到配置，创建默认配置并保存到数据库
设置默认配置，类型: displacement
保存默认配置: { hslHStart: "240", filterAlpha: "0.5", ... }  ← String
保存默认配置成功! ✅
默认配置已保存到数据库
```

**预期效果**:
1. ✅ 页面正常打开
2. ✅ 默认配置保存成功
3. ✅ 色条预览显示
4. ✅ 没有报错

---

**修复完成！刷新页面测试！** 🔧

**现在应该能正常打开并保存配置了！** 🎉

