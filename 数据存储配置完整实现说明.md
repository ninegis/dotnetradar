# 数据存储配置完整实现说明

**实现时间**: 2025-10-25  
**页面**: 工具 → 存储配置 (StorageConfig.vue) ⭐ 新增

---

## 🎯 功能概述

### 核心功能

1. **磁盘使用情况监控** 📊
   - 总空间、已用空间、剩余空间
   - 使用率百分比
   - 实时进度条显示

2. **自动清理配置** 🗑️
   - 启用开关
   - 磁盘空间阈值设置
   - 数据保留天数设置
   - 清理数据类型选择

3. **图像压缩配置** 🖼️
   - 启用开关
   - 图像质量调节

4. **存储路径配置** 📁
   - 数据存储路径
   - 备份存储路径

5. **自动备份配置** 💾
   - 启用开关
   - 备份间隔设置
   - 最大备份数量设置

6. **手动操作** ⚡
   - 立即执行数据清理
   - 立即执行数据备份

---

## 📊 数据库表结构

### data_storage_configs表 ⭐ 新建

```sql
CREATE TABLE data_storage_configs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id TEXT NOT NULL UNIQUE,          -- 每个项目一条记录
    
    -- 自动清理配置
    auto_cleanup_enable INTEGER DEFAULT 0,    -- 启用自动清理
    disk_threshold_percent INTEGER DEFAULT 80,-- 磁盘空间阈值(%)
    data_retention_days INTEGER DEFAULT 90,   -- 数据保留天数
    delete_raw_data INTEGER DEFAULT 0,        -- 删除原始数据
    delete_image_data INTEGER DEFAULT 0,      -- 删除图像数据
    delete_analysis_data INTEGER DEFAULT 0,   -- 删除分析数据
    
    -- 图像压缩配置
    image_quality INTEGER DEFAULT 85,         -- 图像质量(1-100)
    image_compression_enable INTEGER DEFAULT 1,-- 启用图像压缩
    
    -- 存储路径配置
    storage_path TEXT DEFAULT './Data',       -- 数据存储路径
    backup_path TEXT,                         -- 备份路径
    
    -- 自动备份配置
    auto_backup_enable INTEGER DEFAULT 0,     -- 启用自动备份
    backup_interval_days INTEGER DEFAULT 7,   -- 备份间隔(天)
    max_backup_count INTEGER DEFAULT 5,       -- 最大备份数量
    
    create_time TEXT NOT NULL,
    update_time TEXT,
    
    FOREIGN KEY (project_id) REFERENCES Projects(ProjectId) ON DELETE CASCADE
);
```

---

## 🎨 页面布局

```
┌──────────────────────────────────────────┐
│  存储配置                                 │
├──────────────────────────────────────────┤
│ [刷新磁盘信息]  [保存配置]                │
├──────────────────────────────────────────┤
│ 项目: [项目1 ▼]                           │
│                                           │
│ ══════ 磁盘使用情况 ══════                │
│                                           │
│ ┌─ 磁盘信息 ─────────────┐  85% 已使用   │
│ │ 总空间         已用空间        │        │
│ │ 500.00 GB      425.00 GB       │        │
│ │                                │        │
│ │ 剩余空间       驱动器          │        │
│ │ 75.00 GB       C:              │        │
│ │                                │        │
│ │ [━━━━━━━━━━━━━━━━━●━━] 85%   │        │
│ └─────────────────────────┘              │
│                                           │
│ ══════ 自动清理配置 ══════                │
│                                           │
│ 启用自动清理: [●开启 ○关闭]              │
│ 开启后磁盘空间不足时自动清理旧数据        │
│                                           │
│ ⚠ 当磁盘使用率超过阈值时，自动删除超过保留期的数据 │
│    请谨慎配置，删除的数据无法恢复！        │
│                                           │
│ 磁盘空间阈值: [━━━━━━●━━━] 80%          │
│               50% 70% 80% 90% 95%        │
│               [80]                        │
│ 磁盘使用率超过此值时触发自动清理          │
│                                           │
│ 数据保留天数: [90] 天                     │
│ 超过此天数的数据将被清理（建议：90天）    │
│                                           │
│ 清理数据类型:                             │
│ ☐ 原始雷达数据                            │
│ ☐ 图像数据                                │
│ ☐ 分析结果数据                            │
│                                           │
│ ══════ 图像压缩配置 ══════                │
│                                           │
│ 启用图像压缩: [●开启 ○关闭]              │
│ 压缩图像可节省存储空间                    │
│                                           │
│ 图像质量: [━━━━━━●━━━] 85               │
│           最低  中等  高  最高            │
│           [85]                            │
│ 质量越高，文件越大（建议：85）            │
│                                           │
│ ══════ 存储路径配置 ══════                │
│                                           │
│ 数据存储路径: [./Data______] [浏览]      │
│                                           │
│ ══════ 自动备份配置 ══════                │
│                                           │
│ 启用自动备份: [○开启 ●关闭]              │
│ 定期备份数据库到指定位置                  │
│                                           │
│ 备份路径: [./Backup_____] [浏览]         │
│ 备份间隔: [7] 天  (建议：7天)             │
│ 最大备份数量: [5] 个                      │
│ 保留最近N个备份文件，超过自动删除旧备份   │
│                                           │
│ ══════ 数据管理操作 ══════                │
│                                           │
│ [立即执行数据清理]                        │
│ 根据当前配置立即清理数据                  │
│                                           │
│ [立即执行数据备份]                        │
│ 手动触发数据库备份                        │
└──────────────────────────────────────────┘
```

---

## 🔄 完整功能流程

### 流程1: 页面加载

```
用户打开"存储配置"页面
  ↓
onMounted执行
  ↓
loadStorageConfig()
  GET /api/storage/config/PRJ001
  ↓
  ├─ 200 → 加载已保存的配置
  └─ 404 → 使用默认值
  ↓
loadDiskInfo()
  GET /api/storage/diskinfo/PRJ001
  ↓
显示磁盘使用情况:
  总空间: 500GB
  已用: 425GB (85%)
  剩余: 75GB
  ↓
页面显示完整信息 ✅
```

### 流程2: 配置自动清理

```
用户配置:
  启用自动清理: 开启
  磁盘空间阈值: 80%
  数据保留天数: 90天
  清理类型: ☑原始数据 ☑图像数据
  ↓
点击"保存配置"
  ↓
POST /api/storage/config
  {
    projectId: "PRJ001",
    autoCleanupEnable: true,
    diskThresholdPercent: 80,
    dataRetentionDays: 90,
    deleteRawData: true,
    deleteImageData: true,
    deleteAnalysisData: false
  }
  ↓
保存到 data_storage_configs表
  ↓
系统后台定时检查:
  IF (磁盘使用率 > 80%) {
    删除90天前的原始数据和图像数据
  }
```

### 流程3: 手动执行清理

```
用户点击"立即执行数据清理"
  ↓
显示确认对话框:
  "确定要执行数据清理吗？
   将删除90天前的数据，此操作不可撤销！"
  ↓
用户确认
  ↓
POST /api/storage/cleanup
  {
    projectId: "PRJ001"
  }
  ↓
后端执行:
  cutoffDate = NOW() - 90天
  
  IF (deleteRawData) {
    DELETE FROM RadarData 
    WHERE project_id='PRJ001' AND timestamp < cutoffDate
  }
  
  IF (deleteImageData) {
    DELETE FROM RadarImages
    WHERE project_id='PRJ001' AND create_time < cutoffDate
  }
  ↓
返回:
  {
    deletedCount: 1523,
    cutoffDate: "2025-07-27"
  }
  ↓
提示: "数据清理完成！共删除1523条记录"
  ↓
自动刷新磁盘信息
  ✅ 剩余空间增加
```

---

## 💡 功能详解

### 功能1: 磁盘使用情况

**展示信息**:
- 总空间（GB）
- 已用空间（GB）
- 剩余空间（GB）
- 使用率百分比
- 进度条（颜色：绿色<80%，黄色80-90%，红色>90%）

**实时性**:
- 页面加载时查询
- 点击"刷新磁盘信息"更新
- 清理后自动刷新

---

### 功能2: 自动清理策略

**触发条件**:
```
磁盘使用率 > 阈值（如80%）
AND
自动清理已启用
```

**清理规则**:
```
删除条件:
  数据创建时间 < (当前时间 - 保留天数)

清理范围:
  ☑ 原始雷达数据（radar_data表）
  ☑ 图像数据（radar_images表）
  ☑ 分析结果数据（待实现）
```

**安全机制**:
- 必须手动启用
- 明确的保留期设置
- 可选择清理的数据类型
- 手动清理需要确认

---

### 功能3: 图像压缩

**压缩质量级别**:
| 质量 | 文件大小 | 图像效果 | 推荐场景 |
|-----|---------|---------|---------|
| 1-30 | 最小 | 明显失真 | 仅预览 |
| 50 | 中等 | 轻微失真 | 一般场景 |
| 85 | 较大 | 几乎无损 | 推荐 ⭐ |
| 100 | 最大 | 无损 | 归档保存 |

**存储空间节省**:
```
原始图像: 10MB
质量85: 2.5MB (节省75%)
质量50: 800KB (节省92%)
```

---

### 功能4: 自动备份

**备份策略**:
- 每N天自动备份数据库
- 保留最近M个备份文件
- 超过数量自动删除最旧的备份

**备份命名**:
```
radar_backup_PRJ001_20251025_100523.db
            项目ID  日期      时间
```

**备份轮转**:
```
maxBackupCount = 5

备份文件:
  radar_backup_20251025.db  ← 最新
  radar_backup_20251018.db
  radar_backup_20251011.db
  radar_backup_20251004.db
  radar_backup_20250927.db  ← 最旧
  
新备份产生:
  radar_backup_20251101.db  ← 新
  自动删除: radar_backup_20250927.db ✅
```

---

## 🚀 后端接口

### 接口1: 获取存储配置

**端点**: `GET /api/storage/config/{projectId}`

**返回**:
```json
{
  "code": 200,
  "data": {
    "projectId": "PRJ001",
    "autoCleanupEnable": true,
    "diskThresholdPercent": 80,
    "dataRetentionDays": 90,
    "deleteRawData": true,
    "deleteImageData": true,
    "deleteAnalysisData": false,
    "imageQuality": 85,
    "imageCompressionEnable": true,
    "storagePath": "./Data",
    "backupPath": "./Backup",
    "autoBackupEnable": true,
    "backupIntervalDays": 7,
    "maxBackupCount": 5
  }
}
```

---

### 接口2: 更新存储配置

**端点**: `POST /api/storage/config`

**请求**:
```json
{
  "projectId": "PRJ001",
  "autoCleanupEnable": true,
  "diskThresholdPercent": 80,
  "dataRetentionDays": 90,
  "deleteRawData": true,
  "deleteImageData": true,
  "imageQuality": 85,
  "imageCompressionEnable": true
}
```

---

### 接口3: 获取磁盘信息

**端点**: `GET /api/storage/diskinfo/{projectId}`

**返回**:
```json
{
  "code": 200,
  "data": {
    "totalSpace": 536870912000,
    "freeSpace": 80530636800,
    "usedSpace": 456340275200,
    "usedPercent": 85,
    "driveName": "C:",
    "driveFormat": "NTFS",
    "isReady": true
  }
}
```

---

### 接口4: 执行数据清理

**端点**: `POST /api/storage/cleanup`

**请求**:
```json
{
  "projectId": "PRJ001"
}
```

**返回**:
```json
{
  "code": 200,
  "message": "数据清理完成",
  "data": {
    "deletedCount": 1523,
    "cutoffDate": "2025-07-27T10:00:00"
  }
}
```

---

## 📋 配置建议

### 推荐配置1: 标准配置

```
自动清理: 开启
磁盘阈值: 80%
数据保留: 90天
清理类型: 原始数据 + 图像数据
图像压缩: 开启
图像质量: 85
自动备份: 开启
备份间隔: 7天
备份数量: 5个
```

**适用**: 常规项目，平衡存储和性能

---

### 推荐配置2: 长期存档

```
自动清理: 关闭
数据保留: 365天
图像压缩: 开启
图像质量: 100（无损）
自动备份: 开启
备份间隔: 1天
备份数量: 30个
```

**适用**: 重要项目，需要长期保存

---

### 推荐配置3: 节约模式

```
自动清理: 开启
磁盘阈值: 70%
数据保留: 30天
清理类型: 全选
图像压缩: 开启
图像质量: 50
自动备份: 关闭
```

**适用**: 存储空间有限的环境

---

## 📊 前后端字段映射

| 前端字段 | 后端字段 | 数据库字段 |
|---------|---------|-----------|
| `autoCleanupEnable` | `AutoCleanupEnable` | `auto_cleanup_enable` |
| `diskThresholdPercent` | `DiskThresholdPercent` | `disk_threshold_percent` |
| `dataRetentionDays` | `DataRetentionDays` | `data_retention_days` |
| `deleteRawData` | `DeleteRawData` | `delete_raw_data` |
| `deleteImageData` | `DeleteImageData` | `delete_image_data` |
| `deleteAnalysisData` | `DeleteAnalysisData` | `delete_analysis_data` |
| `imageQuality` | `ImageQuality` | `image_quality` |
| `imageCompressionEnable` | `ImageCompressionEnable` | `image_compression_enable` |
| `storagePath` | `StoragePath` | `storage_path` |
| `backupPath` | `BackupPath` | `backup_path` |
| `autoBackupEnable` | `AutoBackupEnable` | `auto_backup_enable` |
| `backupIntervalDays` | `BackupIntervalDays` | `backup_interval_days` |
| `maxBackupCount` | `MaxBackupCount` | `max_backup_count` |

---

## 📝 修改文件清单

### 前端

| 文件 | 说明 |
|-----|------|
| `RadarContrl/src/components/ToolBar/Tool/Storage/StorageConfig.vue` | 新建存储配置页面 ⭐ |

### 后端

| 文件 | 说明 |
|-----|------|
| `RadarSystem.Data/Models/DataStorageConfigEntity.cs` | 新建数据存储配置实体 ⭐ |
| `RadarSystem.Data/Context/RadarDbContext.cs` | 添加`DataStorageConfigs` DbSet |
| `RadarSystem.WebAPI/Program.cs` | 添加data_storage_configs表创建逻辑 |
| `RadarSystem.WebAPI/Controllers/DataStorageConfigController.cs` | 新建存储配置控制器 ⭐ |

---

## 🎯 使用场景

### 场景1: 磁盘空间不足预警

```
磁盘使用率: 88%
  ↓
超过阈值(80%)
  ↓
自动清理触发
  ↓
删除90天前的数据
  ↓
磁盘使用率降至: 75%
  ✅ 预警解除
```

---

### 场景2: 定期清理维护

```
每周一次手动清理:
  1. 打开存储配置
  2. 查看磁盘使用情况
  3. 点击"立即执行数据清理"
  4. 确认清理
  5. 查看删除记录数
  6. 磁盘空间释放 ✅
```

---

### 场景3: 数据备份

```
每周自动备份:
  系统后台任务
  ↓
  每7天执行一次
  ↓
  备份数据库到./Backup/
  ↓
  保留最近5个备份
  ↓
  超过5个自动删除最旧的
```

---

## ✅ 总结

### 完成的工作

1. ✅ 创建数据存储配置实体
2. ✅ 创建数据库表
3. ✅ 创建后端控制器（4个接口）
4. ✅ 创建前端页面
5. ✅ 磁盘信息监控
6. ✅ 自动清理配置
7. ✅ 图像压缩配置
8. ✅ 自动备份配置
9. ✅ 手动清理和备份
10. ✅ 编译成功（0错误0警告）

### 核心功能

- 📊 磁盘使用情况实时监控
- 🗑️ 自动/手动数据清理
- 🖼️ 图像压缩节省空间
- 💾 自动备份保护数据
- 📁 存储路径配置
- ⚙️ 项目级独立配置

### 编译状态

```
✅ 已成功生成
   0 个警告
   0 个错误
```

---

**实现完成！** 🎉

**下一步**: 
1. 在前端工具栏添加"存储配置"入口
2. 重启后端（数据库会自动创建表）
3. 刷新前端测试存储配置功能

**新增功能**:
- ✅ 磁盘监控
- ✅ 自动清理
- ✅ 图像压缩
- ✅ 自动备份
- ✅ 项目级管理

