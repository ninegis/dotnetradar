# 图像生成和速度配置完整修复报告

**修复时间**: 2025-10-25  
**涉及页面**: 
1. 雷达图像生成配置 (ImageGenerate.vue)
2. 速度指标配置 (SpeedTarget.vue)

---

## 🎯 核心需求

### 用户要求

1. **每个项目只有一条配置记录**
2. **首次打开时加载已保存的配置**
3. **切换项目时自动加载该项目的配置**
4. **数据库没有记录时不回显，使用默认值**
5. **有记录时只更新，不创建新记录**
6. **速度配置支持多选和复选框两种方式**

---

## ✅ 修复1: 雷达图像生成配置

### 数据库约束

```sql
CREATE TABLE image_analysis_configs (
    id TEXT PRIMARY KEY,
    project_id TEXT NOT NULL UNIQUE,  -- ✅ UNIQUE约束，每个项目只有一条记录
    ...
);
```

### 后端逻辑（更新或创建）

**原则**: 
- ✅ 先查询是否存在记录
- ✅ 存在 → 更新 (UPDATE)
- ✅ 不存在 → 创建 (INSERT)

```csharp
// 查找现有配置
var existingConfig = await _dbContext.ImageAnalysisConfigs
    .FirstOrDefaultAsync(c => c.ProjectId == request.ProjectId);

if (existingConfig != null)
{
    // ✅ 更新现有记录
    existingConfig.GenImageType = request.GenImageType ?? existingConfig.GenImageType;
    existingConfig.DefoInterval = request.DefoInterval ?? existingConfig.DefoInterval;
    existingConfig.ScatInterval = request.ScatInterval ?? existingConfig.ScatInterval;
    existingConfig.DefoNumber = request.DefoNumber ?? existingConfig.DefoNumber;
    existingConfig.ScatNumber = request.ScatNumber ?? existingConfig.ScatNumber;
    existingConfig.UpdateTime = DateTime.Now;
}
else
{
    // ✅ 创建新记录
    var newConfig = new ImageAnalysisConfigEntity
    {
        Id = Guid.NewGuid().ToString(),
        ProjectId = request.ProjectId,
        GenImageType = request.GenImageType ?? "01",
        DefoInterval = request.DefoInterval ?? 60,
        ...
    };
    _dbContext.ImageAnalysisConfigs.Add(newConfig);
}

await _dbContext.SaveChangesAsync();
```

### 前端逻辑（加载配置）

```javascript
// ✅ 页面加载时
onMounted(async () => {
  if (store.projectInfo.projectSelected) {
    store.radarInfo.projectId = String(store.projectInfo.projectSelected);
    await loadImageConfig();  // 加载配置
  }
});

// ✅ 项目切换时
const onProjectChange = async () => {
  store.radarInfo.projectId = String(store.projectInfo.projectSelected);
  await loadImageConfig();  // 重新加载配置
};

// ✅ 加载配置
const loadImageConfig = async () => {
  const res = await axios.get('/api/protocol/query/project/imageAnalysisConfig/' + projectId);
  
  if (res.data.code === 200) {
    // ✅ 找到配置，加载到页面
    store.radarInfo.imageAnalysisConfig = {
      genImageType: res.data.data.genImageType,
      followDefoInterval: res.data.data.defoInterval,
      scatInterval: res.data.data.scatInterval,
      followDefoNumber: res.data.data.defoNumber,
      scatNumber: res.data.data.scatNumber
    };
  } else if (res.data.code === 404) {
    // ✅ 未找到配置，使用默认值
    store.radarInfo.imageAnalysisConfig = {
      genImageType: '01',
      followDefoInterval: 60,
      scatInterval: 60,
      followDefoNumber: 10,
      scatNumber: 10
    };
  }
};
```

### 完整流程

```
首次打开页面（项目PRJ001）
  ↓
onMounted() 执行
  ↓
GET /api/protocol/query/project/imageAnalysisConfig/PRJ001
  ↓
数据库查询
  ├─ 找到记录 → 返回 { code: 200, data: {...} }
  │   ↓
  │   加载配置到页面
  │   ✅ 显示已保存的配置
  │
  └─ 未找到记录 → 返回 { code: 404 }
      ↓
      使用默认值
      ✅ 显示默认配置
  ↓
用户修改配置
  ↓
点击"保存配置"
  ↓
POST /api/protocol/update/project/imageAnalysisConfig
  ↓
数据库操作
  ├─ 记录存在 → UPDATE (只更新)
  └─ 记录不存在 → INSERT (创建新记录)
  ↓
✅ 保存成功
  ↓
下次打开时显示已保存的配置 ✅
```

---

## ✅ 修复2: 速度指标配置

### 数据库约束

```sql
CREATE TABLE speed_indices (
    id TEXT PRIMARY KEY,
    project_id TEXT NOT NULL UNIQUE,  -- ✅ UNIQUE约束，每个项目只有一条记录
    time_units TEXT,                  -- ✅ 逗号分隔的字符串 "00,01,02"
    enable_30min INTEGER DEFAULT 0,
    enable_1hour INTEGER DEFAULT 0,
    enable_1day INTEGER DEFAULT 0,
    enable_3day INTEGER DEFAULT 0,
    enable_1week INTEGER DEFAULT 0,
    enable_1month INTEGER DEFAULT 0,
    ...
);
```

### 时间单位代码映射

**统一使用前端代码**:

| 代码 | 时间单位 | 后端字段 |
|-----|---------|---------|
| `00` | 30分钟 | `Enable30Min` |
| `01` | 1小时 | `Enable1Hour` |
| `02` | 1天 | `Enable1Day` |
| `03` | 3天 | `Enable3Day` |
| `04` | 1周 | `Enable1Week` |
| `05` | 1月 | `Enable1Month` |

### 前端UI（支持两种模式）

#### 模式1: 下拉多选（默认）

```vue
<el-select v-model="selectedTimeUnits" multiple>
  <el-option label="30分钟" value="00" />
  <el-option label="1小时" value="01" />
  <el-option label="1天" value="02" />
  <el-option label="3天" value="03" />
  <el-option label="1周" value="04" />
  <el-option label="1月" value="05" />
</el-select>
```

**特点**:
- 紧凑，占用空间小
- 适合空间有限的界面

#### 模式2: 复选框（可选）

```vue
<el-checkbox-group v-model="selectedTimeUnits">
  <el-checkbox label="00">30分钟</el-checkbox>
  <el-checkbox label="01">1小时</el-checkbox>
  <el-checkbox label="02">1天</el-checkbox>
  <el-checkbox label="03">3天</el-checkbox>
  <el-checkbox label="04">1周</el-checkbox>
  <el-checkbox label="05">1月</el-checkbox>
</el-checkbox-group>
```

**特点**:
- 直观，一目了然
- 适合选项不多的情况

#### 模式切换

```vue
<el-switch 
  v-model="useCheckboxes"
  active-text="使用复选框"
  inactive-text="使用下拉框"
/>
```

### 已选择的时间单位显示

```vue
<el-tag v-for="unit in selectedTimeUnits" closable @close="removeTimeUnit(unit)">
  {{ getTimeUnitLabel(unit) }}
</el-tag>
```

**效果**:
- [30分钟 ×] [1天 ×] [1周 ×]
- 点击×可快速删除

### 完整流程

```
首次打开页面
  ↓
onMounted() 执行
  ↓
GET /api/protocol/query/speed/target/PRJ001
  ↓
数据库查询
  ├─ 找到记录 → 返回 { code: 200, data: { timeUnits: "00,02,04" } }
  │   ↓
  │   解析: "00,02,04".split(',') = ['00', '02', '04']
  │   ↓
  │   selectedTimeUnits = ['00', '02', '04']
  │   ✅ 显示: [30分钟] [1天] [1周]
  │
  └─ 未找到记录 → 返回 { code: 404 }
      ↓
      selectedTimeUnits = []
      ✅ 显示: 未选择任何时间单位
  ↓
用户修改选择
  ├─ 方式1: 下拉框多选
  └─ 方式2: 复选框
  ↓
selectedTimeUnits = ['00', '01', '02']
  ↓
点击"保存配置"
  ↓
POST /api/protocol/update/speed/target
  {
    projectId: "PRJ001",
    timeUnit: ['00', '01', '02']
  }
  ↓
后端处理
  ├─ 存在记录 → UPDATE
  └─ 不存在 → INSERT
  ↓
保存到数据库:
  time_units = "00,01,02"
  enable_30min = 1
  enable_1hour = 1
  enable_1day = 1
  enable_3day = 0
  enable_1week = 0
  enable_1month = 0
  ↓
✅ 保存成功
  ↓
下次打开显示: [30分钟] [1小时] [1天] ✅
```

---

## 📊 数据存储对比

### 图像生成配置

| 项目ID | 生成类型 | 形变间隔 | 散射间隔 | 形变帧数 | 散射帧数 |
|-------|---------|---------|---------|---------|---------|
| PRJ001 | 01 | 60 | 60 | 10 | 10 |
| PRJ002 | 02 | 30 | 60 | 10 | 10 |

**每个项目一条记录** ✅

### 速度指标配置

| 项目ID | 时间单位 | 30分钟 | 1小时 | 1天 | 3天 | 1周 | 1月 |
|-------|---------|-------|------|-----|-----|-----|-----|
| PRJ001 | 00,02,04 | 1 | 0 | 1 | 0 | 1 | 0 |
| PRJ002 | 01,02,03 | 0 | 1 | 1 | 1 | 0 | 0 |

**每个项目一条记录** ✅

---

## 🎨 速度配置页面布局

```
┌──────────────────────────────────────────┐
│  速度指标配置                             │
├──────────────────────────────────────────┤
│ [保存配置]                                │
├──────────────────────────────────────────┤
│ 项目: [项目1 ▼]                           │
│                                           │
│ ══════ 速度指标配置 ══════                │
│                                           │
│ ┌─ 速度指标说明 ───────────┐             │
│ │ 形变曲线和图像计算基准单位间隔 │        │
│ │ 选择的时间单位将用于计算速度和加速度 │  │
│ └───────────────────────────┘             │
│                                           │
│ 速度时间单位（多选）:                     │
│ [30分钟, 1天, 1周 ▼]                      │
│   • 30分钟                                │
│   • 1小时                                 │
│   • 1天                                   │
│   • 3天                                   │
│   • 1周                                   │
│   • 1月                                   │
│                                           │
│ ══════ 快速选择 ══════                    │
│                                           │
│ 启用单选: [●使用复选框 ○使用下拉框]       │
│                                           │
│ 时间单位:                                 │
│ ☑ 30分钟                                  │
│ ☐ 1小时                                   │
│ ☑ 1天                                     │
│ ☐ 3天                                     │
│ ☑ 1周                                     │
│ ☐ 1月                                     │
│                                           │
│ 已选择:                                   │
│ [30分钟 ×] [1天 ×] [1周 ×]                │
└──────────────────────────────────────────┘
```

---

## 🔄 完整数据流程

### 场景1: 首次配置（数据库无记录）

```
项目PRJ001首次配置
  ↓
打开"图像生成配置"页面
  ↓
GET /api/protocol/query/project/imageAnalysisConfig/PRJ001
  ↓
数据库查询: SELECT * FROM image_analysis_configs WHERE project_id = 'PRJ001'
  ↓
结果: 无记录
  ↓
后端返回: { code: 404, message: "未找到配置" }
  ↓
前端处理:
  if (code === 404) {
    使用默认值
  }
  ↓
页面显示默认配置:
  生成类型: 按帧号
  形变帧数: 10圈
  散射帧数: 10圈
  ↓
用户修改配置
  ↓
点击"保存配置"
  ↓
POST /api/protocol/update/project/imageAnalysisConfig
  ↓
后端查询: 无记录
  ↓
执行: INSERT INTO image_analysis_configs (...)
  ↓
✅ 创建新记录
```

### 场景2: 再次打开（数据库有记录）

```
项目PRJ001再次打开
  ↓
打开"图像生成配置"页面
  ↓
GET /api/protocol/query/project/imageAnalysisConfig/PRJ001
  ↓
数据库查询: SELECT * FROM image_analysis_configs WHERE project_id = 'PRJ001'
  ↓
结果: 找到记录
  {
    gen_image_type: '02',
    defo_interval: 30,
    scat_interval: 60,
    ...
  }
  ↓
后端返回: { code: 200, data: {...} }
  ↓
前端处理:
  if (code === 200) {
    加载配置到页面
  }
  ↓
页面显示已保存的配置:
  生成类型: 按时间间隔 ✅
  形变间隔: 30分钟 ✅
  散射间隔: 60分钟 ✅
  ↓
用户修改配置
  ↓
点击"保存配置"
  ↓
后端查询: 找到记录
  ↓
执行: UPDATE image_analysis_configs SET ... WHERE project_id = 'PRJ001'
  ↓
✅ 只更新记录，不创建新记录
```

### 场景3: 切换项目

```
从PRJ001切换到PRJ002
  ↓
onProjectChange() 执行
  ↓
GET /api/protocol/query/project/imageAnalysisConfig/PRJ002
  ↓
数据库查询PRJ002的配置
  ├─ 有记录 → 显示PRJ002的配置
  └─ 无记录 → 显示默认值
  ↓
✅ 不同项目显示不同配置
```

---

## 📋 修改文件清单

### 前端文件

| 文件 | 修改内容 |
|-----|---------|
| `ImageGenerate.vue` | 1. 添加`loadImageConfig`方法<br>2. 添加`onProjectChange`方法<br>3. `onMounted`时加载配置<br>4. 处理404响应（使用默认值） |
| `SpeedTarget.vue` | 1. 完全重构<br>2. 添加项目选择<br>3. 支持下拉多选和复选框<br>4. 添加已选择标签显示<br>5. 添加加载和保存逻辑 |

### 后端文件

| 文件 | 修改内容 |
|-----|---------|
| `ConfigEntities.cs` | `ImageAnalysisConfigEntity`添加独立字段 |
| `Program.cs` | 添加`image_analysis_configs`表创建逻辑 |
| `ProtocolController.cs` | 1. 优化`UpdateImageAnalysisConfig`（更新或创建）<br>2. 添加`GetSpeedTarget`接口<br>3. 修复时间单位代码映射 |

---

## ✅ 功能对比

### 图像生成配置

| 功能 | 修复前 | 修复后 |
|-----|-------|--------|
| 加载配置 | ❌ 不加载 | ✅ 自动加载 |
| 项目切换 | ❌ 不刷新 | ✅ 自动刷新 |
| 无记录处理 | ❌ 错误 | ✅ 使用默认值 |
| 有记录处理 | ❌ 可能重复创建 | ✅ 只更新 |
| 数据存储 | ❌ ConfigJson | ✅ 独立字段 |

### 速度指标配置

| 功能 | 修复前 | 修复后 |
|-----|-------|--------|
| 选择方式 | ✅ 下拉多选 | ✅ 下拉多选 + 复选框 |
| 加载配置 | ❌ 不加载 | ✅ 自动加载 |
| 项目切换 | ❌ 不刷新 | ✅ 自动刷新 |
| 已选显示 | ❌ 无 | ✅ 标签显示 |
| 无记录处理 | ❌ 错误 | ✅ 使用默认值 |
| 有记录处理 | ❌ 可能重复创建 | ✅ 只更新 |

---

## 🎯 UNIQUE约束的作用

### 防止重复记录

**场景**: 用户多次保存同一项目的配置

**没有UNIQUE约束**:
```sql
-- ❌ 每次保存都INSERT，产生重复记录
INSERT INTO image_analysis_configs (project_id, ...) VALUES ('PRJ001', ...);
INSERT INTO image_analysis_configs (project_id, ...) VALUES ('PRJ001', ...);
INSERT INTO image_analysis_configs (project_id, ...) VALUES ('PRJ001', ...);

-- 结果：3条重复记录
PRJ001 | 01 | 60 | 60
PRJ001 | 02 | 30 | 60  
PRJ001 | 01 | 60 | 60

-- 查询时不知道用哪一条 ❌
```

**有UNIQUE约束**:
```sql
-- ✅ 首次保存：INSERT
INSERT INTO image_analysis_configs (project_id, ...) VALUES ('PRJ001', ...);

-- ✅ 再次保存：UPDATE
UPDATE image_analysis_configs SET gen_image_type='02', defo_interval=30 WHERE project_id='PRJ001';

-- ✅ 再次保存：UPDATE
UPDATE image_analysis_configs SET scat_interval=60 WHERE project_id='PRJ001';

-- 结果：只有一条记录
PRJ001 | 02 | 30 | 60

-- 查询明确 ✅
```

---

## 🚀 测试验证

### 测试1: 图像生成配置

1. **首次打开PRJ001**
   - 预期: 显示默认值（按帧号, 10圈）

2. **修改并保存**
   - 修改: 按时间间隔, 30分钟
   - 预期: 保存成功

3. **关闭并重新打开**
   - 预期: 显示上次保存的配置（按时间间隔, 30分钟）✅

4. **切换到PRJ002**
   - 预期: 显示PRJ002的配置或默认值

5. **切换回PRJ001**
   - 预期: 显示PRJ001的配置（按时间间隔, 30分钟）✅

### 测试2: 速度指标配置

1. **首次打开PRJ001**
   - 预期: 未选择任何时间单位

2. **下拉框选择**
   - 选择: 30分钟, 1天, 1周
   - 预期: 已选择显示3个标签

3. **切换到复选框模式**
   - 预期: 复选框显示，30分钟、1天、1周已勾选

4. **保存配置**
   - 预期: 保存成功

5. **刷新页面**
   - 预期: 显示已保存的配置（30分钟, 1天, 1周）✅

6. **切换项目**
   - 预期: 自动加载新项目的配置

---

## ✅ 总结

### 完成的工作

1. ✅ 图像生成配置 - 加载和保存逻辑
2. ✅ 速度指标配置 - 完全重构
3. ✅ 数据库UNIQUE约束 - 防止重复记录
4. ✅ 更新或创建逻辑 - 智能判断
5. ✅ 404处理 - 使用默认值
6. ✅ 项目切换 - 自动重新加载
7. ✅ 多种选择方式 - 下拉框 + 复选框
8. ✅ 已选择标签 - 可视化显示
9. ✅ 时间单位代码 - 统一映射
10. ✅ 编译成功 - 0错误0警告

### 用户体验提升

- ✅ 打开页面立即看到已保存的配置
- ✅ 切换项目自动加载对应配置
- ✅ 支持两种选择方式（灵活）
- ✅ 已选择的单位一目了然
- ✅ 不会创建重复记录

---

**修复完成！后端编译成功（0错误0警告），刷新前端页面测试！** 🎉

**现在两个页面都支持：**
- ✅ 页面加载时自动加载配置
- ✅ 项目切换时自动重新加载
- ✅ 每个项目只有一条记录
- ✅ 没有记录时使用默认值
- ✅ 有记录时只更新不创建

